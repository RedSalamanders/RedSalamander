# Operation Floodgate Follow-ups — Cloud Parity, Perf, and Proof Depth

> **ARCHIVED 2026-08-09 — SUPERSEDED AS AN EXECUTION QUEUE.** The exact bridge-MOVE
> skipped-child proof and narrowed eventual-consistency follow-up are transferred to
> `../WIP/Operation_Cinderstar_UnifiedRepositoryRiskRemediation_2026-08-09.md`; stale proposals
> are explicitly rejected there. Completed cloud/performance checkpoints remain evidence here.

**Status:** Archived — live remainder transferred to Operation Cinderstar
**Date:** 2026-06-18
**Source plan:** `Specs/Plans/Done/Operation_Floodgate_RiptideMergeCloudDataSafetyAndCloseout_2026-06-17.md`

**2026-08-04 independent-review checkpoint:** `FG-P0-3-FALLBACK-SIZE-ZERO` is now
closed fail-safe: an unproved staged-object size can never be promoted, and the bounded
loopback FTP selftest covers unverifiable and short upload outcomes. `R4-S3-02` is
closed by the self-converging multipart-abort scheduler with cancellation/unload proof;
`R4-TEST-02` is closed by forwarding the optional expected-size capability through the
bridge decorator; and `R4-S3-03` is closed by the pre-transfer S3 multipart planner and
its maximum-object corpus. These rows remain ledger history, not live Floodgate work.

**2026-07-02 folder review:** ~10 rows genuinely open; next action = FG-A1 (destination-side `GetSize` fault-injection hook — the only implemented data-safety block with zero RED-able coverage, `RedSalamander/FolderWindow.FileOperations.State.cpp:8316-8348`). The "Release Decision" section formerly here gated the Riptide/Floodgate closeout (`Operation_Floodgate_RiptideMergeCloudDataSafetyAndCloseout_2026-06-17.md`), which completed 2026-06-17; the only requirement it carried — the short-2xx fake-server fault injection — lives in the FG-P0-3 proof-depth row below.

**2026-07-20 FG-A1 / parallel proof DONE:** the Destination-role bridge decorator, fail-next counter, queue API, and cleanup reset already existed, but no behavioral test armed the hook. `Floodgate_CrossFsMoveDestinationGetSizeFailurePreservesSource` now isolates the serial post-promote failure, while `Floodgate_CrossFsMoveGetSizeFailuresParallelPreserveSourceTree` injects one source and one destination failure across eight children at configured concurrency four. The parallel case captured one durable finding: any copy-phase partial result suppresses MOVE cleanup for the whole tree, retaining all sources even when valid promoted siblings exist. The authoritative bridge contract and structural allowlist are updated. Focused `Floodgate_CrossFsMove*` passed 6/6; the full Fairstream family passed 47/47 (archive `Specs/TestRuns/7d3a1247382a/FileOps/2026-07-20_205550/`); source contracts passed 148/148. This test-only slice changes no production hot path.

**2026-07-22 R4-S3-01 DONE — atomic final-writer publication:** S3's atomic final-key writer now sends
`If-None-Match: *` on both small-object `PutObject` and `CompleteMultipartUpload` when overwrite was not granted;
explicit overwrite remains unconditional. Conditional `412` and relevant `409` responses map to the host's
`ERROR_FILE_EXISTS` conflict and preserve the object that won the race. The failure-first transport injects that
object only after preflight: RED was 34/4, while Debug and test-enabled Release are 38/0 with separate overwrite
controls and exact multipart abort proof. Complete `PluginContractTests` passes in both configurations, source
contracts pass 149/149, and Debug/test-enabled Release/tests-disabled Release focused builds are warning/error-free.
The extra header adds no provider request. Durable contracts are in `Specs/FileSystem/FileSystem_S3.md` and
`Specs/Core/Core_FileSystemBridge.md`.

**2026-07-22 EG-S3-01 DONE — atomic direct directory publication:** within-provider directory COPY/MOVE now carries
the no-overwrite condition through small `CopyObject`, multipart completion, and relay upload. A late `409`/`412`
maps to `ERROR_FILE_EXISTS`; interactive operations refresh and re-enter the normal conflict decision, no-callback
operations fail closed, and MOVE deletes its source only after successful publication. The deterministic small /
multipart / relay × COPY / MOVE matrix preserves the concurrently created destination and source, proves multipart
abort, and covers explicit conditional-relay rejection plus Skip/Overwrite controls. Test-enabled Release provider
contracts pass 37/37 and source contracts pass 149/149. The 16-object same-binary perf contract retains 16 requests,
changes the conditional subset `0 -> 16`, and records `251,041 us -> 253,786 us` (+1.09%) under fixed publication
latency. Durable behavior is in `Specs/FileSystem/FileSystem_S3.md`; evidence is under
`Specs/TestRuns/7d3a1247382a/FileOps/2026-07-22_1740_s3_direct_publication_safe/`.
The production Release provider and final Debug solution builds are warning/error-free; complete Debug provider
contracts pass; and focused Debug FileOps passes 111/111 runnable cases with 20 expected environment skips.

**2026-08-04 EG-S3-02 PROOF CONSUMED — exact S3 source revision:** Emberglass remains the sole implementation owner.
Its repaired transfer plan carries ETag plus VersionId through small copy, every multipart part, relay GET, and final
MOVE deletion. The deterministic replacement matrix is 131/0 in Debug and in each of five test-enabled Release runs;
it covers COPY/MOVE, all three transfer paths, versioned/unversioned buckets, same/changed size, cancellation, and
rollback. The 16-object same-binary shape changes source identity probes/conditional reads from `0/0 -> 16/16` with
median fixed-latency time `536,572 -> 786,180 us` (+46.52%), inside the 175% gate. Durable behavior is in
`Specs/FileSystem/FileSystem_S3.md`; bounded evidence is under
`Specs/TestRuns/7d3a1247382a/FileOps/2026-08-04_1024_s3_source_revision_pinning/`. This checkpoint consumes the cloud
proof and does not create a second Floodgate work package or reopen EG-S3-01.

**2026-08-08 proof-scope correction:** the archive above remains valid evidence that the
captured source revision is used for reads/copy parts. It is not evidence that ordinary MOVE
hides the source key in a versioned bucket: the test expected deletion of only the captured
`VersionId`, which can expose an older version. It also does not prove rollback isolation from a
concurrent destination replacement, and fake-provider success did not serialize the SDK
`CopySource` header where double encoding occurs. A8-S3-01 through A8-S3-03 in
`CodeReview_August8_CorrectnessDataSafetyAndSimplification_2026-08-08.md` are the sole owners of
those newly confirmed defects. Floodgate must not cite the 2026-08-04 archive as their green
proof until the new focused regressions and live-compatible request-serialization evidence pass.

**2026-07-20 FG-P0-2-PERF-RESTAT DONE — final spec/code state and latest finding:** proof-capable bridge MOVE no longer performs the redundant immediate final-path `CreateFileReader` + `GetSize` after writer `Commit` and optional staged promotion. The host queries the optional, separately UUID'd `IFileWriterCommitSizeProof` only for MOVE, only accepts an exact post-Commit byte count, and never branches on provider identity, atomic-writer capability, or staging-name syntax. `IFileWriter::GetPosition` remains only a stream-position report. COPY retains its final-path reader/hash; MOVE retains cleanup-time final size/hash verification before source deletion. Missing, failed, or mismatched proof increments the MOVE fallback metric and performs the legacy fail-closed re-stat, so the existing destination-`GetSize` fault cases remain authoritative safety guards. Dummy, S3 multipart, and Microsoft Drive writers expose the proof from writer-local state without metadata or network I/O. The durable contract is in `Specs/Core/Core_FileSystemBridge.md` and `Specs/Plugins/Plugins_VirtualFileSystem.md`; detailed evidence is closed in `Specs/Plans/Done/Operation_FileOperations_CommitSizeProofRestatOptimization_2026-07-20.md`.

**Protected perf scenario:** cross-filesystem COPY/MOVE of many small files to a high-metadata-cost or remote destination. Primary per-operation metrics are `FileOps.Bridge.ImmediateDestinationSizeProbeCount`, `FileOps.Bridge.ImmediateDestinationSizeProbeUs`, `FileOps.Bridge.MoveCleanupDestinationSizeProbeCount`, `FileOps.Bridge.MoveCleanupDestinationSizeProbeUs`, `FileOps.Bridge.CommitSizeProofCount`, and `FileOps.Bridge.CommitSizeProofFallbackCount`. Deterministic proof requires both a proof-capable Dummy destination path (zero immediate destination-size probes; MOVE retains exactly one cleanup verification per moved file) and a legacy/fallback path (the existing injected `GetSize` failure still returns `ERROR_PARTIAL_COPY` and preserves MOVE sources). Final evidence must be same-machine, test-enabled Release, archived under `Specs/TestRuns/`, and demonstrate no more than one destination metadata size probe per proof-capable moved file without weakening correctness.

**2026-07-20 instrumentation/baseline harness DONE:** the task perf aggregate separates immediate post-commit destination-size probes from MOVE cleanup probes and emits the six metric keys above. `Floodgate_CrossFsMoveCommitSizeProofAvoidsImmediateRestat` drives a 16-file local→Dummy MOVE with deterministic 25 ms Dummy metadata latency and concurrency four, verifies every destination byte plus source deletion, and requires the candidate shape (0 immediate + 16 cleanup probes) during normal execution. Test-enabled builds may set `REDSALAMANDER_FILEOPS_BRIDGE_DISABLE_COMMIT_SIZE_PROOF=1` to obtain the same-binary fallback shape (16 immediate + 16 cleanup probes) for repeatable comparison.

**2026-07-20 same-machine Release baseline PASS:** test-enabled Release build completed with 0 warnings/errors; focused `Floodgate_CrossFsMoveCommitSizeProofAvoidsImmediateRestat` passed 3/3 and archived to `Specs/TestRuns/7d3a1247382a/FileOps/2026-07-20_212924/`. Baseline metrics: `FileOps.Operation=1,109,000 us`, immediate destination-size probes `16 / 506,623 us`, MOVE-cleanup probes `16 / 490,527 us`, commit proofs `0`, proof fallbacks `0`. The baseline commit field is `148f270de...` because perf capture records `HEAD`; the working tree contains the instrumentation/harness changes described above, and baseline/candidate source state is distinguished by the archived metric shape and this plan checkpoint.

**2026-07-20 proof implementation DONE:** added optional `IFileWriterCommitSizeProof` with a new UUID and a writer-local/no-I/O `GetCommittedSize` contract; the bridge queries it through COM for MOVE, accepts it only after successful `Commit` and an exact expected-size match, and otherwise increments the fallback metric and executes the unchanged final-path re-stat. Proof-capable MOVE skips only the immediate re-stat; COPY still opens/hashes its destination and MOVE still performs cleanup-time size/hash verification. Dummy, S3 multipart, and Microsoft Drive writers implement the proof from committed writer-local byte counts; S3 and Microsoft Drive debug contracts cover pre-Commit rejection and exact post-Commit size. The test decorator forwards the optional interface, the many-small-file selftest requires the candidate shape by default, the existing local-destination `GetSize` fault case asserts one legacy fallback, and Pester locks interface/provider wiring plus the absence of provider-ID branching.

**2026-07-20 final validation PASS:** finalized test-enabled Release and full-solution Debug builds completed with 0 warnings/errors. Same-binary five-sample archives are fallback `Specs/TestRuns/7d3a1247382a/FileOps/2026-07-20_215537/` and candidate `Specs/TestRuns/7d3a1247382a/FileOps/2026-07-20_220125/`; the quality gate required five samples for p95/p99 and reported p95 1,000,350 us → 490,890 us (-50.9%), 0 regressions, 1 improvement, 0 noise. Release provider contracts passed; Debug provider contracts passed Microsoft Drive 115/115, S3 150/150, and S3 multipart 26/26. Source contracts passed 148/148; Debug Fairstream passed 48/48; complete Debug FileOps passed 111/111 runnable cases with 20 expected environment-gated skips. A repository-wide Full attempt was separately classified as inconclusive/unrelated after its outer timeout and partial Commands UI/settings failures; it produced no FileOps result and is documented in the Done plan.

**2026-07-21 FG-P2-5 DONE - final finding and current state:** the redundant whole-plan destination refresh was removed only from interactive and pre-authorized-overwrite S3 directory transfers. The no-callback/no-overwrite mode retains its complete fail-closed preflight before progress or mutation; every executed object still refreshes just in time, and Retry or successful ancestor-blocker removal re-enters that refresh. Production perf capture emits aggregate destination refresh/probe count and probe-time rows. The test-enabled 16-object same-binary contract proves legacy `32` refreshes / `64` probes versus candidate `16` / `32`, while exact fail-closed, Retry, pre-authorized overwrite, stacked-ancestor, byte, and COPY-source assertions preserve behavior. Five archived Release samples show median fixed-latency probe time `1,011,056 us -> 504,449 us` (-50.11%). Full Debug, test-enabled Release, production tests-disabled Release, provider contracts (new slice 16/16), and source contracts (148/148) are green. Durable behavior is in `Specs/FileSystem/FileSystem_S3.md`; detailed implementation and evidence are in `Specs/Plans/Done/Operation_S3_InteractiveDirectoryDestinationProbeOptimization_2026-07-21.md`.

**2026-07-21 FG-P2-6 DONE - final finding and current state:** Microsoft Drive merge now threads each just-fetched destination child `Present`/`Missing` result into `MoveOrRenameItem`, removing exactly one serial metadata GET per ordinary child while retaining source/source-parent/destination-parent by-path freshness checks. Retry replaces the hint from a fresh probe; overwrite retains reversible backup/cleanup; a destination created after a missing probe collides at PATCH without source loss or DELETE. The 16-file same-binary contract proves `83/84 -> 67/68` logical/total GETs with 16 PATCHes unchanged, and five Release samples show median fixed-latency time `1,323,954 us -> 1,059,139 us` (-20.00%). Microsoft Drive passes 137/137 in Release and Debug; source contracts pass 149/149; full Debug and focused production Release are warning/error-free. `$batch`/pacing is deferred because the remaining probe is the just-in-time freshness boundary and there is no live throttling evidence to justify broader snapshot/mutation semantics. Durable behavior is in `Specs/FileSystem/FileSystem_MicrosoftDrive.md`; detailed evidence is in `Specs/Plans/Done/Operation_MicrosoftDrive_MergeDestinationMetadataReuseOptimization_2026-07-21.md` and `Specs/TestRuns/7d3a1247382a/FileOps/2026-07-21_131329_msdrive_merge_metadata/`.

**2026-07-21 FG-P2-1 / FG-P2-2 DONE - final finding and current state:** failed Microsoft Drive PATCH transports now reconcile exactly once by stable item ID and accept a commit only for the exact requested name and parent; definitive HTTP conflicts do not probe. Item-ID DELETE is idempotent: direct `404/itemNotFound` succeeds, a failed transport succeeds only when one by-ID GET proves absence, and a first attempt that commits but reports `503` followed by retry `404` succeeds with two DELETEs and zero reconciliation GETs. Ordinary successful PATCH/DELETE paths remain one mutation request with zero reconciliation GETs. Debug and test-enabled Release provider contracts pass 165/165, complete PluginContractTests passes in both flavors, source contracts pass 149/149, and production Release exposes no debug-selftest export. The broader closeout proves the repaired settings lineage/race slice 102/102, Compare 226/0, FileOperations 111/0, twelve standalone/native harnesses green, and the Monitor latency drill green; remaining Commands/Pester reds are durably classified independent UIA-desktop and pre-existing workspace/archive failures. Durable behavior is in `Specs/FileSystem/FileSystem_MicrosoftDrive.md`; detailed implementation and evidence are in `Specs/Plans/Done/Operation_MicrosoftDrive_AmbiguousMutationReconciliation_2026-07-21.md` and `Specs/TestRuns/7d3a1247382a/FileOps/2026-07-21_194100_msdrive_ambiguous_reconciliation/`.

**2026-07-22 EG-MSD-01 DONE — mutation-time overwrite identity:** a cached `Present` merge result now authorizes
only conflict handling. Before renaming an existing destination to its rollback sibling, Microsoft Drive re-resolves
the path, requires exact ID/parent/name/eTag equality, and conditions the PATCH with `If-Match`; moved, replaced,
revised, missing, inconclusive, and `412` outcomes fail as conflicts before source mutation or backup deletion.
Deterministic coverage includes `MoveItem`/`MoveItems`, pre-authorized/prompt overwrite, sequential/parallel entry
points, moved-away identity, same-path replacement, and in-place revision. Release and Debug provider contracts pass
202/202, the full Debug solution and production Release provider build warning/error-free, the production DLL exports
no debug selftest, and source contracts pass 149/149. Five same-machine Release samples preserve the no-conflict merge `83/84 -> 67/68`
GET improvement; overwrite validation adds exactly one GET per overwritten child (`83/84 -> 99/100`) at 19.60%
median fixed-latency overhead, under the 30% advisory ceiling. Durable behavior is in
`Specs/FileSystem/FileSystem_MicrosoftDrive.md`; evidence is under
`Specs/TestRuns/7d3a1247382a/FileOps/2026-07-22_1600_msdrive_overwrite_validation/`.

**2026-07-21 FG-P0-3 DONE - final finding and current state:** FileSystemCurl now has an `ENABLE_TESTS`-only bounded
loopback FTP transport that drives the public cross-connection MOVE path. The fault endpoint consumes all 4,099 upload
bytes, retains 2,049, and returns `226`; the existing independent staged `SIZE` guard returns `ERROR_PARTIAL_COPY`,
deletes the staged sibling, preserves the source, and leaves the existing final destination unchanged. The complete
control retains/promotes all bytes, cleans its rollback sibling, and deletes the source. Debug and test-enabled Release
provider contracts pass 102/102; the deliberately weakened `>` size comparison turns the same harness red at 97/102;
production Release is warning-clean and exports no selftest entry point. Durable contracts are in
`Specs/FileSystem/FileSystem_FileOperations.md` and `Specs/Testing/Testing_SelfTests.md`; implementation details and
evidence are archived in `Specs/Plans/Done/Operation_Curl_Short2xxStagedUploadProof_2026-07-21.md` and
`Specs/TestRuns/7d3a1247382a/FileOps/2026-07-21_220148_curl_short_success/`.

## Follow-up Checklist

| State | Source slice | Work item | Required proof |
|-------|--------------|-----------|----------------|
| [x] | R4-S3-01 | **DONE 2026-07-22:** make small and multipart final-writer publication conditional on absence unless overwrite was explicitly granted. | Satisfied by deterministic post-preflight destination injection, preserved-object assertions, `409`/`412` mapping, overwrite controls, exact multipart abort, Debug/Release provider contracts, and SDK-wiring source guards. |
| [x] | EG-S3-01 | **DONE 2026-07-22:** make small, multipart, and relay direct directory publication conditional on absence unless overwrite was explicitly granted. | Satisfied by the six-path COPY/MOVE race matrix, preserved concurrent destination and MOVE source, explicit condition-rejection and decision-reentry controls, aggregate metrics, exact 16-request perf shape, and focused Release/source-contract proof. |
| [ ] | FG-P1-3 | Add the bridge-MOVE skipped-child data-safety behavioral selftest: cross-FS MOVE of a directory where one child collides and is skipped while a sibling moves. | Runtime test asserts `ERROR_PARTIAL_COPY`, skipped child source bytes survive, moved sibling source is deleted, and prompt count is one; deliberately removing `skippedFileConflictCount == 0` must fail the test. |
| [x] | FG-P0-3 proof depth | **DONE 2026-07-21:** the test-only loopback FTP endpoint returns `226` after retaining a strict prefix, while the public Curl MOVE path rejects the staged-size mismatch and cleans up fail-closed. | Satisfied in Debug and test-enabled Release with 102/102 provider assertions, exact byte/command-state proof, a 97/102 RED guard mutation, and a tests-disabled production Release export check. |
| [x] | FG-P2-1 | **DONE 2026-07-21:** Microsoft Drive failed PATCH transports reconcile once by stable item ID and require exact requested name/parent; failed DELETE transports reconcile absence by ID. | Satisfied by committed/pre-commit fake Graph transport faults, exact rollback/source/backup identity assertions, one-probe bounds, and zero-probe ordinary-success/definitive-conflict contracts in Debug and test-enabled Release. |
| [x] | FG-P2-2 | **DONE 2026-07-21:** item-ID DELETE treats direct and retry `404/itemNotFound` as the already-satisfied postcondition. | Satisfied by the exact first-attempt-commits/returns-`503`, second-attempt-`404` sequence: `S_OK`, two DELETEs, zero reconciliation GETs; drained merge-source recursive delete remains forbidden. |
| [x] | EG-MSD-01 | **DONE 2026-07-22:** existing-destination overwrite revalidates exact path identity/revision and makes the backup PATCH conditional on the observed eTag. | Satisfied by moved/replaced/revised race tests across both public move APIs and overwrite authorization paths, simultaneous-host proof, exact source/destination preservation, aggregate metrics, and five-sample Release evidence. |
| [x] | FG-P2-4 | **DONE (2026-07-02 folder review, verified in code):** malformed `FileSystemOptions::sizeBytes` guards implemented across FileSystem, S3, Dummy, Curl, and MSDrive entry points, locked by a Pester source-contract test, and made normative in `Specs/Plugins/Plugins_VirtualFileSystem.md:1333` (commit `385e5ba5a`). | Provider contract or focused selftests assert `E_INVALIDARG` for malformed options on every accepting path — satisfied by the Pester contract coverage above. |
| [x] | FG-P2-5 | **DONE 2026-07-21:** S3 interactive and pre-authorized-overwrite directory transfers skip the redundant whole-plan destination refresh while retaining just-in-time freshness and the fail-closed no-callback preflight. | Satisfied by exact same-binary Release counts (`32 -> 16` refreshes, `64 -> 32` probes), 50.11% median fixed-latency reduction, and focused fail-closed/Retry/ancestor/byte-preservation provider contracts. |
| [x] | FG-P2-6 | **DONE 2026-07-21:** Microsoft Drive merge threads the explicit current-child destination result into the nested move; `$batch`/pacing is deferred pending live throttle evidence and a separate mixed-result proof. | Satisfied by exact same-binary Release counts (`83/84 -> 67/68` GETs, 16 PATCHes unchanged), 20.00% median fixed-latency improvement, and overwrite/Retry/nested/late-destination identity contracts. |
| [x] | FG-P2-7 | **Resolved 2026-07-13 by Causeway CW-9:** bridge MOVE uses one copy-time FNV-1a proof and re-hashes only the destination; native local cross-volume MOVE uses successful-copy plus stable-snapshot proof and performs zero post-copy content rereads. | Durable contract and final evidence are linked from the Causeway Done plan; Clearwater CW-P1/CW-T-P1 is updated as completed. |
| [ ] | FG-P2-9 | Unify serial and concurrent bridge-MOVE conflict classification through one helper. | Serial and concurrent paths classify nested unsupported reparse and source-delete `ACCESS_DENIED` identically, including `failedDuringMoveDelete` and reparse hints. |

## Added by post-closeout review - 2026-06-19 (commit `68306b9ec`)

| State | Source slice | Work item | Required proof |
|-------|--------------|-----------|----------------|
| [x] | FG-P0-3-FALLBACK-SIZE-ZERO | **DONE 2026-08-04:** staged publication now fails closed unless the exact remote size is proved. Unknown zero is distinct from a proved empty object, and failed/short probes remove the staged sibling while preserving the source and existing destination. | Bounded loopback FTP coverage exercises exact, short, and unverifiable sizes through the public COPY/MOVE path; provider and source-contract suites are green. |
| [x] | FG-A1 | **DONE 2026-07-20:** the destination-side `GetSize` hook at the Destination-role decorator boundary is now armed by a serial Fairstream step that forces post-promote `destinationReader->GetSize` failure. | `ERROR_PARTIAL_COPY`, promoted destination bytes, and source preservation are asserted; registration and decorator wiring are source-contract locked. Focused and full-family evidence is recorded above. |
| [ ] | FG-P0-2-RESTAT-FALSEPOS | Make the post-promote destination re-stat resilient to eventual consistency: distinguish a transient "could not verify" (short retry/backoff, do not strand a duplicate) from a confirmed wrong-size (true corruption → consider deleting the partial destination before returning). | Fake eventually-consistent destination where the first by-path GET misses then succeeds: MOVE completes without spurious `ERROR_PARTIAL_COPY` and without leaving a source+destination duplicate. |
| [x] | FG-P0-2-PERF-RESTAT / FG-P0-3-EXTRA-LIST-ROUNDTRIP | **DONE:** Curl uses its single-object `CurlProbeRemoteFileSize` rather than a per-file parent `LIST` (commit `385e5ba5a`). The bridge now uses optional exact post-Commit writer proof for MOVE, with capability-based QueryInterface, exact-size validation, and fail-closed legacy fallback. COPY hashing and MOVE cleanup verification remain unchanged. Final implementation/evidence: `Specs/Plans/Done/Operation_FileOperations_CommitSizeProofRestatOptimization_2026-07-20.md`. | Satisfied: same-binary five-sample proof shows 16→0 immediate probes and exactly 16 cleanup probes; p95 improves 50.9%; legacy fallback remains 16+16; complete FileOps and provider/source contracts pass. |
| [ ] | FG-MSDRIVE-2 | Add an MSDrive selftest where the malformed empty-name child lives under a **nested** existing-on-both-sides subfolder. | Asserts the nested source folder AND the top source folder both survive, valid siblings move, op returns `ERROR_PARTIAL_COPY`. Locks the recursion-propagation contract (currently only the top-level child is covered). |
| [x] | FG-P0-2-TEST-SERIAL-ONLY | **DONE 2026-07-20:** added the eight-child, `copyMoveMaxConcurrency: 4` source+destination `GetSize` failure variant alongside the serial FG-A1 case. | Exactly one source and one destination injection occur, 7 promoted destinations retain expected bytes, all 8 sources remain because partial copy suppresses tree cleanup, and the aggregate operation returns `ERROR_PARTIAL_COPY`. |
| [x] | FG-P0-1-A | **SUPERSEDED (2026-07-02 folder review, verified in code):** commit `385e5ba5a` reworked S3 to per-object conflict resolution, so the whole-transfer fail-closed abort this row wanted to preserve no longer exists as such. The self-delete loss vector the abort protected against is now guarded directly: descendants whose ancestor key collides with a planned destination are declined per object via `plannedDestinationKeys` (`Plugins/FileSystemS3/FileSystemS3.Directory.cpp:1486-1491`), selftested by `RunDebugPlannedDestinationAncestorCollisionSelfTest` and Pester-locked. | Superseded — the guard, selftest, and Pester lock above replace the distinct-HRESULT requirement. |
| [x] | FG-A3 | **DONE (2026-07-02 folder review, verified in code):** spec and code are in lockstep — commit `385e5ba5a` implemented the sizeBytes guard across FileSystem/S3/Dummy/Curl/MSDrive entry points (see FG-P2-4 above) and the normative MUST at `Specs/Plugins/Plugins_VirtualFileSystem.md:1333` now has implementing guards behind it, locked by Pester source-contract coverage. | Spec and code in lockstep; no normative MUST without an implementing guard — satisfied. |
| [ ] | FG-HARNESS-COVERAGE | Add a build-time/Pester guard asserting every non-Setup/Cleanup step in `kFileOpsPhaseOrder` is a member of exactly one `kFileOpsFamily*` array. This is the systemic fix for the FG-TEST-1 silent-skip class (a per-case string match is not enough). **2026-07-02 folder review:** this row is now the single owner of the family-membership guard — Keystone KS-T3 was routed here. | Guard fails if any registered fileops case is not family-reachable; validated against a deliberately-unregistered case. |

## Closeout Rule

Each item that touches correctness must include deterministic selftest or fake-provider coverage and update the authoritative spec that owns the durable behavior. Each item that claims a performance improvement must follow `Specs/Testing/Testing_PerformanceValidation.md` with archived before/after evidence under `Specs/TestRuns/`.
