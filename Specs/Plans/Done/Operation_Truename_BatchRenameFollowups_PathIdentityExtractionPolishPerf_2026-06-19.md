# Operation Truename Follow-ups — Path Identity Completion, Execution Extraction, Polish, and Perf

**Status:** Done - moved from Truename follow-up queue after focused verification
**Date:** 2026-06-19
**Source plan:** `Specs/Plans/Done/Operation_Truename_BatchRenameRemediation_DataSafetyVerificationFoldConsistencyAndInteractivePerf_2026-06-16.md`

## Release Decision

This plan owns the non-blocking Truename follow-ups that remain after the Batch Rename P0/P1/P2 remediation and bounded T-S3-2 hardening landed.

The accepted closeout scope for the completed Truename remediation is:

- Batch Rename uses a provider `pathIdentity` profile for identity-sensitive preview, execution, target refresh, cache notification, undo finalization, provider-selection matching, and outcome pairing.
- Destructive recovery paths are covered: swap/cycle temp rollback, close-while-executing teardown, under-reporting provider outcomes, and stale generation payloads.
- View-only preview refreshes no longer rebuild the plan, post-execution target refresh is linear, metadata and Date/Time formatting contracts are shared, and P2 coverage gaps are closed.
- Focused and combined same-machine Commands evidence is archived in `Specs/TestRuns/4cb089111a23/Commands/`.

The items below remain valuable, but they are not blockers for moving the completed remediation plan to `Specs/Plans/Done/` once the final verification gate is green.

## Follow-up Checklist

| State | Source slice | Work item | Required proof |
|-------|--------------|-----------|----------------|
| [x] | T-S0-0b | Complete path-identity machinery and file-ops rollout: concrete `componentComparison` and `caseOnlyRename:notApplicable` declarations for non-rename providers, accepted-separator normalization, helper-owned `ComponentKey`/`PathKey` builders with key-hit verification, same-context Copy/Move/Delete `pathIdentity` enforcement, and matching developer-doc/spec updates. | Completed 2026-06-19: `Common/FileSystemPathIdentity` now parses the full v1 contract, normalizes accepted separators for path equality, and exposes helper-owned component/path keys that decline unsafe collisions; 7z, Google Drive, IMAP, and S3 Table now emit concrete `pathIdentity`; same-context Copy/Move/Delete rejects missing/invalid/unstable profiles before task creation. Proof: Debug build green; `cmd_pane_batchRename_path_identity_parser` and `FileOps_ProviderCapabilityMatrix` pass; provider docs/specs no longer contain staged-rollout language. |
| [x] | T-S3-1 | Extract `BatchRenameExecutionEngine.{h,cpp}` as a window-free execution engine while preserving `noexcept`, MTA setup, provider-owner-before-`CoUninitialize` lifetime, posted completion semantics, and `cancelRequested std::atomic_bool&` wiring. | Completed 2026-06-19: `BatchRenameExecutionEngine.{h,cpp}` owns dependency layers, temp-hop cycle breaking, per-item outcome bookkeeping, undo entries, directory-move path rewriting, and execution metrics; `BatchRenameWindow.cpp` now keeps only the MTA COM/progress/completion-posting worker envelope. Proof: Debug build green; `cmd_pane_batchRename_execution_engine_direct`, `cmd_pane_batchRename_window_executes_swap_rename`, `cmd_pane_batchRename_stale_generation_payloads_are_ignored`, and `cmd_pane_batchRename_close_while_execution_inflight_is_bounded` pass. |
| [x] | T-S3-3 | Low-severity correctness/UX polish: bijection-checked manual sort mapping, Path-column sorting by displayed root-relative path, direction-aware stable tiebreaking, `CONIN$`/`CONOUT$` reserved-device validation decision, supplementary-plane whole-word semantics, nonnumeric counter/index format decision, empty selected-text helper UX, helper-insertion unification, dead `ExecutedDirectoryMove::depth` cleanup, footer wording/count consistency, settings empty-guard asymmetry, and temp-hop progress clamping. | Completed 2026-06-19: manual `Sort like preview` now bijection-checks target indexes, `Path` sorting uses displayed root-relative text with stable equal-key order, reserved-name validation covers `CONIN$`/`CONOUT$`, invalid counter/index formats emit `macro_invalid_format`, whole-word literal matching rejects surrogate-pair splits, helper insertion uses shared insertion math including empty selected-text skeletons, footer text includes unchanged counts, empty MRU settings are guarded, dead execution depth state is removed, and temp-hop progress is clamped to changed-row totals. Proof: Debug build green; archived Commands run `Specs/TestRuns/4cb089111a23/Commands/2026-06-19_215050/` passed `cmd_pane_batchRename_path_sort_uses_displayed_path_and_stable_ties`, `cmd_pane_batchRename_helper_menus_expose_canonical_insertions`, `cmd_pane_batchRename_window_helper_buttons_insert_into_rule_fields`, `cmd_pane_batchRename_engine_reserved_device_names_and_counter_width`, `cmd_pane_batchRename_engine_warnings_and_remaining_transforms`, `cmd_pane_batchRename_window_rules_recompute_preview`, `cmd_pane_batchRename_execution_engine_direct`, and `cmd_pane_batchRename_manual_sort_like_preview_with_hide_unchanged`. Durable behavior is reflected in `Specs/UI/UI_BatchRenameWindow.md`, `docs/BatchRename.md`, and `docs/DeveloperGuide.md`. |
| [x] | T-S3-4 | Performance cleanup: allocation-free leaf splitter, parse scope mask once per collection run, cache provider-identity parent/op keys only where the helper owns and verifies key hits, and poll cancel within large provider directories after malformed-buffer bounds checks. | Completed 2026-06-19: `{stem}`/`{ext}` use allocation-light leaf splitting pinned against `std::filesystem`, folder-scope collection builds one parsed scope matcher per run, duplicate validation/target refresh/provider-selection matching use helper-owned keys with equality verification, and provider directory cancellation is polled after safe entry bounds checks. During verification the 10,000-row preview test exposed the old duplicate-detection O(n^2) validation path: archived run `Specs/TestRuns/4cb089111a23/Commands/2026-06-19_214440/` failed with `batchrename.validation.us=134238236` and `batchrename.preview.build_plan_us=134677795`. The fixed candidate archived at `Specs/TestRuns/4cb089111a23/Commands/2026-06-19_214739/` passed with `batchrename.validation.us=85016` and `batchrename.preview.build_plan_us=533941`. Additional archived Commands run `Specs/TestRuns/4cb089111a23/Commands/2026-06-19_214811/` passed `cmd_pane_batchRename_engine_leaf_splitter_matches_filesystem`, `cmd_pane_batchRename_post_execution_target_refresh_is_linear`, `cmd_pane_batchRename_target_collection_respects_provider_cancellation`, and `cmd_pane_batchRename_duplicate_source_target_refresh_consumes_distinct_rows`. |

## Closeout Rule

Each correctness item must include deterministic selftest or fake-provider coverage and update the authoritative spec that owns the durable behavior. Each performance item must include scenario definition, instrumentation review, and archived before/after evidence under `Specs/TestRuns/`.
