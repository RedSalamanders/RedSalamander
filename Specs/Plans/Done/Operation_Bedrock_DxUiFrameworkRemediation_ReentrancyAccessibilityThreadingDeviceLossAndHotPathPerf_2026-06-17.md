# Operation Bedrock — DxUi Framework Remediation: Reentrancy UAF, Accessibility Threading, Device-Loss Recovery & Hot-Path Performance

**Status:** Done
**Date:** 2026-06-17
**Author:** Independent adversarial multi-agent review of the entire `Common/DxUi` framework at `master` `a68274ade`. Workflow run `wf_ddf495f4-3e9` (115 agents, run in 6 throttled waves): 17 review lenses — per-component deep dives on every translation unit (core object model, Controls ×2 safety+arch, Accessibility ×2 correctness+arch, Menu, Grid, WindowHost, TextInput, TSF/TextStoreACP, ComboBox, Tree/Typeahead, Theme/Typography, PointerInput/Scrollbar/FocusRestore) **plus three whole-framework cross-cutting lenses** (D2D/DXGI device-loss resource lifetime; reentrancy / delete-on-stack / mutate-while-iterating; WIL-RAII & COM refcount compliance) → per-finding adversarial refutation pass that re-opened the real source at each cited line and tried to *disprove* it. **56 findings survived verification (1 critical, 6 high, 14 medium, 35 low); 41 refuted** as guarded / by-design / impossible.

**Scope of this pass (~45,634 LOC under `Common/DxUi`):**
- `DxUi.h` / `DxUi.Internal.h` / `DxUi.cpp` — core `Element`/`Control`/`Panel` retained tree, frame runtime, invalidation.
- `DxUi.WindowHost.cpp` / `DxUi.Win32Hooks.h` — HWND host, Win32 message loop, swap-chain/device + device-loss, DPI, focus, animation dispatcher.
- `DxUi.Accessibility.cpp` — UI Automation provider tree (the largest correctness surface in this plan).
- `DxUi.Controls.cpp` — Button/Toggle/RadioButton/ScrollPanel/MenuBar/TabControl etc.
- `DxUi.Grid.cpp` — virtualized grid/list. `DxUi.Tree.cpp` / `DxUi.Typeahead.cpp` — tree + type-to-select.
- `DxUi.TextInput.cpp` / `DxUi.SingleLineTextEditing.cpp` — editing engine. `DxUi.NativeTextInput.cpp` / `DxUi.TextStoreACP.cpp` — TSF `ITextStoreACP`.
- `DxUi.ComboBox.cpp`, `DxUi.Menu.cpp` / `DxUiNativeMenuInterop.h`, `DxUi.Theme.cpp` / `DxUi.Typography.h`, `DxUi.Scrollbar.cpp`, `DxUi.PointerInput.{cpp,h}`, `DxUi.FocusRestore.h`.
- Consumers touched by Theme findings: `RedSalamander/DxUiThemePalette.h`, `RedSalamander/SplashScreen.cpp`, `RedConfigure/RedConfigureSplashScreen.cpp`.
- Tests: `Tests/DxUiTests/*` (Accessibility, Animation, ComboBox, Controls, Grid, Menu, MultilineText, NativeTextInput, Rendering, TextField, Theme, Tooltip, Tree, WindowHost). Registered in the `Full` suite via `Tools/Run-AllTests.ps1`.

> **Anchors are relative to `a68274ade`. Re-grep every `~line` before editing — line numbers drift.**

## 2026-06-21 owner decisions and current re-review addendum

This addendum records the decisions from the 2026-06-21 plan review and makes them part of the implementation contract:

- **ComboBox outside-click behavior is settled:** follow standard Windows combobox behavior. One outside click while the dropdown is open must dismiss the dropdown and continue through normal hit-testing so the underlying target receives the click/focus/activation when appropriate. Do not preserve the current "consume first outside click" test contract; update that test to the standard behavior.
- **UIA threading architecture is settled:** use **UI-thread-built immutable accessibility snapshots**. Provider/RPC threads must not walk the live retained `Control`/`Panel` tree or caller-owned Tree/Grid models. The accessibility mutex is not the correctness boundary for live tree access; it may only protect provider bookkeeping/snapshot publication.
- **UIA live-host access invariant:** `WindowHostAccessibilityTarget::ResolveHost()` may return a live `WindowHost*` only on the owning window thread. Provider/RPC read paths must consume the published snapshot when they run off-thread; UIA mutation paths must marshal to the window thread before resolving or mutating controls.
- **Credential prompt secure teardown invariant:** password text storage is owned and wiped by `TextField` itself, including `_text`, undo/redo snapshots, and cached layout text during destruction. `TextField` must not expose a public/live `SecureClear()` teardown API that callers can invoke through retained raw child pointers. Native text-input session teardown must securely wipe its cached `NativeTextInputState::text` copy before invalidating the cache; `WindowHost::Detach()` / `ResetRootInteractionState()` must deactivate text input and clear native text caches before `_root.reset()` can destroy retained `TextField` instances; and TSF text-store teardown must disconnect the store's raw host/control pointers before `ITfDocumentMgr::Pop`, `ITfThreadMgr::Deactivate`, or COM reference release can synchronously call back into it. Prompt teardown must not walk or wipe retained controls through raw child pointers; it destroys/detaches the `WindowHost`, lets owned controls run their destructors, nulls raw observers after detach, and only wipes prompt-owned `_acceptedSecret` directly.
- **Selftest UIA worker lifetime invariant:** UIA automation helper threads that query live HWND/provider surfaces must be bounded and joined before the owning dialog/window can tear down. Timeout paths may request stop and keep pumping messages, but must not detach or return while a worker can still call UIA against that window.
- **Current-tree re-review:** the plan's P0/P1 findings still apply on current `HEAD` `a72512919ba5`. Additional current anchors to preserve while implementing: `WindowHost` pointer-up diagnostics also keep using a callback target after `OnMouseUp`; `ComboBox` tests currently assert the old outside-click consumption behavior; no Bedrock-specific archived perf evidence was found under `Specs/TestRuns/`.

---

## Why this plan exists

**Verdict: DxUi is NOT currently rock-solid — but it is very fixable, and almost every P0/P1 defect already has a correct sibling in the tree.** The architecture is sound and the team clearly understands the hazards: `Control::GetLifetimeToken()`/`std::weak_ptr<int> _lifetimeToken` is used correctly in `Button::InvokeDropDown`; UIA *mutations* are correctly marshalled to the UI thread via `SendMessageTimeout`; `DebugSimulateDeviceLoss` calls `Invalidate()` after discarding resources; `wil::BeginPaint` already exists in Helpers.h; `GetOrCreateMultilineLayout` and `WindowHost::GetTextFormat` already show the layout/format caching pattern. **The defects are that these correct patterns are applied inconsistently — and the sites that miss them are the load-bearing ones.** Three classes block sign-off:

1. **Reentrancy use-after-free on the hottest gestures.** The primary input-dispatch path dereferences a raw `Control*` target after a user callback that routinely destroys the control tree (double-click folder → navigate; tree-item invoke; context-menu action). **This is a reproducible heap UAF on everyday file-manager use, in production, today.** `UpdateHover` and several `ScrollPanel`/`TabControl` sites share the same unguarded shape. The fix pattern already exists in-tree; it is simply not applied here.

2. **The accessibility threading model is broken.** UIA read methods run on RPC worker threads and walk the live, UI-thread-affine `Control`/`Panel`/model tree with **no** synchronization; the one mutex that exists is taken only by providers, never by the UI thread during mutation/teardown — a *false* safety guarantee. `GetBoundingRectangles`/`Compare` don't even take that lock, and `Detach` destroys the tree after releasing it (shutdown UAF). **Any Narrator / Inspect / UI-automation user can crash the app during normal navigation.** Reachable, not theoretical.

3. **Device-loss recovery is incomplete.** On a real GPU TDR / driver reset / RDP transition the production render path discards resources and returns **without `Invalidate()`**, so a static window stays blank/stale until an unrelated event repaints it. The debug-only simulation path proves the fix is a single missing call.

Beyond those: a consistent performance anti-pattern (O(n²) DirectWrite layout creation per frame / per mouse-move in MenuBar/TabControl; uncached per-cell/per-row `GetCellData` + text layouts in Grid/Tree/TextInput/ComboBox; redundant UIA control-tree re-resolution), plus a cluster of bounded correctness bugs (grid bottom-row drop, tree typeahead jam, surrogate-pair/RTL/masked-caret text handling, dark-theme palette gaps). None of the perf/correctness items is individually catastrophic, but collectively they keep DxUi below the project's strict-correctness bar.

**Fix the class, not the instance.** Seven systemic patterns drive the 56 findings — fixing each once kills a cluster (see "Cross-cutting themes" below). In particular:
- The **reentrancy guard** (Theme 1) is *one* convention; apply it uniformly instead of patching each site.
- The **UIA thread-affinity** fix (Theme 2) is *one* model decision enforced globally: UI-thread-built immutable snapshots, never provider-thread live-tree reads.
- The **single-line DWrite layout cache** (Theme 3) is *one* shared facility that subsumes the MenuBar/TabControl/Grid/Tree/ComboBox/TextInput churn.

---

## Implementation Tracking Checklist (update first, before editing code in a slice)

Use `[ ]` not started, `[~]` in progress, `[x]` complete, `[blocked]` needs a product decision.

| State | Slice | Sev | Component | Required proof before `[x]` |
|-------|-------|-----|-----------|-----------------------------|
| [x] | B-S0-1 | CRIT | WindowHost / Controls | Reentrant callbacks do not leave stale dispatch/focus/capture/hover targets: pointer down/up, hover, key down/up, char input, focus transitions, `ScrollPanel`, and `TabControl::CloseTab` now have lifetime/interactive revalidation plus deterministic selftests. Credential prompt modal-loop `WM_QUIT` exits now destroy the HWND before reposting quit; prompt teardown no longer walks retained controls through raw child pointers, and `TextField` now owns secure destruction of its text/undo/redo/layout buffers; native text-input deactivation securely wipes its cached text copy; TSF deactivation disconnects `DxUiTextStoreACP` raw host/control pointers before TSF `Pop`/`Deactivate` callbacks or COM release can re-enter the store; `WindowHost::~WindowHost()` falls through `Detach()` so native text input/TSF state is released before retained `TextField` controls are destroyed even if an owner misses `WM_NCDESTROY`; command selftest UIA pump timeout paths now request stop and join instead of detaching workers that can outlive credential prompt teardown. AppVerifier Basics covers the credential-prompt theme-cycle crash route (`Specs/TestRuns/a72512919/AppVerifier/2026-06-27_173006/`), full ASan `DxUiTests` is green, and ASan `RedSalamander` credential-prompt command routes are green (`Specs/TestRuns/a72512919/DxUi/2026-06-27_184400_BS01_ASan/`, `Specs/TestRuns/a72512919/Commands/2026-06-27_184400_BS01_ASan/`). |
| [x] | B-S0-2 | HIGH | Accessibility | Every provider *read* uses a UI-thread-built immutable snapshot or a bounded UI-thread query; no provider/RPC thread derefs live `Control`/`Panel` objects or Tree/Grid client models. Snapshot publication/teardown is lifetime-safe, and concurrent Inspect/Narrator-style traversal during navigation/list-refresh/root teardown shows no AV under AppVerifier Basics. Snapshot spine/publication is in place; `GetFocus`, `ElementProviderFromPoint`, `Navigate`, `get_BoundingRectangle`, ordinary Control property/pattern/value/toggle/range reads, Grid/Table item relationship reads, GridHeader property reads, GridRow property reads, GridCell property/pattern/value/toggle/range reads, Tree/Grid selection-provider reads, TreeItem property/expand-collapse reads, TextPattern document/visible/selection/supported-selection/range-text reads, selected TextPattern range bounding rectangles, arbitrary/moved TextPattern range bounding rectangles, visual-line TextPattern range movement, TextEdit active-composition/conversion-target reads, and TextField password-reveal pseudo-button invoke/property reads no longer deref retained controls on provider threads. Native text-input accessibility event fan-out now publishes one fresh snapshot per changed native state before raising UIA Text/TextEdit events, so TextEdit range providers observe current IME preview/conversion endpoints without provider-thread live reads. `WindowHostAccessibilityTarget::ResolveHost()` now refuses live host access from non-window threads; arbitrary/moved `GetBoundingRectangles` and visual-line TextRange `Move` / `MoveEndpointByUnit` copy their range under the provider lock, release the lock, and use bounded `SendMessageTimeoutW` dispatch for UI-thread DirectWrite geometry. Masked `TextField` controls report protected/password state but do not expose TextPattern/TextEditPattern over password content. Fresh focused evidence: `Specs/TestRuns/a72512919/DxUi/2026-06-27_171926/` (`Accessibility`, `TextField`, `ComboBox`, app build, RED/GREEN cross-thread endpoint/span visual-line movement dispatch, and 230-row `perf_metrics.jsonl` with one `move-endpoint-line-dispatch` row and one `move-line-dispatch` row) plus `Specs/TestRuns/a72512919/AppVerifier/2026-06-27_173006/` (AppVerifier Basics enabled for `DxUiTests.exe`, full Accessibility suite including `TestAccessibilityProviderTraversalSurvivesConcurrentRootReplacement`, then verifier settings removed). |
| [x] | B-S0-3 | HIGH | WindowHost | After the device-loss discard branch in BOTH `Render` overloads, a full repaint is scheduled (`Invalidate()`), the device is recreated, and the first post-recreate present is forced full. Proof: `TestWindowHostDeviceLossSchedulesRepaintAndForcesFullPresent` covers simulated discard/recreate and structurally guards both render overloads. |
| [x] | B-S1-1 | HIGH | Grid | With a viewport height that is a non-integer multiple of row height, the last partially-visible row is painted AND hit-testable (click/hover/context-menu/select); `_hoveredRow` reset on data change. Proof: `TestGridPartiallyVisibleBottomRowIsPaintedAndHitTestable` plus updated visible-work metrics cover partial bottom rows. |
| [x] | B-S1-2 | HIGH | TextInput | Masked caret/selection/IME correct for emoji/CJK (dot count tracks code units); caret+selection rects correct under RTL/BiDi; undo never leaves a lone surrogate. Proof: exact-mask code-unit count, surrogate-pair undo, and mixed-BiDi selection-paint-vs-DirectWrite-range selftests are green; caret/range rects stay on DWrite hit-test metrics. |
| [x] | B-S1-3 | HIGH | TextInput | Ctrl+Z undoes a typing/backspace/delete run rather than one code point at a time; no full `_text` heap copy per keystroke; single-line layout and masked dot-count work are bounded and correctly invalidated. Undo coalescing is complete, coalesced direct-edit runs structurally guard that full history snapshots are captured only when a new undo run starts, selected/unselected single-line paint each create one DirectWrite layout for metrics/draw/selection work, focused single-line paint adjusts horizontal caret visibility once per frame, TextField and editable ComboBox retain/reuse the shared single-line DirectWrite layout cache, exact masked length is O(1), concealed masked length remains bucket-cached, and same-machine before/after perf evidence is archived. |
| [x] | B-S1-4 | HIGH | Grid | Ordered groups + content rect computed once per frame; per-cell `GridCellData` not reconstructed per frame and not re-scanned every animation tick; geometry matches freshly-computed visible values after invalidation triggers. Paint now reuses one collected group vector/body rect for visible rows, visible-cell paint/metrics/animation scans reuse reset `GridCellData` scratch storage instead of constructing it per visible cell, visible-work metrics expose and guard bounded cell-data read count, paint emits archived cell-data callback metrics, and animation ticks reuse Paint-discovered animated-cell state, with only a lazy one-time scan when Tick runs before Paint has seeded the cache. Allocation proof shows large-grid paint grows the reused cell-data text storage at most once per repaint, so broader cell-data memoization is not justified by current bounded-work evidence. |
| [x] | B-S1-5 | HIGH | TSF / Accessibility | `GetTextExt` computes the union rect without O(n) layouts; Tree UIA hit-test does not allocate a full `TreeItemData` per row; control/cell/item resolved once per public UIA method. TSF multiline `GetTextExt` now uses control-provided range rects before caret-walk fallback; UIA point hits, bounds, fragment navigation, ordinary Control property/pattern/value/toggle/range reads, Grid/Table item relationship reads, GridHeader property reads, GridRow property reads, GridCell property/pattern/value/toggle/range reads, Tree/Grid selection-provider reads, TreeItem property/expand-collapse reads, TextPattern document/visible/selection/supported-selection/range-text reads, selected TextPattern range bounding rectangles, arbitrary/moved TextPattern range bounding rectangles, visual-line TextPattern range movement, TextEdit active-composition/conversion-target range reads, TextPattern `RangeFromPoint`, TextRange `GetEnclosingElement`, and TextField password-reveal pseudo-button invoke/property reads now come from bounded snapshot records or bounded UI-thread queries instead of per-query live Tree/Grid/model/control walks on provider threads; native IME updates refresh snapshot publication before UIA TextEdit events, and the unused text-range helper that resolved a live control path for text overrides is removed. Live host resolution is now UI-thread-only, which blocks provider/RPC threads from using retained controls during prompt teardown/theme-cycle races; provider-thread visual-line `Move` and `MoveEndpointByUnit` now dispatch to the owning window thread without holding the provider lock, preserving wrapped-line DirectWrite fidelity instead of falling back to logical lines; masked TextFields suppress TextPattern/TextEditPattern so protected content cannot be reached through live text-range reads; Tree snapshot hit capture starts at the first visible row and uses geometry-only row rects. Final residual cleanup: TextPattern `RangeFromPoint` now validates text support/path from the immutable snapshot, dispatches exact point hit-testing to the owning window thread, and creates the returned range from the snapshot path; TextRange `GetEnclosingElement` validates the enclosing element from the captured snapshot instead of resolving a live control. Focused proof: `TestAccessibilityTextRangeFromPointDispatchesToWindowThread` plus structural snapshot/no-live-resolve guards are green, and `Specs/TestRuns/a72512919/DxUi/2026-06-27_174651/` archives the Accessibility run with 696 perf rows, including 4 `range-from-point` rows. |
| [x] | B-S1-6 | HIGH | Menu / ComboBox | Closing the owner while a context menu is open breaks the modal loop (no stuck/no-capture state); open ComboBox follows standard Windows behavior: outside click dismisses and reaches the underlying target; class-registered flag set only after `RegisterClassExW` succeeds; HRGN freed on `SetWindowRgn` failure. Proof: Menu/ComboBox selftests cover missing-root dismissal, standard outside-click fall-through, popup selection, field toggling, class-registration state, and HRGN ownership. |
| [x] | B-S1-7 | HIGH | Tree | Typeahead single-char fallback (one mistype doesn't poison the buffer); UIA-driven expand schedules an animation tick; keyboard context-menu scrolls selection into view first. Proof: `TestTreeTypeaheadFallsBackToSingleCharacterAfterPrefixMiss`, `TestTreeKeyboardContextMenuBringsOffscreenSelectionIntoViewBeforeAnchoring`, and the TreeItem UIA expand-collapse animation assertion are green. |
| [x] | B-S1-8 | HIGH | Theme | `MakeDefaultThemePalette(dark=true)` sets dark focus/pressed/knob/accent-variant/smokeOverlay; `accentHover/Pressed` recomputed whenever `accent` is reassigned (splash screens + rainbow header tints correct). Proof: Theme selftests cover dark default interaction-token overrides and app/rainbow palette accent-variant refresh. |
| [x] | B-S2-1 | MED | Controls / ComboBox / Tree / Typography | Shared single-line measurement/layout cache; MenuBar/TabControl/ComboBox/Tree-badge/Typography measurement no longer O(n²)/per-frame; rect-correctness selftests pass after layout-affecting invalidation triggers. TextField and editable ComboBox retain/reuse the shared single-line layout cache for paint/caret work, editable ComboBox paint ensures caret visibility once and reuses that caret offset, TabControl title widths/header geometry, MenuBar item widths/rects, Tree badge widths/hovered tooltip overflow, and Typography family/text-format measurement cache per control/factory as appropriate, invalidate on layout-affecting changes, and emit gated/archived cache-miss counters. Mica/Acrylic popup backdrop capture uses one shared bounded helper; ComboBox passes the current popup rect and Menu passes the visible surface rect, so neither component owns duplicate GDI capture code or captures larger shadow/window bounds. Fresh runtime rect guards cover MenuBar item replacement/bounds/RTL and TabControl header scroll/title/closability/bounds/RTL invalidation. Same-machine perf evidence is archived with baseline-only DirectWrite layout-create instrumentation. |
| [x] | B-S2-2 | MED | Accessibility / WindowHost | Single `QueryPattern` source of truth for QueryInterface+GetPatternProvider; `BuildRuntimeId`/`MakeProvider<T>` helpers; `ScopedPaint`→`wil::BeginPaint`; root pane exposes exactly one accessible content element. Paint RAII, `MakeProvider<T>` provider-factory consolidation, runtime-id builder consolidation, Accessibility pattern dispatch consolidation, collapsed direct-root provider behavior, and UIAutomation-client content-tree proof are complete. |
| [x] | B-S3-1 | LOW | framework-wide | Batch of behavior-preserving simplifications (Toggle/RadioButton dedup, const/non-const overload collapse, `ComputeScrollbarPageStepDip`, DPI-bitmap recreate, hidden-host timer release, pointer input source-plumbing removal, dead-code removal). Toggle/RadioButton activation now uses shared helpers, Tree expansion moves captured `beforeItems` into the animation state instead of deep-copying it, unused Tree/typeahead probes are removed, scrollbar visual strength resolution reuses pre-resolved animation targets, const hit-test overloads delegate to the mutable traversal instead of duplicating reverse child walks, Menu reserves the submenu chevron lane uniformly once any row has a submenu, Grid/Tree/ScrollPanel/ComboBox scrollbar track paging all route through `ComputeScrollbarPageStepDip`, DPI changes now detach stale target bitmaps even when the swap-chain pixel size is unchanged, hidden HWND animation ticks now drop the dispatcher subscription until the window is shown again, and the single-value pointer-source enum/event field/API parameters plus the unused `MSG` builder/private message-time adapter are removed entirely. All named simplifications are complete and independently guarded; full `DxUiTests` stays green. Closeout proof: `Specs/TestRuns/a72512919/DxUi/2026-06-27_175247/` archives full `DxUiTests.exe` exit 0 with 8,838 perf rows. |

---

## Guiding Principle

> **Never deref a control across a user callback that could free it; never touch the retained tree or client models off the UI thread; never drop the recovery frame after device loss; geometry that matches the glyphs DWrite actually draws; one balanced Release per AddRef; WIL RAII for every handle.** Prove the dangerous / dead / wrong path on the user's actual route, not just the happy path.

## Validation Contract (mandatory)

- **Build gate:** `.\build.ps1 -ProjectName DxUi` then `.\build.ps1` (0 warnings / 0 errors, x64 Debug; spot-check ARM64 for the SIMD/DPI-sensitive slices).
- **Functional gate:** `.\Tools\Run-AllTests.ps1 -Suite Full -SkipBuild` (long; serialized by a machine-wide mutex — exit 3 means another selftest run holds it, retry). Register every new case in `Tests/DxUiTests/*` so it actually runs in `Full`.
- **For each P0/P1 slice the proof must make the dangerous/dead/wrong condition observable and FAIL (RED) on the current tree before the fix, then pass after.** Where a UAF/race is impractical to force deterministically (B-S0-1 teardown race, B-S0-2 UIA threading), the proof is (a) a line-by-line lifetime/snapshot trace showing "no raw `Control*`/`self->_member` deref after the callback / no provider-thread live-tree or model deref" **plus** (b) a structural assertion in DxUiTests **plus** (c) a run under **Application Verifier** (heap + lock checks) / ASAN driving the repro gesture.
- Archive perf before/after evidence (frame time, allocation counts) under `Specs/TestRuns/<machine>/DxUi/<timestamp>/`.

---

## Cross-cutting themes (fix once)

1. **Lifetime-token guard around reentrant user callbacks.** Pattern exists (`Button::InvokeDropDown`). Missing at WindowHost pointer-down, `UpdateHover`, `ScrollPanel` hover/capture, `TabControl::CloseTab`. Establish one "capture token before user callback → re-validate or bail after" convention; `ControlBelongsToTree(_root.get(), target)` is the existing validation helper. → **B-S0-1**.
2. **UI-thread-built immutable UIA snapshots.** `DxUi.h:2660` says "Model state accessed only on UI thread." UIA reads violate this on RPC worker threads. The chosen architecture is snapshots: the UI thread is the only code allowed to walk the retained tree or Tree/Grid client models; it copies UIA-visible names, roles, states, bounds, runtime-id ingredients, selection/focus, and supported pattern data into immutable snapshot records. Provider/RPC threads read only those snapshots. `GetAccessibilityTargetMutex()` is not a live-tree lock; use it only for provider bookkeeping or atomic snapshot publication as needed. → **B-S0-2**.
3. **Single-line DWrite layout / model-callback caching on the paint+hit-test hot path.** `IDWriteTextLayout`/`IDWriteTextFormat` recreated (and `GetCellData`/`GetVisibleItem` materialized with owning `wstring`s) per frame / per cell / per mouse-move across Grid, Tree, TextInput, ComboBox, MenuBar, TabControl, Typography. Build one shared cache keyed on `(text, width, role, readingDirection, dpi)` + a per-frame content-rect/displayText memo; route all consumers through it. → **B-S1-3, B-S1-4, B-S1-5, B-S2-1**.
4. **Redundant UIA control-tree re-resolution.** Provider methods re-run `ResolveControl*`/`ResolveGridCellData`/`ResolveTreeItemData` several times per public call; `Supports*` predicates re-resolve from root on every probe. With snapshots, resolve once on the UI thread while building/refreshing the snapshot, store the minimal UIA-facing data, and make provider methods query snapshot ids rather than live controls. → **B-S1-5, B-S2-2**.
5. **Divergent magic-number policies for shared widgets.** Scrollbar track-click paging now routes through `ComputeScrollbarPageStepDip`; the central pointer-input router is trimmed to delivered Win32 message metadata only, with no source-kind modality enum or unused `MSG` builder. (Mirrors the project's known "divergent fold schemes" family.) → **B-S3-1**.
6. **Manual COM/Win32 resource accounting the RAII rule forbids.** Hand-coded AddRef/Release-on-OOM across ~12 provider factories; 6 near-identical SAFEARRAY runtime-id builders; hand-rolled `BeginPaint/EndPaint`; an HRGN released-before-success-check. Factor `MakeProvider<T>`, `BuildRuntimeId`; use `wil::BeginPaint`. → **B-S1-6, B-S2-2**.
7. **Activation/notification logic copy-pasted across input paths.** Toggle/RadioButton state-transition+invalidate+notify duplicated 5×/3×; const/non-const hit-test overloads byte-identical. Centralize into `ApplyCheckedState`/`SelectSelf`; delegate const → non-const. → **B-S3-1**.

---

## Phase P0 — Ship-blockers: reentrancy UAF, accessibility threading, device-loss recovery

### B-S0-1. `[CRITICAL / reentrancy — pointer-down UAF]` WindowHost dispatch dereferences the hit-test target after a handler that destroyed it

**Symptom:** A heap use-after-free on the most common file-manager gestures — double-click a folder row, click a navigation-tree item, right-click + context-menu action — leading to crashes or silent memory corruption.

**Root cause:** `DxUi.WindowHost.cpp:2412-2472`. The `WM_LBUTTONDOWN/WM_RBUTTONDOWN/WM_*BUTTONDBLCLK` handler dispatches to a raw, non-owning `Control* target`:
```
const bool controlHandled = target && (doubleClick ? target->OnMouseDoubleClick(...) : target->OnMouseDown(...));
...
if (controlHandled) {
    if (target->IsFocusable())            // <-- direct deref of possibly-freed target (line ~2451)
        if (target->SupportsTextInput())  // <-- direct deref (line ~2453)
            SetFocusControl(target);
        ...
    CaptureMouse(target);
    RememberPointerButtonDown(target, ...);
}
```
`SetFocusControl`/`CaptureMouse` internally re-validate via `ControlBelongsToTree`, but `IsFocusable()`/`SupportsTextInput()` at `:2451/:2453` deref the raw pointer with **no** guard. The dispatched handler routinely tears down the tree synchronously: `Grid::OnMouseDoubleClick` → `OnGridRowActivated` (folder navigate → panel rebuilt), `Tree::OnMouseDown` → `OnTreeItemInvoked`/`OnTreeToggleExpanded`, right-click → `Control::OnContextMenu` → Delete/Open rebuilds the view. All return `true`, after which `target` is dangling.

**Fix:** Capture the lifetime token (`Control` already exposes `GetLifetimeToken()`/`std::weak_ptr<int> _lifetimeToken`) **before** the `OnMouseDown/OnMouseDoubleClick` call; bail out of the entire `controlHandled` post-dispatch block if it expired — or re-validate with `ControlBelongsToTree(_root.get(), target)` before any post-callback deref, routing `IsFocusable`/`SupportsTextInput`/`CaptureMouse`/`RememberPointerButtonDown` through that guard. **Apply the identical guard to `WM_LBUTTONUP`**, including diagnostic/trace helpers that describe the target after `OnMouseUp`. Establish this as the framework-wide convention.

**Sibling sites folded into this slice (same shape):**
- `WindowHost::UpdateHover` `:3785-3811` — re-validate `target` against `ControlBelongsToTree`/token before assigning `_hoveredControl` and before `OnHoverChanged(true)`/`OnMouseMove`. *(LOW today: not reachable with stock controls, but same defect.)*
- `WindowHost` keyboard/focus paths — `OnKeyDown`, `OnKeyUp`, `OnChar`, `OnContextMenu`, `SetFocusControl`, `OnKillFocus`, and hover/capture-loss cleanup must not sync native text input, focus/capture a control, or call follow-up hover/focus callbacks unless the target survived the callback and is still effectively interactive.
- `ScrollPanel` `DxUi.Controls.cpp:6204-6210, 6432-6454, 6662-6731, 6825-6861` — raw `_innerHoveredChild`/`_innerCapturedChild` can dangle when a child handler replaces children; recompute from the live `_children` span after any dispatch instead of caching raw pointers across a callback. *(MED.)*
- `Controls.cpp:5197-5218, 6733-6742, 6688-6701, 2929-2933` and `TabControl::CloseTab` — snapshot a lifetime token before user callbacks (`onTabClosed` etc.) and skip post-callback member access/`Invalidate` if expired.

**Required proof:** A DxUiTests case double-clicks a row whose delegate resets `_root`; assert no UAF under AppVerifier/ASAN and that no `target->` deref occurs after the callback without re-validation (structural + grep). RED on current tree. **Status:** complete; deterministic RED/GREEN coverage now includes root-replacing pointer down/up, root-replacing hover enter, key-down and char callbacks that disable the focused target, and focus-loss callbacks that invalidate the requested next focus target. `ConnectionCredentialPromptWindow::ShowModal` now also destroys its HWND on `GetMessageW` failure / `WM_QUIT` before returning from the modal loop, so the normal `WM_NCDESTROY` path detaches `WindowHost` before prompt object destruction; `TestConnectionCredentialPromptDestroysWindowOnModalQuit` failed on the previous non-destroy exit and now guards it. The credential prompt crash shape `RtlSecureZeroMemory -> TextField::SecureClear -> ~ConnectionCredentialPromptWindow` is guarded by making that production call shape impossible: prompt teardown no longer has `ControlTreeContains(...)` / `SecureClearLiveSecretField(...)`, `_secretField->SecureClear`, or `_userField->SecureClear`; `TextField` no longer exposes public `SecureClear()` or a `TextField::SecureClear()` implementation; and `TextField::~TextField()` wipes `_text`, undo/redo history strings, and cached layout text from inside the object while its members are alive. `TestConnectionCredentialPromptTeardownDoesNotWipeThroughRawTextFieldPointers`, `TestTextFieldDoesNotExposeLiveSecureClearTeardownApi`, and `TestTextFieldDestructorOwnsSecureStorageWipe` failed on the previous raw-pointer/public-live-wipe design and now guard this contract. `WindowHost::DeactivateNativeTextInputSession(...)` routes cache invalidation through `SecureClearNativeTextInputStateCache()`, which wipes `NativeTextInputState::text` before resetting the cache; `TestNativeTextInputDeactivateSecureClearsCachedText` failed when deactivation only set `_nativeTextInputStateCacheValid = false` and now guards the cache-copy secret lifetime. After the repeated crash report, teardown now also disconnects `DxUiTextStoreACP` before TSF `Pop`/`Deactivate`/COM release can synchronously re-enter the store: `DisconnectNativeTextInputTextStore(...)` nulls `_host`/`_control`, drops the sink, and secure-clears `_observedState.text`; `DeactivateNativeTextInputTsf()` calls it before `documentMgr->Pop(...)`, `threadMgr->Deactivate()`, and `_nativeTextInputTsfTextStore.reset()`. `TestNativeTextInputTsfDeactivateDisconnectsTextStoreBeforeTsfCallbacks` failed before that helper/order existed and now guards the reported lifetime hole. The command selftest `cmd_connection_credential_prompt_theme_cycle_keeps_surface_legible` seeds a live secret through theme swaps before Escape teardown, covering the reported stack's scenario; latest rerun after the repeated crash report passed via `.\.build\x64\Debug\RedSalamander.exe --commands-selftest --selftest-case=cmd_connection_credential_prompt_theme_cycle_keeps_surface_legible --selftest-timeout-multiplier=2.0`, then passed 5/5 repeated iterations of that exact command. `WindowHost::~WindowHost()` now calls `Detach()` as a last-resort teardown path, which releases native text input/TSF state before `_root` member destruction can destroy retained text fields; `TestWindowHostDestructorDetachesBeforeMemberTeardown` failed on the passive destructor and now guards the order. Fresh guard after the latest pasted crash: `TestWindowHostDetachDeactivatesSecureTextInputBeforeDestroyingRoot` installs a focused masked `TextField`, proves native text input contains the secret before detach, and then asserts `Detach()` destroys the owned field only after clearing focus, the text-input session, and native cache; RED was `FAILED: detach clears text-input session before retained secure text field storage is destroyed` when `ResetRootInteractionState()` temporarily omitted `DeactivateTextInput(true)`, and GREEN passed after restoring that path. The still-reproducible theme-cycle crash also exposed a command selftest lifetime hole: `RunUiaTaskWithMessagePump(...)` could return after `worker.detach()`, leaving a UIA worker alive while the credential prompt destructed. Timeout now requests stop, keeps pumping, joins the worker, and only then returns a safe sentinel; `TestConnectionCredentialPromptUiaPumpDoesNotDetachTimedOutWorkers` failed on the detached-worker helper and now guards no-detach/request-stop/join-before-return order. Fresh 2026-06-21 verification includes: `.\build.ps1 -Configuration Debug -ProjectName DxUiTests`; RED `.\.build\x64\Debug\DxUiTests.exe --suite=NativeTextInput` failure on `TestNativeTextInputTsfDeactivateDisconnectsTextStoreBeforeTsfCallbacks` before adding the disconnect hook; RED `.\.build\x64\Debug\DxUiTests.exe --suite=WindowHost` failure on `TestConnectionCredentialPromptUiaPumpDoesNotDetachTimedOutWorkers` before removing `worker.detach()`; RED `.\.build\x64\Debug\DxUiTests.exe --suite=WindowHost` failure on `TestWindowHostDetachDeactivatesSecureTextInputBeforeDestroyingRoot` before restoring text-input deactivation; GREEN `.\.build\x64\Debug\DxUiTests.exe --suite=NativeTextInput`; GREEN `.\.build\x64\Debug\DxUiTests.exe --suite=WindowHost`; `.\.build\x64\Debug\DxUiTests.exe --suite=TextField`; production grep `rg --glob "*.cpp" --glob "*.h" -n -- "TextField::SecureClear|->SecureClear\(|\.SecureClear\("` shows no production `TextField::SecureClear` call/definition; `.\build.ps1 -Configuration Debug -ProjectName RedSalamander`; `.\.build\x64\Debug\RedSalamander.exe --commands-selftest --selftest-case=cmd_connection_credential_prompt_theme_cycle_keeps_surface_legible --selftest-timeout-multiplier=2.0`; `.\.build\x64\Debug\RedSalamander.exe --commands-selftest --selftest-fail-fast --selftest-case=cmd_connection_credential_prompt_ --selftest-timeout-multiplier=2.0` passed all 8 credential-prompt cases; and latest focused archives are `Specs/TestRuns/a72512919/DxUi/2026-06-21_215557/` plus `Specs/TestRuns/a72512919/Commands/2026-06-21_215557/`. Fresh AppVerifier Basics proof for the credential-prompt theme-cycle crash route is archived at `Specs/TestRuns/a72512919/AppVerifier/2026-06-27_173006/` (`RedSalamander.exe` verifier enabled, focused command selftest exit 0, verifier settings removed). Fresh ASan proof is archived at `Specs/TestRuns/a72512919/DxUi/2026-06-27_184400_BS01_ASan/` and `Specs/TestRuns/a72512919/Commands/2026-06-27_184400_BS01_ASan/`: first ASan `WindowHost` run caught a test-probe-only self-UAF in `RootReplacingHoverControl::OnHoverChanged` because the probe replaced its own root and then called `Control::OnHoverChanged(...)` on the freed object; the probe now calls the base hover setter before self-destruction while preserving the root-replacement scenario. After that fix, `ASan Debug` `DxUiTests` rebuilt with 0 warnings/errors, focused ASan `WindowHost`, `Control`, `NativeTextInput`, `TextField`, and `Accessibility` suites exited 0, the full ASan `DxUiTests` executable exited 0, ASan `RedSalamander` rebuilt with 0 warnings/errors, and the exact credential-prompt theme-cycle command plus the broader `cmd_connection_credential_prompt_` command group both exited 0.

**Latest 2026-06-21 still-crash triage:** the newly pasted stack still names `Z:\src\RedSalamander\.claude\worktrees\agent-a16744a4edd11e9f9` and the obsolete `TextField::SecureClear` symbol/call shape. `Test-Path Z:\src\RedSalamander\.claude\worktrees\agent-a16744a4edd11e9f9` is false in this session, while current workspace `C:\Users\eric\.codex\worktrees\1c07\RedSalamander` rebuilt `RedSalamander.exe` SHA256 `A03520F263DAD212507647CBC218A93A78A73802C9238B8DA91832E6F2D170CB`, production grep finds only the `TextField::SecureClearStorage()` implementation and the guard-test assertion for the obsolete name, the exact command selftest exits 0, 5/5 repeats exit 0, the credential-prompt command group exits 0, and the `WindowHost`, `TextField`, and `NativeTextInput` guard suites all pass. Archive: `Specs/TestRuns/a72512919/CrashTriage/2026-06-21_222808/`. Treat any remaining `TextField::SecureClear` crash as a stale/different binary/worktree until a stack from the current rebuilt binary proves a new root cause.

**Fresh 2026-06-21 22:51 current-worktree verification:** after the latest pasted `agent-a16744a4edd11e9f9` stack, `Test-Path Z:\src\RedSalamander\.claude\worktrees\agent-a16744a4edd11e9f9` still returns false from this session; current source still has no production `TextField::SecureClear` call/definition; `TextField::~TextField()` owns `SecureClearStorage()`; and `ConnectionCredentialPromptWindow::~ConnectionCredentialPromptWindow()` destroys/detaches the HWND then clears only prompt-owned `_acceptedSecret`. Rebuilt `DxUiTests` and `RedSalamander` from `C:\Users\eric\.codex\worktrees\1c07\RedSalamander` with 0 warnings and 0 errors; rebuilt `RedSalamander.exe` SHA256 is `B5A52ECFB9632CD7A595544D59754E177D6AB0321E910137BA5D98561DA4EE0D`. Fresh GREEN checks: `.\.build\x64\Debug\DxUiTests.exe --suite=TextField`, `--suite=NativeTextInput`, `--suite=WindowHost`, exact `cmd_connection_credential_prompt_theme_cycle_keeps_surface_legible`, 5/5 repeats of that exact command, and the broader `cmd_connection_credential_prompt_` command group. Archive: `Specs/TestRuns/a72512919/CrashTriage/2026-06-21_225116/`. A remaining crash with `TextField::SecureClear` is therefore not from this current rebuilt binary; the next actionable crash must include the stack and binary path/hash from the current worktree, or a dump from the rebuilt executable, to distinguish stale binary execution from a new root cause.

---

**Additional 2026-06-21 crash note:** The repeated `RtlSecureZeroMemory -> TextField` credential-prompt theme-cycle crash also confirmed that UIA provider/RPC threads must never obtain live retained controls during prompt teardown. B-S0-2 now enforces that by making `WindowHostAccessibilityTarget::ResolveHost()` UI-thread-only: off-thread UIA reads stay on snapshots, and UIA actions must marshal before mutation.

### B-S0-2. `[HIGH ×6 / concurrency + resource-lifetime — UIA threading]` Provider reads walk the live tree/models off the UI thread; teardown races in-flight reads

**Symptom:** Intermittent access violations and corrupted property reads whenever a screen reader / UIA client (Narrator, Inspect.exe, Accessibility Insights, automated UI tests) traverses the tree while the app adds/removes/replaces controls (navigation, dialog open/close, list refresh) — reachable in completely normal use by any assistive-technology user. Plus a shutdown UAF when a window/dialog closes mid-traversal.

**Root cause:** `AccessibilityProvider::get_ProviderOptions` (`DxUi.Accessibility.cpp:3026`) returns `ProviderOptions_ServerSideProvider` only (no `UseComThreading`), so UIA calls providers on its own RPC worker threads. Mutating actions are correctly marshalled to the UI thread (`IsCurrentThreadWindowThread()` + `SendMessageTimeoutW`, e.g. `:3981-3992, 4134-4145`). But **all read methods run directly on the worker thread**:
- **Tree walk without sync** (`:168-190, 3160-3479, 3497-3806, 5119-5232`): `GetPropertyValue`, `Navigate`, `get_BoundingRectangle`, `GetSelection`, `GetFocus`, `ElementProviderFromPoint`, `ResolveControl*`, `IsControlPathVisible`, `FindFirstSemanticControl` walk `Panel::GetChildren()` (a `std::span` over `std::vector<unique_ptr<Control>> _children`, `DxUi.h:1406-1429`) while the UI thread mutates that same vector (`Panel::AddChild`/`ClearChildren`, `WindowHost::SetRoot` reset `DxUi.WindowHost.cpp:1299-1306`) with **no lock**. `GetAccessibilityTargetMutex()` is referenced only inside Accessibility.cpp and **never taken by the UI thread during mutation** — it serializes providers against each other, not against the UI thread. A reallocating `push_back` or a `reset()`/`ClearChildren` during iteration is a textbook UAF/torn read.
- **Client model off-thread** (`:3195-3228, 3256-3360, 3457-3476, 4626-4666, 5234-5300, 5366-5383`): violates the explicit contract `DxUi.h:2660` "Model state accessed only on UI thread — no synchronization needed." `GetPropertyValue` (Tree) → `model->GetVisibleItem`; (Grid) → `BuildGridRowAccessibleName` loops `model->GetCellData` per column; `GetSelection` iterates `GetOrderedSelection`/`FindRowByStableId`; `FindTreeItemAtPoint` → `GetItemLayoutMetrics` per row — all on the UIA thread. Client model code (e.g. FolderView) mutates its store on the UI thread without locking *because the contract promised single-thread access*.
- **`GetBoundingRectangles` omits the lock entirely** (`:2513-2553`): unlike every sibling text-range method it takes no `scoped_lock(GetAccessibilityTargetMutex())`; calls `ResolveHost()`/`ResolveControl()`/`ResolveText()` + geometry on the worker thread, so it isn't even serialized against `Detach`. *(Also: it reads/clamps `_rangeStart`/`_rangeEnd` without the lock — MED data race.)*
- **`Compare` omits the lock** (`:2413-2426`): reads another range's mutable endpoints without the accessibility lock. *(LOW.)*
- **Teardown UAF** (`:116-124, 6002-6027` + `DxUi.WindowHost.cpp:1247-1263`): `UnregisterWindowHostAccessibilityTarget` stores `nullptr` into `target->host` under the mutex, but `Detach` then — **outside** the mutex — runs `_root->PropagateHost(nullptr)` and `_root.reset()`, destroying every `Control`. The atomic keeps neither the `WindowHost` nor its tree alive; a provider that captured a non-null host and is still walking (especially the unlocked `GetBoundingRectangles`) derefs freed memory.

**Fix:** Implement the chosen **UI-thread-built immutable snapshot** model globally.
- Add an accessibility snapshot owner on `WindowHost` (or a small adjacent helper) whose refresh runs only on the UI thread. It walks the live retained tree/client models once, copies only UIA-facing data into immutable records, then publishes the completed snapshot atomically for provider threads.
- Snapshot records must contain the data provider methods need without live pointers: stable runtime-id path, parent/child/sibling relationships, control type, localized/name/value text, bounds, visibility/enabled/focus/selection state, supported pattern flags, and Tree/Grid item/cell metadata already reduced to strings/ids/rects.
- Provider/RPC threads may hold snapshot ids/weak snapshot generations, but must never deref `Control*`, `Panel::_children`, or Tree/Grid model interfaces. `ResolveControl*` remains UI-thread-only implementation detail used during snapshot refresh, not provider method execution.
- Mutation/invalidation points (`SetRoot`, `Panel::AddChild`/`ClearChildren`, Tree/Grid data refresh, focus/selection/value/expand changes, bounds/DPI/theme/layout changes, `Detach`) must mark or rebuild the snapshot so UIA clients do not read stale topology. Teardown must publish an empty/dead snapshot before destroying the tree.
- Keep `GetAccessibilityTargetMutex()` only for provider bookkeeping/snapshot publication if needed; do not expand it into a global live-tree lock.

**Watch for:** snapshot freshness and size. Keep records minimal, versioned, and cheap to publish; avoid synchronous UI-thread waits from provider threads where a stale-but-valid snapshot is acceptable. If a provider operation must synchronously refresh, bound it with `SendMessageTimeout` and never hold provider locks while waiting for the UI thread.

**Required proof:** Line-by-line snapshot trace ("provider/RPC thread reads immutable snapshot only; live tree/model derefs occur only on the UI thread during snapshot build; teardown publishes an empty/dead snapshot before `_root.reset()`") + a DxUiTests/Accessibility case that drives UIA traversal (Inspect-style provider walk) concurrently with navigation/list-refresh/window-close under **AppVerifier** (lock + heap) with no AV. Add structural tests/grep guards for the former holes: `GetBoundingRectangles`, `Compare`, `ElementProviderFromPoint`, `GetFocus`, `GetSelection`, and `Navigate` must not call `ResolveControl*` or Tree/Grid model methods directly from provider threads.

**Status:** snapshot migration and provider-thread proof are complete for B-S0-2. `WindowHostAccessibilityTarget` publishes immutable snapshots, `SetRoot` publishes an empty snapshot before teardown and refreshes after installing a root, and focus changes refresh a compact focused-fragment record. The UI thread now also snapshots point-hit/bounds records (control path, hit rect, provider kind, Tree visible index, Grid stable row id/column), navigation records (semantic control order, ordinary control name/help/value/text/selection/type/enabled/focus/password/pattern state, Tree visible count, Tree item text/depth/expanded/child state, Grid row/column counts, Grid visible row indexes/IDs/columns, visible GridHeader names/enabled state, visible GridRow names/offscreen/focus state, visible GridCell control type/name/help/enabled/offscreen/toggle/value/range state, TextField reveal affordance name/enabled state), Tree/Grid selection state, selected text-range bounding rectangles, and the DPI scale needed to convert UIA screen coordinates without resolving the live host. `AccessibilityProvider::GetFocus`, `ElementProviderFromPoint`, `Navigate`, `get_BoundingRectangle`, ordinary Control `QueryInterface`/`GetPatternProvider`/`GetPropertyValue`/`get_ToggleState`/`get_Value`/`get_IsReadOnly`/range metadata, Grid/Table relationship reads (`GetRowHeaders`, `GetColumnHeaders`, `get_RowOrColumnMajor`, Grid row/column count properties, GridItem row/column/span/containing-grid, TableItem header lookups), GridHeader property reads (`GetPropertyValue` GridHeader branch), GridRow property reads (`GetPropertyValue` GridRow branch), GridCell read methods (`QueryInterface`, `GetPatternProvider`, `GetPropertyValue`, `get_ToggleState`, `get_Value`, `get_IsReadOnly`, range metadata), Tree/Grid selection-provider reads (`GetSelection` Tree/Grid branch, TreeItem/GridRow `GetPatternProvider(UIA_SelectionItemPatternId)`, Tree/Grid `UIA_SelectionItemIsSelectedPropertyId`, `get_CanSelectMultiple`, `get_IsSelectionRequired`, `get_IsSelected`, `get_SelectionContainer`), TreeItem state reads (`GetPropertyValue` TreeItem branch, `get_ExpandCollapseState`, TreeItem `QueryInterface`/`GetPatternProvider` for `IExpandCollapseProvider`), Tree/Grid selection/table interface discovery (`QueryInterface`, `GetPatternProvider`), TextPattern document/visible/selection/supported-selection/range-text reads (`GetSelection`, `GetVisibleRanges`, `get_DocumentRange`, `get_SupportedTextSelection`, `AccessibilityTextRangeProvider::ResolveText`) plus selected-range bounding rectangles (`AccessibilityTextRangeProvider::GetBoundingRectangles`), TextEdit active-composition/conversion target reads, and TextField password-reveal pseudo-button reads (`QueryInterface`/`GetPatternProvider` for `IInvokeProvider`, `GetPropertyValue`) now load the published snapshot and return providers, rectangles, header names, row names, cell state, selection state, ordinary control state, text range content, or reveal button state without `ResolveHost`, `ResolveRootControl`, `GetFocusControl`, `FindSemanticControlAtPoint`, `ResolveControlAtPath`, semantic live-tree navigation, Tree layout materialization, live TextField reads, or live Tree/Grid model reads on the provider thread. Arbitrary/moved TextRange `GetBoundingRectangles` and visual-line TextRange `Move` / `MoveEndpointByUnit` now copy the requested range under the provider lock, release that lock, and use bounded `SendMessageTimeoutW` dispatch so exact DirectWrite geometry is resolved only on the owning window thread before provider-thread screen-coordinate conversion or range mutation. `WindowHostAccessibilityTarget::ResolveHost()` now additionally returns a live host only on the owning window thread, so remaining live geometry/action helpers cannot hand provider/RPC threads a UI-thread-affine retained tree during prompt teardown. Masked TextFields still report `UIA_IsPasswordPropertyId` from the snapshot but do not expose TextPattern/TextEditPattern over protected content, closing that live text-range route during secure teardown. TextEdit active-composition/conversion target range text and state lookup now use captured native input-state snapshot endpoints/text. Successful Tree expand/collapse, GridCell toggle/select, TextField text/selection/masking/reveal changes, and ComboBox editable text/selection/caret changes refresh the published snapshot before invalidating so the next UIA state query is not stale. RED/GREEN proof: `TestAccessibilityTargetPublishesImmutableSnapshotBeforeTreeTeardown` failed on the live-read implementation and now structurally guards empty-before-clear teardown plus snapshot-backed `GetFocus`; `TestAccessibilityLiveHostResolutionIsWindowThreadOnly` failed while `ResolveHost()` returned live hosts to any thread and now guards `GetWindowThreadProcessId(...)`/`GetCurrentThreadId()` enforcement; `TestAccessibilityElementProviderFromPointUsesSnapshot` failed on the live-hit-test implementation and now guards snapshot-backed point resolution; `TestAccessibilityNavigateUsesSnapshot` failed on the live-navigation implementation and now guards snapshot-backed fragment navigation; `TestAccessibilityBoundingRectangleUsesSnapshot` failed on the live-bounds implementation and now guards snapshot-backed rectangle resolution; `TestAccessibilityGridPatternReadsUseSnapshots` failed on the live-grid-read implementation and now guards snapshot-backed Grid/Table relationship and GridHeader property reads; `TestAccessibilityControlStateReadsUseSnapshots` failed on ordinary live-control property/pattern/value/read-only fallbacks and now guards snapshot-backed ordinary control reads; `TestAccessibilityGridRowPropertyReadsUseSnapshots` failed on the live GridRow property implementation and now guards snapshot-backed GridRow property reads; `TestAccessibilityGridCellValueReadsUseSnapshots` failed on the live GridCell read implementation and now guards snapshot-backed GridCell pattern/property/value/toggle/range reads; `TestAccessibilityTreeGridSelectionReadsUseSnapshots` failed on the live-selection implementation and now guards snapshot-backed Tree/Grid selection reads; `TestAccessibilityTreeItemStateReadsUseSnapshots` failed on the live TreeItem state implementation and now guards snapshot-backed TreeItem state reads; `TestAccessibilityTextPatternDocumentAndSelectionReadsUseSnapshots` failed while TextPattern document/visible/selection reads and range text re-resolved live controls and now guards snapshot-backed TextPattern read state plus selected range bounding rectangles; `TestAccessibilityTextRangeBoundingRectanglesDispatchesToWindowThread` failed while moved ranges returned from provider threads without window-thread dispatch and now guards non-empty cross-thread moved-range geometry; `TestAccessibilityTextRangeEndpointLineMovementDispatchesToWindowThread` and `TestAccessibilityTextRangeSpanLineMovementDispatchesToWindowThread` failed while provider-thread visual-line moves completed without host window-thread dispatch and now guard wrapped-line DirectWrite movement fidelity; `TestAccessibilityPasswordRevealButtonReadsUseSnapshots` failed on live TextField reveal reads and now guards snapshot-backed reveal invoke/property reads; the masked-TextField TextPattern guard failed while protected controls exposed text providers and now passes. Fresh proof after the repeated credential-prompt crash report: `.\build.ps1 -Configuration Debug -ProjectName DxUiTests`, RED `.\.build\x64\Debug\DxUiTests.exe --suite=Accessibility` on `TestAccessibilityLiveHostResolutionIsWindowThreadOnly`, GREEN `.\.build\x64\Debug\DxUiTests.exe --suite=Accessibility`, `.\build.ps1 -Configuration Debug -ProjectName RedSalamander`, and GREEN `.\Tools\Run-AllTests.ps1 -Suite Commands -SkipBuild -FailFast -CaseFilter cmd_connection_credential_prompt_theme_cycle_keeps_surface_legible`; focused archives: `Specs/TestRuns/a72512919ba5/DxUi/2026-06-21_212632/` and `Specs/TestRuns/a72512919ba5/Commands/2026-06-21_212632/`. Fresh selected-range geometry proof: `Specs/TestRuns/a72512919/DxUi/2026-06-27_165251/` (`Accessibility`, `NativeTextInput`, `TextField`, app build, and 214-row `perf_metrics.jsonl`; 8 `bounding-rectangles-snapshot` rows, max 16 us). Fresh moved-range geometry proof: `Specs/TestRuns/a72512919/DxUi/2026-06-27_170257/` (`Accessibility`, `TextField`, `ComboBox`, app build, and 225-row `perf_metrics.jsonl`; 1 synthetic `bounding-rectangles-dispatch` row, including test wait/pump time). Fresh visual-line movement proof: `Specs/TestRuns/a72512919/DxUi/2026-06-27_171926/` (`Accessibility`, `TextField`, `ComboBox`, app build, and 230-row `perf_metrics.jsonl`; one `move-endpoint-line-dispatch` row and one `move-line-dispatch` row, both synthetic rows include test wait/pump time). Fresh AppVerifier proof: `TestAccessibilityProviderTraversalSurvivesConcurrentRootReplacement` drives worker-thread provider traversal (`GetFocus`, point hit, `Navigate`, property reads) while the UI thread replaces roots, mutates tree items, publishes empty root snapshots, and clears the root; `Specs/TestRuns/a72512919/AppVerifier/2026-06-27_173006/` enabled AppVerifier Basics for `DxUiTests.exe`, ran the full Accessibility suite exit 0, then removed verifier settings. Existing Accessibility focus/point-hit/navigation/bounds/grid-pattern/selection/expand-collapse/text-pattern behavior remains green.

---

### B-S0-3. `[HIGH ×2 / resource-lifetime — device loss]` Render discards GPU resources without re-invalidating; static window stays blank after device loss

**Symptom:** After a real device-lost event (GPU TDR/hang recovery, driver update/restart, RDP/remote-session transition, hybrid-GPU/adapter reset — all surfacing as `D2DERR_RECREATE_TARGET`/`DXGI_ERROR_DEVICE_REMOVED`/`DXGI_ERROR_DEVICE_RESET` from `EndDraw`/`Present`), a static window (idle file pane, a modal/popup host with no animation) renders **blank or stale** until an unrelated event (mouse move, resize, focus, theme change) happens to invalidate it. The window looks hung. Because the device is shared across all WindowHosts on the thread and siblings aren't invalidated either, every sibling host also stays stale. A secondary hazard: when the swap chain is later recreated, the next paint may be a partial `WM_PAINT` whose dirty-rect-only `Present1` leaves the rest of the uninitialized FLIP back buffer as garbage.

**Root cause:** Both `WindowHost::Render` overloads — retail `DxUi.WindowHost.cpp:3314-3323` and the `ENABLE_TESTS` twin `:3526-3535` — handle device loss by `DiscardSizeDependentResources` / `DiscardDeviceResources` / `ResetSharedWindowHostGraphicsResources` then `return;` with **no `Invalidate()`**. Render is driven by `WM_PAINT` (`:2280-2286` via `ScopedPaint`/`BeginPaint`, which validates the update region `:1125-1150`); nothing schedules a new paint after teardown, so no further `WM_PAINT` arrives and the device is never recreated. The debug-only `DebugSimulateDeviceLoss` (`:2098-2104`) *does* call `Invalidate()` after the identical discard sequence — confirming the production path is simply missing it. The animation dispatcher only ticks while something animates (`:3875-3905`), so an idle window has no render loop to recover it. `WindowHost::Invalidate()` (`:1433`) already does the right thing (`InvalidateRect(_hwnd, nullptr, FALSE)`).

**Fix:** In the device-loss branch of **both** `Render` overloads, after the discard sequence, call `Invalidate()` (`InvalidateRect(_hwnd, nullptr, FALSE)`) before returning so a fresh `WM_PAINT` re-runs `EnsureSizeDependentResources`→`EnsureDeviceResources`. Set a "first frame after recreate" flag so the next present is forced **full** (non-partial) regardless of `rcPaint`. Optionally iterate the existing attached-hosts registry to invalidate sibling hosts so the whole UI recovers in one cycle.

**Required proof:** Extend the device-loss selftest: after the discard sequence assert a repaint is scheduled and the device is recreated on the next paint; assert the first post-recreate present is full. RED today (static window with no further input stays blank). *Risk: very low — this is exactly what the debug-simulation and resize/focus paths already do.*

---

## Phase P1 — Serious reliability / correctness / performance

### B-S1-1. `[HIGH / grid correctness]` Bottom partially-visible row is un-painted AND un-hittable

**Symptom:** Every grid whose viewport height isn't an exact multiple of row height (essentially all — `kScrollbarThicknessDip=12`, rows ~28dip) renders a blank strip at the bottom and leaves the last partially-visible row un-paintable **and** un-hittable: it can't be clicked, hovered, context-menued, or selected by mouse. Keyboard nav that scrolls a row flush to the bottom can land selection on an undrawn row.

**Root cause:** `DxUi.Grid.cpp:157-162, 3938-3950` — `BuildVisibleBodyItems` uses a floor-based end-row count; `HitTestPoint` (`:3468`), `GetVisibleRowCount`, `GetVisibleRowAt` all consume the same output, so the dropped row is invisible to both paint and hit-test.

**Fix:** For the **END** boundary only, compute `std::min(sectionRowCount, ceil((visibleBottomDip - sectionTopDip - epsilon) / safeRowHeightDip))` (add a dedicated ceil helper; keep the floor helper for `beginRowOffset`). Also reset `_hoveredRow`/`_hoveredColumn` in `NotifyDataChanged`/`SetModel` (already done after group toggles), or store hover as a stable row id via `FindRowByStableId` consistent with the selection model (folds in the MED stale-hover finding `:2068-2069, 2689`).

**Required proof:** DxUiTests/Grid: with a viewport height that is a non-integer multiple of row height, assert the last visible index == `floor((scroll+viewportHeight)/rowHeight)` and is hit-testable. RED today.

### B-S1-2. `[HIGH / text correctness]` Masked caret, BiDi/RTL geometry, and surrogate-pair undo are wrong

**Symptom:** (a) Masked password fields containing emoji/CJK supplementary/combining characters mis-position caret, drag-selection, and the IME composition window (clamped left) — display dot count ≠ text code-unit count. (b) Caret/selection rendering is visibly wrong for RTL/BiDi single-line input (Arabic/Hebrew, or LTR with embedded RTL runs). (c) Undo after typing an emoji can leave a lone half-surrogate → replacement glyph + corrupt copy/paste.

**Root cause:** (a) `DxUi.TextInput.cpp:3030-3039, 1127-1154, 2517-2555` index `displayText` (one dot per grapheme) with `_text` (code-unit) indices. (b) `DxUi.SingleLineTextEditing.cpp:535-559, 828-865` + `DxUi.TextInput.cpp:1525-1527` use left-anchored scalar X math instead of `HitTestTextRange`. (c) `DxUi.TextInput.cpp:2429-2437, 2908-2917` snapshot undo mid-surrogate-pair.

**Fix:** (a) make masked display text track `_text` in **code units** (one dot per UTF-16 code unit) or build an explicit code-unit↔dot index map; for the Concealed policy route caret/selection through a fixed dot position. (b) use `IDWriteTextLayout::HitTestTextRange` for selection and derive the caret rect from hit-test metrics (as the multiline path does); fix the left-anchored clamp in `TextField::Paint`. (c) suppress the undo snapshot for a lead-surrogate-only insertion (defer until the pair completes) — coordinate with B-S1-3's typing-run coalescing — and validate/strip lone surrogates at the buffer boundary.

**Required proof:** DxUiTests cases: masked caret with emoji; RTL caret/selection rects; undo-after-emoji leaves well-formed UTF-16. *Risk: medium — easy to regress the common LTR/ASCII case; add those as regression guards.* **Status:** complete; exact masked dots now track UTF-16 code units, WM_CHAR surrogate-pair insertion shares one undo boundary, and single-line selection paint unions `IDWriteTextLayout::HitTestTextRange` metrics instead of scalar endpoint offsets. RED/GREEN proof: `TestMaskedTextFieldExactMaskUsesUtf16CodeUnitCount`, `TestTextFieldUndoAfterSurrogatePairInsertionRemovesWholeCodePoint`, and `TestTextFieldBidiSelectionPaintUsesDirectWriteRangeBounds`.

### B-S1-3. `[HIGH / text undo UX + perf]` No undo coalescing; per-keystroke full-buffer copies and uncached layouts

**Symptom:** Ctrl+Z undoes one character at a time instead of by typing run; every keypress in a large multiline field does a full-text heap copy on the UI thread; every paint/blink recomputes `GetDisplayText()` copies, an O(n) grapheme scan, and a fresh single-line layout.

**Root cause:** `DxUi.TextInput.cpp:2404-2455, 2908-2917, 2805-2809` push a full-buffer snapshot per keystroke; `:1447-1538, 3030-3039, 3073-3098` recompute display text + layout per frame; `GetSecretVisibleDotCount` (`:1127-1154`) rescans graphemes per `GetDisplayText`.

**Fix:** Coalesce consecutive same-kind edits (insert-run / delete-run) into one undo transaction, broken by caret jump, selection change, focus change, or edit-kind change; treat a surrogate pair as one unit (subsumes B-S1-2c). Compute `displayText` once per Paint and thread it through; cache the grapheme/dot count, invalidated only when `_text` changes (alongside the existing concealed-mask epoch). Add a single-line `IDWriteTextLayout` cache keyed on `(text, width, height, role, readingDirection)` analogous to `GetOrCreateMultilineLayout`, reused across measure/hit-test/draw within a frame.

**Required proof:** DxUiTests: typing run is one undo unit; delete-run is one unit; boundary breaks (caret jump) split units; allocation count per keystroke drops; layout cache invalidates on text/width/dpi/role change (no stale glyphs). **Status:** complete; merge-aware direct-edit undo now coalesces printable typing runs, plain backspace runs, and plain delete runs while breaking on caret/selection movement and preserving surrogate-pair insertion behavior. `TestTextFieldCoalescedDirectEditsCaptureHistoryOnlyOnce` guards the allocation/copy side by requiring the full `_text` snapshot capture (`CaptureEditHistoryState()`) to stay inside the `if (! coalesced)` branch of `RecordUndoStateForDirectEdit(...)` and to appear only once in that hot path; RED failed when an additional full-history capture marker was introduced, and GREEN passed after restoring the guarded path. Selected single-line paint shares one `IDWriteTextLayout` between normal text draw, selection-bound calculation, and selected-text draw, and computes metrics from that same layout before `SetMaxWidth(...)`; unselected single-line paint uses the same one-wide-layout pattern in `DrawSingleLineTextClipped(...)`; focused single-line paint keeps the pre-draw `EnsureCaretVisible(...)` adjustment and does not repeat it in the caret drawing branch. TextField and editable ComboBox now retain `SingleLineTextLayoutCache`, clear it on relevant text/mask/reveal/padding/bounds/DPI/density/flow-direction invalidation boundaries, reuse it for paint/caret/range geometry, update `IDWriteTextLayout::SetMaxWidth/SetMaxHeight` for width/height changes without recreating the layout, and emit gated `dxui.textinput.single_line_layout_create_count` / `dxui.textinput.single_line_layout_create_us` rows only on actual layout creation/cache misses. Exact masked dot count is O(1) (`_text.size()` after the B-S1-2 UTF-16 mask change) and concealed masked length remains covered by the existing retained bucket cache; `TestTextFieldMaskedDotCountStaysBounded` guards against reintroducing grapheme scans. RED/GREEN proof: `TestTextFieldUndoCoalescesTypingRun`, `TestTextFieldUndoSplitsTypingRunsAfterCaretMove`, `TestTextFieldUndoCoalescesBackspaceRun`, `TestTextFieldUndoCoalescesDeleteRun`, `TestTextFieldCoalescedDirectEditsCaptureHistoryOnlyOnce`, `TestSingleLineSelectionPaintReusesOneTextLayout`, `TestSingleLineTextClippedReusesOneTextLayout`, `TestTextFieldPaintEnsuresSingleLineCaretVisibleOncePerFrame`, `TestTextFieldSingleLineLayoutCacheInvalidatesAndFeedsHotPaths`, `TestEditableComboBoxPaintReusesSingleLineLayoutAndCaretOffset`, and `TestTextFieldMaskedDotCountStaysBounded`. Same-machine before/after proof: `Specs/TestRuns/a72512919/DxUi/2026-06-27_181315/` builds a clean HEAD baseline with an archived instrumentation-only `CreateSingleLineTextLayout` patch and compares it with the candidate. TextField single-line layout-create rows dropped 279→130 (-53.41%) and layout-create duration 11980→6789 us (-43.33%); editable ComboBox rows dropped 23→4 (-82.61%) and duration 1315→347 us (-73.61%); TextField grapheme-step rows dropped 347→39 (-88.76%) and masked secret-render duration dropped 8612→59 us (-99.31%). Earlier focused archives remain: `Specs/TestRuns/a72512919/DxUi/2026-06-21_221439/`, `Specs/TestRuns/a72512919/DxUi/2026-06-21_221923/`, `Specs/TestRuns/a72512919/DxUi/2026-06-21_222225/`, and `Specs/TestRuns/a72512919/DxUi/2026-06-21_224214/`.

### B-S1-4. `[HIGH / grid perf]` Per-frame content-rect/group recompute and per-cell `GetCellData` allocation storm

**Symptom:** For grouped grids a single frame does ~a dozen redundant `CollectOrderedGroups` allocations+sorts + `GetBodyContentHeight` scans; for wide grids each frame constructs O(visibleRows×visibleCols) `GridCellData` (5 `std::wstring` + `com_ptr`) via model callbacks — and animation frames double it via `HasAnimatedVisibleCells`. Avoidable UI-thread churn that hurts scroll/animation frame pacing.

**Root cause:** `DxUi.Grid.cpp:4014-4053, 4055-4067, 4115-4169, 2285-2316` (`GetContentRect`/`CollectOrderedGroups` recomputed many times per Paint); `:2122-2123, 2359-2378` (per-cell `GridCellData` rebuilt every frame + again every tick).

**Fix:** Compute ordered groups + content rect **once per frame** and thread them through, or memoize `GetContentRect` keyed on bounds/scroll/header/column state, invalidated by `NotifyDataChanged`/`SetModel`/resize; extend the existing `_cachedGroups` to the scrollbar geometry helpers. Reuse a single `GridCellData` per loop iteration (clear/reset rather than reconstruct) or populate `string_view`s/an arena; for animation detection, have the model expose a cheap "has animated cells" hint or cache the animated-cell set computed during Paint instead of a second full scan in `Tick`.

**Required proof:** DxUiTests/Grid: assert geometry equals a freshly-computed value after each invalidation trigger (data change, sort, filter, resize, dpi, column/header change); allocation/callback count per frame drops. *Risk: medium — cache must invalidate on every layout-affecting state.* **Status:** complete for this slice. `Grid::Paint` now collects ordered groups once, computes the content rect from that frame group vector, and builds visible body items from the same group/body-rect inputs instead of re-entering `CollectOrderedGroups` through `GetContentRect`/`BuildVisibleBodyItems`. Visible-cell paint, visible-work metrics, single-cell layout metrics, and the fallback animated-cell scan reuse reset `GridCellData` scratch storage so the common visible-cell loops no longer reconstruct the five-string/COM cell payload per item; `ResetGridCellData(...)` clears caller-visible fields back to the `GridCellData` defaults before each model callback. `GridVisibleWorkMetrics::visibleCellDataReadCount` records the number of cell-data callbacks performed by the bounded visible-work query, and `TestGridVisibleWorkMetricsStayBoundedForLargeDatasets` guards that the large-grid query reads exactly once per visible cell (`visibleCellDataReadCount == visibleCellCount`) instead of scanning hidden rows or making extra passes. `Grid::Paint` emits `dxui.grid.paint_cell_data_reads` with value0 = visible paint cell-data callbacks and value1 = visible body-item count, guarded structurally by `TestGridPaintEmitsCellDataReadMetric`; the latest Rendering archive contains 56 matching JSONL entries under `Specs/TestRuns/a72512919/DxUi/2026-06-21_221010/perf/perf_metrics.jsonl`. `TestAttachedLargeGridPaintReusesCellDataStringStorage` proves the 1,000,000-row/64-column/512-wchar payload paint path reuses `GridCellData::text` storage across visible cells and grows that storage at most once per repaint. `Grid::Tick` reuses Paint-discovered animated visible-cell state and avoids repeated model `GridCellData` scans, while preserving the pre-paint Tick path through a lazy cache seed. RED/GREEN proof: `TestGridPaintReusesCollectedGroupsForContentAndVisibleRows`, `TestGridTickUsesPaintDiscoveredAnimatedCellState`, `TestGridPaintReusesCellDataScratchStorage`, `TestGridPaintEmitsCellDataReadMetric`, the `visibleCellDataReadCount` assertion in `TestGridVisibleWorkMetricsStayBoundedForLargeDatasets`, and `TestAttachedLargeGridPaintReusesCellDataStringStorage`; fresh focused `Grid` and `Rendering` suites are green. Focused archive: `Specs/TestRuns/a72512919/DxUi/2026-06-21_221010/`. Decision: do not add broader cell-data memoization now; current metrics show bounded visible callbacks and allocation reuse, and a new cache would add invalidation complexity without measured need.

### B-S1-5. `[HIGH / TSF + UIA perf]` `GetTextExt` O(n) layouts; UIA tree-walk allocation churn; redundant resolution

**Symptom:** TSF/IME/screen-reader extent queries on a large range in a multiline field stall the UI thread (O(n) DirectWrite layouts per character); UIA hit-testing a Tree allocates a full `TreeItemData` per visible row per query; `Supports*` predicates and `Navigate` re-resolve the control tree from root multiple times per single UIA query.

**Root cause:** `DxUi.TextStoreACP.cpp:104-139, 680-693` (`GetTextExt` multiline = O(n) layouts); `DxUi.Accessibility.cpp:5366-5383` (per-row `TreeItemData` in `FindTreeItemAtPoint`); `:5302-5364` (`Supports*` re-resolve from root); `:3520-3546` (`Navigate` re-resolves up to 3×); `:3160-3479` (`ResolveControl`/`ResolveGridCellData` run multiple times per public call).

**Fix:** `GetTextExt` — use the existing `Control::TryGetTextInputRangeRects` to get the union rect in one pass, or compute the bounding rect from just start+end caret rects + line metrics; cache multiline layout/line-metrics for the call duration. Tree hit-test — add a geometry helper returning only `rowRect` for a `visibleIndex` without filling text, or compute the index arithmetically (as `DebugGetFirstVisibleIndex` does) + one `GetItemLayoutMetrics` validation. UIA — resolve the control (and grid-cell/tree-item data) once while building the UI-thread snapshot, store only the reduced UIA data needed by provider methods, and add a lightweight tree-item existence predicate that doesn't allocate the four `wstring`s.

**Required proof:** `GetTextExt` union-rect matches the per-index result for wrapped multiline text (selftest); UIA tree hit-test allocation count is O(1) not O(N); provider public methods do zero live-tree/model resolves and consume snapshot records only; snapshot refresh resolves each visible/control item once per refresh pass. **Status:** complete; multiline TSF `GetTextExt` now calls `TryGetTextInputRangeRects` and unions the returned rects before falling back to the old caret walk for collapsed/unsupported ranges. Tree row-index hit testing has a geometry-only `Tree::FindVisibleItemAtPoint(...)` guard, `ElementProviderFromPoint` and `get_BoundingRectangle` now consume UI-thread-built point-hit/bounds snapshot records so provider-thread point and bounds queries do not call `FindSemanticControlAtPoint(...)`, `ResolveControlAtPath(...)`, `GetModel(...)`, `GetItemLayoutMetrics(...)`, or per-query Tree/Grid layout/model walkers, and Tree snapshot hit-record capture starts at `Tree::GetFirstVisibleItemIndex()` plus `Tree::GetVisibleItemHitRect(...)` rather than `GetItemLayoutMetrics(...)`/`GetVisibleItem(...)`. `Navigate` now consumes snapshot navigation records instead of walking `Panel` children or asking Tree/Grid models for visible items on the provider thread; ordinary control property/pattern/value/toggle/range reads now consume snapshot records instead of live `Supports*`, `GetControlAccessible*`, `IsValueReadOnly`, or TextField password-state probes; Grid/Table relationship reads now consume snapshot row/column records instead of `ResolveGridControl(...)`, `ResolveGridCellData(...)`, or visible-column model callbacks; GridHeader property reads now consume snapshot header records instead of `ResolveGridHeaderColumn(...)`; GridRow property reads now consume visible-row snapshot records instead of `ResolveGridControl(...)`, `ResolveGridRowIndex(...)`, or per-query row-name model walks; GridCell property, pattern, value, toggle, and range reads now consume visible-cell snapshot records instead of `ResolveGridCellData(...)` / `SupportsGridCell*` live re-resolution; Tree/Grid selection-provider reads now consume snapshot selection records instead of live Tree/Grid selection/model lookups; TreeItem property/expand-collapse reads now consume one UI-thread-built Tree item snapshot record instead of per-query `ResolveTreeItemData(...)`; TextPattern document range, visible ranges, selection range, supported-selection state, range text, selected range bounding rectangles, `RangeFromPoint`, and TextRange `GetEnclosingElement` now consume snapshot text/selection/geometry records or bounded UI-thread dispatch results instead of resolving live TextField controls on provider threads; arbitrary/moved TextPattern bounding rectangles and visual-line TextPattern `Move` / `MoveEndpointByUnit` now use bounded UI-thread dispatch instead of resolving the live host/control on provider threads; TextEdit active-composition/conversion target ranges now carry captured native input text for range reads; TextField password-reveal pseudo-button Invoke/property reads now consume the TextField reveal snapshot record instead of live TextField visibility/name/enabled probes; and masked TextFields no longer expose TextPattern/TextEditPattern over protected content. RED/GREEN proof: `TestNativeTextInputTextStoreGetTextExtUsesRangeRectsBeforeCaretWalk`, `TestAccessibilityTreeHitTestUsesCheapVisibleIndexLookup`, `TestAccessibilityElementProviderFromPointUsesSnapshot`, `TestAccessibilityBoundingRectangleUsesSnapshot`, `TestAccessibilityGridPatternReadsUseSnapshots`, `TestAccessibilityControlStateReadsUseSnapshots`, `TestAccessibilityGridRowPropertyReadsUseSnapshots`, `TestAccessibilityGridCellValueReadsUseSnapshots`, `TestAccessibilityTreeGridSelectionReadsUseSnapshots`, `TestAccessibilityTreeItemStateReadsUseSnapshots`, `TestAccessibilityTextPatternDocumentAndSelectionReadsUseSnapshots`, `TestAccessibilityTextRangeBoundingRectanglesDispatchesToWindowThread`, `TestAccessibilityTextRangeEndpointLineMovementDispatchesToWindowThread`, `TestAccessibilityTextRangeSpanLineMovementDispatchesToWindowThread`, `TestAccessibilityTextRangeFromPointDispatchesToWindowThread`, `TestAccessibilityPasswordRevealButtonReadsUseSnapshots`, `TestAccessibilityTreeSnapshotHitRecordsUseVisibleGeometryOnly`, and `TestAccessibilityNavigateUsesSnapshot`; existing multiline/wrapped `GetTextExt`, `Accessibility`, `TextField`, `WindowHost`, `ComboBox`, and `Tree` tests remain green. Final focused proof: `Specs/TestRuns/a72512919/DxUi/2026-06-27_174651/` archives RED failures for the old `RangeFromPoint` and `GetEnclosingElement` live-resolution paths, GREEN Accessibility with 0 warning(s)/0 error(s), and 696 perf rows including 4 `range-from-point` rows. Provider public-method live-resolve audit now finds live `Resolve*` calls only in UI-thread `Execute*` action helpers; TextRange exact geometry/movement public methods retain same-thread live branches only after provider-thread dispatch or snapshot fast paths. B-S1-5 proof is complete.

### B-S1-6. `[HIGH / menu+combo lifetime, hit-test, RAII]` Stuck modal menu, over-greedy combo hit-test, class-flag + HRGN bugs

**Symptom:** (a) The modal menu loop becomes stuck (no visible popups, no mouse capture) if the owner window is destroyed while a context menu is open — recovers only on the next mouse-down on the same thread or WM_QUIT. (b) While a combo dropdown is open, an outside click is swallowed, so users click twice; this diverges from standard Windows combobox behavior where the first outside click closes the dropdown and reaches the underlying target/focus path when appropriate. (c) A one-time menu class-registration failure permanently disables every DxUi context menu for the process. (d) Each failed `SetWindowRgn` leaks an HRGN; popups apply a region on every create and DPI relayout.

**Root cause:** (a) `DxUi.Menu.cpp:4826-4848, 3120, 2032-2052`. (b) `DxUi.ComboBox.cpp:2187-2195` — `HitTestOverlay` ignores the hit point and claims every point while the popup is open. (c) `DxUi.Menu.cpp:2106-2119, 1307` — `s_classRegistered=true` set **before** `RegisterClassExW` and its result ignored. (d) `DxUi.Menu.cpp:2942-2945` — `region.release()` before checking `SetWindowRgn`'s result.

**Fix:** (a) in the modal loop detect the root popup HWND becoming invalid (`!GetRootPopup() || !IsWindow(root->hwnd)`) and call `controller.Dismiss()`, mirroring the async `WM_NCDESTROY` teardown. (b) implement standard Windows combobox outside-click behavior: hit-test against the actual interactive region (`GetHitBounds`/`GetPopupBounds` union); return `this` only when inside field or popup; on an outside down, close/cancel capture first, then allow normal WindowHost hit-testing/dispatch to continue to the underlying control so one click can move focus or activate a button. Update `Tests/DxUiTests/DxUiTests.ComboBox.cpp` because the current outside-click consumption assertion is the old, wrong contract. (c) set the flag only **after** `RegisterClassExW` succeeds (treat `ERROR_CLASS_ALREADY_EXISTS` as success), as `NativeMenuBarHost::EnsureWindowClass` already does (`DxUiNativeMenuInterop.h:551`); check + log the return. (d) pass `region.get()` to `SetWindowRgn`; `region.release()` only after it returns non-zero so the `unique_hrgn` dtor frees on failure.

**Required proof:** Selftests — close the owner while a context menu is up (loop breaks); a ComboBox outside click both dismisses the combo and reaches the underlying control/focus path in one click; clicking inside the popup still selects without falling through; clicking the field still follows normal toggle behavior; `SetWindowRgn`-failure path doesn't leak (grep + structural). *(c)/(d) are tiny and obviously correct.* **Status:** complete; covered by focused Menu/ComboBox tests plus the post-fix full `DxUiTests.exe` run.

### B-S1-7. `[HIGH / tree interaction]` Typeahead jam, frozen UIA-expand animation, off-screen context-menu anchor

**Symptom:** (a) One mistyped/non-matching key (or repeated-letter cycling) jams tree type-to-select for ~1s. (b) Screen-reader/automation-driven expand shows a stuck partial-expand frame + stale animation state until an incidental repaint. (c) Shift+F10/Menu-key context menu appears clamped at the wrong place when the selected node is scrolled off-screen.

**Root cause:** (a) `DxUi.Tree.cpp:954-977` — no single-char fallback after the accumulated-prefix lookup fails (diverges from `ComboBox::OnChar`). (b) `:382-406` — `RequestExpandedState` from the UIA path never schedules an animation tick. (c) `:988-1000` — keyboard branch anchors before scrolling the row into view.

**Fix:** (a) mirror `ComboBox::OnChar` — after the prefix lookup fails, retry with `_typeaheadBuffer.assign(1u, ch)` and `FindNextTypeaheadMatch` again; only return false if the single-char retry also fails. (b) have `AccessibilityProvider::ExecuteExpandOnWindowThread` also call `host->RequestAnimation()`, or (better) make `RequestExpandedState` take the `WindowHost` and call `RequestAnimation()` itself so all callers benefit. (c) call `EnsureVisibleIndex(visibleIndex)` and recompute scroll-derived `rowTop` before computing the anchor.

**Required proof:** Selftests — repeated-letter cycling works; UIA-expand animation completes; keyboard context-menu anchors correctly on an off-screen selection. *Risk: low — all three match existing in-framework patterns.* **Status:** complete; RED/GREEN proof added for poisoned typeahead fallback, UIA expand animation scheduling, and off-screen keyboard context-menu anchoring.

### B-S1-8. `[HIGH / theme correctness]` Dark default palette gaps + stale accent variants

**Symptom:** (a) Anything rendering with `MakeDefaultThemePalette(dark=true)` shows light-theme focus stroke / pressed fill / toggle-knob / smoke overlay — confirmed at `RedSalamander/SplashScreen.cpp:350` and `RedConfigure/RedConfigureSplashScreen.cpp:310` (both `SetTheme(MakeDefaultThemePalette(true))` with no overrides). (b) `accentHover`/`accentPressed` are derived once from the default accent and go stale when a caller reassigns `accent` — `RedSalamander/DxUiThemePalette.h:28` sets `palette.accent=theme.accent` but `:99-100` mixes the **default**-derived variants for rainbow header hover/press tints → wrong hue.

**Root cause:** `DxUi.Theme.cpp:901-958` (dark branch leaves `focusStroke`/`pressedFill`/`toggleKnobFill`/`toggleKnobCheckedFill`/`smokeOverlay`/accent at light defaults); `:908-909` (variants derived once).

**Fix:** In the dark branch explicitly set dark-appropriate `focusStroke`, `pressedFill`, `toggleKnobFill`, `toggleKnobCheckedFill`, `smokeOverlay`, and a dark default accent (recompute variants **after** any accent change) — or give `ThemePalette` a dark-aware default constructor. Provide `RefreshAccentVariants(palette)` (recomputes `accentHover`/`accentPressed` from `accent`, as `MakeThemePaletteFromViewerTheme:1017-1018,1030-1034` already does correctly) and call it wherever `accent` is set; fix the App and Monitor palette builders.

**Required proof:** Screenshot/selftest of the splash screens + rainbow header tints in dark mode; assert variants track a reassigned accent. *Risk: low — color-value corrections; eyeball the dark values.* **Status:** complete; `RefreshAccentVariants(...)` now centralizes derived accent variants, default dark tokens are dark-specific, and app palette builders refresh variants after assigning `accent`.

---

## Phase P2 — Performance hot-path caching & architecture consolidation

### B-S2-1. `[MED / perf]` O(n²) and per-frame DirectWrite measurement across MenuBar/TabControl/ComboBox/Tree/Typography

Establish the shared single-line measurement/layout cache from Theme 3 (keyed on `text, role, readingDirection, dpi[, width]`) and route all consumers through it.
- `DxUi.Controls.cpp:4331-4368, 4626-4693, 4609-4624` — **MenuBar::Paint** O(n²) layout creation per frame/hover: done; `MenuBarLayoutCache` now stores measured item widths and laid-out rects, rebuilds only on item/host/bounds/density/DPI/flow/theme-metric changes, `GetItemRect`/hit-testing reuse cached rects, `Paint` hoists `GetVisualHighlightIndex` out of the loop, and cache misses emit gated `dxui.menubar.layout_rebuild_count`.
- `DxUi.Controls.cpp:4991-5031, 5033-5044, 5312-5455, 4939-4965` — **TabControl::Paint** O(n²); `GetTabRect` re-measured all preceding tabs per call: title-width caching plus header tab-width/rect/total-width/overflow/button-rect memoization is in place, guarded, and instrumented; cached measurements invalidate on `_tabs`/bounds/scroll/host/dpi/density/flow changes.
- `DxUi.ComboBox.cpp:814-865` — editable combo recreated 4–5 layouts/frame and computed `EnsureEditableCaretVisible` twice: done; editable ComboBox now owns the shared `SingleLineTextLayoutCache`, invalidates it on text/import/undo/redo/direct-edit mutations, computes caret visibility once per paint, reuses that caret offset for caret drawing, and draws selection/text through `DrawSingleLineSelectionWithLayout(...)`.
- `DxUi.Tree.cpp:1054-1063` — per-row/per-frame badge layout + per-mousemove tooltip layout: done; Tree now caches badge width by badge text plus text-format identity, caches tooltip overflow for the current hovered visible index/item/text/tooltip/format/available-width tuple, invalidates both caches on model/data/density changes, and emits gated `dxui.tree.badge_width_cache_miss_count` / `dxui.tree.tooltip_overflow_cache_miss_count` counters.
- `DxUi.Typography.h:298-362` — measurement helpers queried the system font collection on every call, in per-row dialog loops (`RedSalamander/ManagePluginsDialog.cpp:3292-3293,3386-3387`; `Preferences.*.cpp` per card): done; Typography now caches font-family availability per `IDWriteFactory*` + family name and cached measurement `IDWriteTextFormat` instances per `IDWriteFactory*` + `FontRole` + wrap mode. Public-header cache instrumentation uses an opt-in atomic emitter hook so plugin TUs do not inline-link Common perf internals.
- `DxUi.ComboBox.cpp:193-253, 2349-2389`; `DxUi.Menu.cpp:697-757, 3006-3024, 3187-3218`; `DxUi.cpp:20-88` — Mica/Acrylic popup backdrop capture: done for bounded capture and dedup. ComboBox/Menu no longer keep duplicate `BitBlt`/`CreateDIBSection` helpers; both call shared `CaptureBackdropScreenRegion(...)` after layout, with ComboBox passing `popupRectPx` and Menu passing `surfaceRectPx` rather than the larger shadow/window rect. The helper owns the GDI screen-copy implementation, clears failed captures, rejects empty/overflow-sized rectangles before allocation, and preserves component-specific diagnostics.

**Required proof:** before/after frame-time + allocation counts; a rect-correctness selftest after each invalidation trigger. *Risk: medium — many invalidation sites; convert incrementally.* **Status:** complete; `DrawSingleLineSelection(...)` now creates one `IDWriteTextLayout` for selected TextField/editable ComboBox paint, reuses it for normal text, selection bounds, and selected text, and avoids the old `ResolveSingleLineLayoutWidthDip(...)`/`MeasureSingleLineTextWidthDip(...)` pre-measurement layout on that path. The shared single-line layout helper now has an optional retained cache and gated `dxui.textinput.single_line_layout_create_*` counters; TextField uses the cache for paint/caret/range geometry with invalidation guarded by `TestTextFieldSingleLineLayoutCacheInvalidatesAndFeedsHotPaths`, and editable ComboBox now owns the same cache, uses it for paint selection/text and caret visibility, and reuses the single visibility pass's caret offset for caret drawing. TabControl now caches title measurement per tab using the `IDWriteTextFormat*` plus reading direction as the cache key, invalidates on title changes, treats closability changes as layout-affecting, and emits gated `dxui.tabcontrol.title_measure_count` on title-measure cache misses. TabControl also owns a lazy header layout cache for tab widths, tab rects, total width, overflow state, viewport edges, and overflow-button rects; `GetTotalTabWidthDip()` and `GetTabRect()` now reuse the cache instead of walking tabs per query, and host propagation invalidates pre-attach fallback measurements before attached DirectWrite widths are used. MenuBar now owns a lazy item layout cache for measured widths and item rects, keyed on host/bounds/reading direction/text-format identity/font role/theme metrics, and paint resolves the visual highlight once per frame. Tree now owns a bounded badge-width cache and hovered tooltip-overflow cache, routes both item-layout overloads and mouse hover through those helpers, and invalidates the caches through one `InvalidateTreeTextMeasurementCaches()` path. Typography now caches family availability and no-wrap/wrap measurement text formats, and its public header exposes only an opt-in metric emitter rather than including `Helpers.h` or inline-calling `Debug::Perf`, which keeps plugin consumers link-safe. Mica/Acrylic popup backdrop capture now routes through shared bounded `CaptureBackdropScreenRegion(...)`, with the duplicate ComboBox/Menu GDI implementations removed and a pre-allocation overflow guard added. RED/GREEN proof: `TestTabControlCachesMeasuredTitleWidths` failed on the old direct `MeasureSingleLineTextWidthDip(...)` width path; `TestTabControlCachesHeaderLayoutRectsAndWidths` failed first on the missing header layout cache and then on missing host-change invalidation; `TestMenuBarCachesItemLayoutRectsAndWidths` guards MenuBar cache shape, invalidation, non-walking rect/hit-test access, highlight hoisting, and the `dxui.menubar.layout_rebuild_count` hook; `TestEditableComboBoxPaintReusesSingleLineLayoutAndCaretOffset` failed on the missing ComboBox retained cache and now guards the one-pass editable paint path; `TestPopupBackdropCaptureUsesSharedBoundedHelper` failed before the shared helper existed and now guards one implementation in `DxUi.cpp`, no component-local `BitBlt`/DIB helpers, ComboBox `popupRectPx` capture after layout, and Menu `surfaceRectPx` capture instead of `windowRect`; `TestTreeCachesBadgeWidthAndHoveredTooltipOverflow` failed on the missing Tree caches and later on missing Tree cache-miss metrics; `TestTreeHoveredClippedTextShowsFullTextTooltip` drives the real hovered clipped-text tooltip path; `TestDxUiTypographyMeasurementCachesFormatsAndFamilyResolution` failed first on missing Typography caches and later on the public-header `Helpers.h` dependency that caused `ViewerVLC` unresolved `Debug::detail` symbols, then passed after the cache and emitter-hook cleanup. Fresh runtime rect proof: `TestMenuBarLayoutCacheRecomputesHitRectsAfterLayoutInvalidations` covers item replacement, bounds movement, and RTL flow through real `HitTestPoint(...)` / `TryGetItemScreenRect(...)`; RED failed with `SetItems invalidation recomputes cached item width` when the MenuBar cache invalidator was mutated to a no-op. `TestTabControlHeaderCacheRecomputesRectsAfterLayoutInvalidations` covers header wheel scroll, title width, closability, wide-bounds overflow removal, and RTL flow through `DebugGetTabRect(...)` / `DebugGetCloseButtonRect(...)`; RED failed with `scroll invalidation recomputes cached tab rect positions` when `ScrollHeaderBy(...)` temporarily skipped header-cache invalidation. Focused `Control`, `Menu`, `ComboBox`, `NewControls`, `Tree`, `Rendering`, and `WindowHost` suites are green for their slices, and a fresh `RedSalamander` build proves plugin consumers are link-safe. Archives: `Specs/TestRuns/a72512919/DxUi/2026-06-27_144824/`, `Specs/TestRuns/a72512919/DxUi/2026-06-27_145621/`, `Specs/TestRuns/a72512919/DxUi/2026-06-27_151600/`, `Specs/TestRuns/a72512919/DxUi/2026-06-27_152500/`, `Specs/TestRuns/a72512919/DxUi/2026-06-27_154820/`, `Specs/TestRuns/a72512919/DxUi/2026-06-27_160335/`, and `Specs/TestRuns/a72512919/DxUi/2026-06-27_161506/`; the ComboBox archive contains 4 `dxui.textinput.single_line_layout_create_count` rows, the Tree archive contains 1 `dxui.tree.badge_width_cache_miss_count` row plus 1 `dxui.tree.tooltip_overflow_cache_miss_count` row, the Typography archive contains 1 `dxui.typography.family_cache_miss_count` row plus 2 `dxui.typography.text_format_cache_miss_count` rows, and the popup-backdrop archive contains 1 `dxui.combo.backdrop_capture_us` row at 252x68 px plus 7 `dxui.menu.backdrop_capture_us` rows bounded to 128x107, 231x38, 128x128, and 128x98 px in `perf/*.jsonl`. Fresh same-machine before/after evidence is archived at `Specs/TestRuns/a72512919/DxUi/2026-06-27_183200/`; a clean `a72512919` baseline was rebuilt, then rerun with the archived baseline-only `baseline_singleline_instrumentation.patch` so old DirectWrite layout creation is counted as an allocation proxy. Instrumented baseline → candidate: ComboBox `dxui.textinput.single_line_layout_create_count` rows 952→10 (-98.95%) and create duration 24963→647 us (-97.41%); Rendering `dxui.textinput.single_line_layout_create_count` rows 13454→15 (-99.89%) and create duration 436111→4755 us (-98.91%). Direct component frame timings also improved where row counts match: Rendering `dxui.tabcontrol.paint` 22275→1618 us (-92.74%) and WindowHost `dxui.tabcontrol.paint` 1807→219 us (-87.88%). Broad `DxUI::Paint` totals are archived but treated as smoke signals only because the candidate suite has additional guard tests/paint rows; the direct component timings and layout-create counters are the closeout proof.

### B-S2-2. `[MED / architecture + RAII]` Single source of truth for UIA patterns, factored COM/Win32 helpers, honest root-pane shape

- [x] `DxUi.Accessibility.cpp:2888-2996` — pattern eligibility encoded twice (QueryInterface vs GetPatternProvider as parallel dispatch tables that must stay in lock-step): both COM entry points now map to `AccessibilityPatternKind` and call one `AccessibilityProvider::QueryPattern(...)`, so snapshot-backed TreeItem/GridRow/Control/GridCell/password-reveal eligibility is encoded once. RED/GREEN proof: `TestAccessibilityPatternDispatchUsesSharedQueryPattern` failed on the split dispatch and now guards the shared helper; focused Accessibility suite is green.
- [x] `DxUi.Accessibility.cpp:1608-2012` — six near-identical RuntimeId SAFEARRAY builders duplicated VT_I4 `SAFEARRAY` creation and `SafeArrayPutElement` loops: collapsed through `BuildRuntimeId(SAFEARRAY**, std::span<const LONG>)` plus shared runtime-id prefix helpers, with named `1001..1005` fragment discriminators. RED/GREEN proof: `TestAccessibilityRuntimeIdsUseSharedBuilder` failed before the helper existed and now guards the single builder; focused `Accessibility` suite is green.
- [x] `DxUi.Accessibility.cpp:5752-5844` — seven `Create*Provider` factories duplicated `AddRefTarget`/`new(nothrow)`/release-on-failure (a single omitted `Release` leaks a target + host for the window's life): collapsed through `MakeProvider<TInterface, TProvider>(Args&&...)`, including the text-range provider path. RED/GREEN proof: `TestAccessibilityProviderFactoriesUseSharedMakeProviderHelper` failed while factories open-coded `new (std::nothrow)`/manual `Release()` and now guards the shared helper; focused `Accessibility` suite is green.
- [x] `DxUi.WindowHost.cpp:1125-1150, 2280-2286` — hand-rolled `ScopedPaint` violates the mandatory-RAII rule: replaced with `PAINTSTRUCT ps{}; wil::unique_hdc_paint hdc = wil::BeginPaint(hwnd,&ps);` and only `Render` when `hdc` is valid (matches `OnThemedMessageBoxPaint`). RED/GREEN proof: `TestWindowHostPaintUsesWilBeginPaintRaii` failed while `ScopedPaint` existed and now guards the WIL paint path; focused `WindowHost` suite is green.
- [x] `DxUi.Accessibility.cpp:417-439, 3170-3193, 3380-3478, 3515-3519, 5130-5156` — root pane provider duplicates a single semantic child as both its own properties and a navigable child (UIA tree announces the control twice, claims Invoke/Toggle/Value it routes to the child, runtime IDs differ): direct single-control roots now use the collapsed-child representation. The root provider exposes the semantic control's properties/patterns, `Navigate(FirstChild/LastChild)` returns no duplicate child, and point-hit/focus resolution returns the root provider for the collapsed path. RED/GREEN proof: `TestAccessibilityProviderExposesDirectSemanticRootControls` failed on the duplicate-child shape and now guards the collapsed root behavior; `TestAccessibilityDirectSemanticRootMatchesUiAutomationClientTree` exercises the OS UIAutomation `ElementFromHandle` path, asserts the direct root is the combo-box content element with the expected `ValuePattern`, and asserts the content-view child count is zero (raw UIA enumeration can include Win32 chrome such as type 50037). Focused Accessibility suite is green. Archive: `Specs/TestRuns/a72512919/DxUi/2026-06-27_180602/`; `inspect_launch.txt` records that `inspect.exe /?` could not be launched in this environment because Windows requested elevation, so the automated proof uses UIAutomation-client traversal over the same provider surface.

**Required proof:** QueryInterface and GetPatternProvider still agree; single-control dialogs expose exactly one accessible content element through UIAutomation client traversal; `BeginPaint`-failure path guarded; existing DxUiTests green. *Risk: low but broad; F31/root-pane carry UIA-behavior risk — verify with Inspect/UIAutomation client.* **Status:** complete; WindowHost paint now uses WIL RAII, Accessibility provider factories use one ownership helper, runtime IDs use one SAFEARRAY builder with named fragment discriminators, UIA pattern eligibility flows through one shared `QueryPattern` helper, the direct-root duplicate-child shape is removed by a collapsed-root selftest, and the UIAutomation client proof shows the HWND provider's content tree contains the semantic combo root with zero content children. `inspect.exe` was present but could not be launched in this environment without elevation, so the archived proof records that limitation and uses the same UIAutomation provider path programmatically.

---

## Phase P3 — Low-risk simplifications (batch opportunistically once structure is stable)

Behavior-preserving; each independently revertable; keep existing DxUiTests green.

| Item | File:line | Action |
|------|-----------|--------|
| [x] Toggle/RadioButton activation duplicated 5×/3× | `Controls.cpp:2505-2595` | Done: `Toggle::ApplyCheckedState(WindowHost&,bool,bool)` and `RadioButton::SelectSelf(WindowHost&)` centralize state mutation, invalidation, and callback dispatch across mouse/key/mnemonic paths. RED/GREEN proof: `TestToggleAndRadioActivationUseSharedHelpers` failed before the helpers existed; focused `Control` and `NewControls` suites are green. |
| [x] Byte-identical const/non-const hit-test overloads | `Controls.cpp:6384-6430, 1195-1277` | Done: `Panel` and `ScrollPanel` const hit-test/overlay lookup overloads delegate to the mutable implementations, keeping one reverse child traversal path. RED/GREEN proof: `TestConstHitTestOverloadsDelegateToMutableImplementations` failed while the const overloads still duplicated `_children`/`GetChildren()` walks and focused `Control` suite is green. |
| [x] Divergent scrollbar paging constants | `Grid.cpp:2876, 2899` (+ Tree/ScrollPanel/Combo) | Done: `ComputeScrollbarPageStepDip(trackRect, orientation, viewport, content)` is shared by Grid vertical/horizontal paging, Tree, ScrollPanel, and ComboBox popup scrollbars, preserving DPI/viewport-aware page deltas through one helper. RED/GREEN proof: `TestScrollbarTrackPagingUsesSharedPageStepHelper` failed while consumers carried divergent page-step math and focused `Control`, `Grid`, `Tree`, and `ComboBox` suites are green. |
| [x] `ResolveScrollbarVisuals` 4-arg recomputes what 6-arg recomputes | `Scrollbar.cpp:22-37` | Done: the strength overload now accepts `ScrollbarAnimationTargets` and Grid/Tree pass their already-resolved target pairs, so reduced-motion fallback no longer recomputes them. RED/GREEN proof: `TestScrollbarVisualStrengthOverloadReusesResolvedTargets` failed on the recompute path and focused `Control` suite is green. |
| [x] Chevron column reserved globally vs per-item | `Menu.cpp:2257-2258, 2343-2346` | Done: `MenuPopup::hasSubmenuItems` is computed by the existing size pass and row layout reserves the chevron lane uniformly when any submenu exists, so plain shortcut rows stay aligned outside the chevron lane. RED/GREEN proof: `TestMenuShortcutRowsReserveChevronLaneWhenAnySubmenuExists` failed on the per-item reservation path and focused `Menu` suite is green. |
| [x] Stale target-bitmap DPI after `WM_DPICHANGED_AFTERPARENT` (size unchanged) | `WindowHost.cpp:2154-2190, 3050-3082` | Done: `OnDpiChanged(...)` now calls `PrepareForSwapChainResize()` after applying the new D2D DPI when a target bitmap is bound, so the next render recreates the target bitmap with the current DPI even if swap-chain dimensions did not change. RED/GREEN proof: `TestWindowHostDpiChangedRecreatesTargetBitmapWhenSizeIsUnchanged` failed before the target detach and focused `WindowHost` is green. Archived focused run: `Specs/TestRuns/4cb089111a23/DxUi/2026-06-21_211347/`. |
| [x] 8ms animation timer kept hot while host hidden | `WindowHost.cpp:3875-3905` | Done: hidden `WindowHost::OnAnimationTick(...)` now records `_animationSuspendedWhileHidden`, clears `_animationSubscriptionId`, and returns `false` so the dispatcher stops waking an invisible HWND; `WM_SHOWWINDOW TRUE` resubscribes once before invalidating so suspended root animation work resumes when visible. RED/GREEN proof: `TestWindowHostHiddenAnimationTickDropsSubscriptionUntilShown` failed while hidden ticks returned `true`; `.\build.ps1 -Configuration Debug -ProjectName DxUiTests` and `.\.build\x64\Debug\DxUiTests.exe --suite=WindowHost` are green. Archived focused run: `Specs/TestRuns/4cb089111a23/DxUi/2026-06-21_211142/`. |
| [x] Redundant deep copy of `beforeItems` on tree expand | `Tree.cpp:389-404` | Done: `beforeItems` is non-const and moved into `BeginTreeExpansionAnimation`. RED/GREEN proof: `TestTreeExpansionAnimationMovesCapturedItems` failed on the old deep copy and focused `Tree` suite is green. |
| [x] PointerInput source plumbing remained after only `WindowProc` was left | `PointerInput.h`, `PointerInput.cpp`, `NavigationView.cpp`, pointer input tests | Done: removed the single-value source enum, the `PointerInputEvent` source field, the source pass-through parameters, the unused `TryBuildPointerInputEventFromMsg(...)` public API, and the now-dead private message-time adapter. Pointer events now carry only the delivered Win32 message metadata, target/root/capture HWNDs, points, and wheel delta. RED/GREEN proof: `TestPointerInputOriginAbstractionIsRemoved` first failed on the remaining enum, later failed on the stale `NavigationView` consumer, then failed on the unused `MSG` builder/private adapter; it now guards that the header/implementation and app routing have no source enum, event field, pass-through parameter, or unused message builder. Focused `Menu` suite and `RedSalamander` build are green. |
| [x] Dead code | `Tree.cpp:1358-1365`, `Typeahead.cpp:8-18` | Done: removed unused `Tree::HasActiveExpanderAnimation` and deleted the unused `NormalizeTypeaheadChar` helper/test dependency so production typeahead stays on the shared `StartsWithInsensitive` path. RED/GREEN proof: `TestTreeRemovesUnusedExpanderAnimationProbe` and `TestTreeRemovesUnusedTypeaheadNormalizationProbe` failed while the dead symbols remained and focused `Tree` suite is green. |

---

## Appendix — base commit & re-verification

- All anchors relative to `master` `a68274ade` (the worktree branch and `master` were at the same commit at review time). **Re-grep every `~line` before editing.**
- The review **refuted 41 of 97 raw findings** (guarded / by-design / impossible); only the 56 above survived an adversarial re-read of the real source. The single-UI-thread assumption was applied when judging "race" claims — the surviving concurrency findings (B-S0-2) are specifically the cross-thread UIA cases.
- Workflow run `wf_ddf495f4-3e9` (115 agents). Standing rule per the Validation Contract: a slice is not done because a selftest is green — for each P0/P1 slice the dangerous/dead/wrong condition must FAIL (RED) on the current tree before the fix, proven on the user's actual route (UAF/race slices: line-by-line lifetime/snapshot trace + structural assert + AppVerifier/ASAN run).
