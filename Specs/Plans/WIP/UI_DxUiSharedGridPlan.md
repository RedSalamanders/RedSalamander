# Full-Window DirectX UI And Shared Grid Plan

Last updated: 2026-03-28

Status: WIP

References:
- `Common/DxUi/DxUi.h`
- `Common/DxUi/DxUi.Internal.h`
- `Common/DxUi/DxUi.cpp`
- `Common/DxUi/DxUi.Controls.cpp`
- `Common/DxUi/DxUi.TextInput.cpp`
- `Common/DxUi/DxUi.Tree.cpp`
- `Common/DxUi/DxUi.Theme.cpp`
- `Specs/Plans/WIP/UI_DxUiWindowMigrationPlan.md`
- `Specs/UI/UI_DxUiSharedGrid.md`
- `Specs/UI/UI_PreferencesDialog.md`
- `Specs/UI/UI_PreferencesDialog_MigrationHistory.md`
- `Specs/UI/UI_ManagePluginsDialog.md`
- `Specs/Plugins/Plugins_ViewerSqlite.md`
- `Specs/FileSystem/FileSystem_FileOperations.md`
- `RedSalamander/FolderWindow.FileOperations.IssuesPane.cpp`
- `PoC/DxUiTests/DxUiTests.cpp`
- `RedSalamander/FindFilesWindow.cpp`
- `Plugins/ViewerSqlite/ViewerSqlite.cpp`

## Living Implementation Checklist

This checklist is the execution control list for the migration:
- mark completed work with `[x]`
- keep pending or active work as `[ ]`
- mark blockers as `[blocked]` and capture the missing dependency or decision
- add newly discovered required work under the relevant phase before continuing
- do not mark a phase complete until its validation items have passed

## Cleanup Milestone Checklist

- [x] Collapse the live high-fanout page and dialog surfaces by rolling the unstable multi-host `Preferences.Viewers`, `Preferences.Plugins` (including the nested plugin-details/configuration editor surface), `Preferences.Panes`, `Preferences.HotPaths`, `Preferences.Advanced`, `Preferences.Themes`, `Preferences.Keyboard`, `Preferences.CompareDirectories`, `Preferences.Editors`, `Preferences.Mouse`, `CompareDirectoriesWindow.Options`, `ConnectionManagerDialog`, and `ManagePluginsDialog` slices back to the proven legacy path.
- [x] Keep the live Preferences DX shell limited to the category tree, the one-host shell header/description/footer chrome, and the shared page-host background/card surface while the broader page-owned re-land work remains deferred.
- [x] Stop building heavy hidden-page child-control trees during Preferences open; non-active pages now create their real page-local child controls lazily on first activation instead of being built during initial dialog creation.
- [x] Stop pre-creating inactive Preferences pane containers during initial open; the dialog first created only the active page container up front, and with `Preferences.General` now direct-hosted it reports zero created or visible pane-host windows on initial open.
- [x] Stop keeping inactive note-page pane containers alive after first activation; `Preferences.Editors` and `Preferences.Mouse` now destroy their pane container and DX note host when leaving the page and recreate them on demand when revisited.
- [x] Remove the dedicated pane child HWND entirely from the simplest direct-host note pages; `Preferences.Editors` and `Preferences.Mouse` now attach their active DX note subtree directly to the shared page host and intentionally report zero created/visible pane-host windows on those pages while keeping one visible DX note host and zero visible legacy note statics.
- [x] Stop creating visible legacy note placeholders on the direct-host note pages; `Preferences.Editors` and `Preferences.Mouse` now create their note content DX-first on the reset path.
- [x] Extend that direct-host reset slice to larger DX-backed pages; `Preferences.HotPaths` and `Preferences.CompareDirectories` now attach their active DX page subtree directly to the shared page host and intentionally report zero created/visible pane-host windows while still keeping one visible DX page surface and zero visible legacy page chrome.
- [x] Extend that direct-host reset slice to the next toggle/input page; `Preferences.Advanced` now attaches its active DX page subtree directly to the shared page host and intentionally reports zero created/visible pane-host windows while still keeping one visible DX page surface and zero visible legacy page chrome.
- [x] Extend that direct-host reset slice to the next larger settings page; `Preferences.Panes` now also attaches its active DX page subtree directly to the shared page host and intentionally reports zero created/visible pane-host windows while still keeping one visible DX page surface and zero visible legacy page chrome.
- [x] Extend that direct-host reset slice through the remaining mixed pages; `Preferences.Themes`, `Preferences.Keyboard`, `Preferences.Viewers`, and `Preferences.Plugins` now also attach their active DX page subtree directly to the shared page host and intentionally report zero created/visible pane-host windows while still keeping one visible DX page surface and zero visible legacy page chrome.
- [x] Remove the last dedicated Preferences pane child HWND by moving `Preferences.General` onto the same direct-host reset path; initial Preferences open now reports zero created/visible pane-host windows while still exposing the shared DX page-host surface.
- [x] Stop relying on visible legacy backing toggles on `Preferences.General`; the direct-host reset path now creates the General toggle-card surface DX-first and applies setting changes directly from DX toggle callbacks.
- [x] Stop creating hidden Win32 label/toggle backing controls on `Preferences.General`; the direct-host reset path now binds labels, descriptions, and toggle state directly to the retained DX subtree and `workingSettings`.
- [x] Stop creating hidden Win32 static/toggle backing controls on `Preferences.Panes`; the direct-host reset path now binds section headers, field labels, descriptions, and toggle state directly to the retained DX subtree and `workingSettings`, leaving only the temporary combo/edit bridge controls.
- [x] Stop creating hidden Win32 static/toggle backing controls on `Preferences.Advanced`; the direct-host reset path now binds section headers, field labels, descriptions, and toggle state directly to the retained DX subtree and `workingSettings`, leaving only the temporary combo/edit bridge controls.
- [x] Stop creating hidden Win32 static/toggle backing controls on `Preferences.CompareDirectories`; the direct-host reset path now binds section headers, field labels, descriptions, and toggle state directly to the retained DX subtree and `workingSettings`, leaving only the temporary combo/edit bridge controls.
- [x] Stop creating hidden Win32 static backing controls on `Preferences.Keyboard`; the direct-host reset path now binds the search/scope labels directly from resources and keeps the hint as explicit page state, leaving only the temporary edit/frame/combo/list/button bridges.
- [x] Stop creating hidden Win32 static backing controls on `Preferences.Viewers`; the direct-host reset path now binds the search/extension/viewer labels and hint directly from resources, leaving only the temporary edit/frame/combo/list/button bridges.
- [x] Stop using hidden Win32 note, label, search-edit, theme/base combo, command-button, swatch, and name/key/color edit backers on `Preferences.Themes`; the direct-host reset path now keeps the page note as explicit page state with no hidden note backing HWND, removes the old hidden label HWND backers, and no longer creates the hidden Win32 search frame/edit, hidden theme/base combos, hidden command buttons, hidden legacy swatch, or hidden name/key/color edits when the shared DX host is active.
- [x] Stop creating hidden Win32 list-mode note/search/custom-paths header/custom-paths note statics on `Preferences.Plugins`; the direct-host reset path now binds those texts directly from resources, details-mode ID/error text now also comes from retained page state on the shared DX path with no hidden ID/error fallback HWNDs, the active DX list-mode path no longer creates hidden legacy search, command-button, or listview bridges, the active DX main-list path now keeps the selected plugin id on retained page state instead of using hidden legacy listview selection as the active-path source of truth, the active DX custom-paths flow now keeps selection/removal on retained page state instead of hidden listview selection, the active DX main-list checkbox flow now keeps enabled/disabled state on retained page state instead of hidden legacy row-state images, and the nested schema-editor path now keeps label/description text plus interactive edit/combo/toggle/checkbox state on retained field state instead of recreating legacy config statics or interactive bridges.
- [x] Require `Preferences.Plugins` main-list round-trip selection to survive explicit hidden legacy listview selection clear on the active DX path; `cmd_preferences_dialog_plugins_main_selection_survives_legacy_list_clear` now proves the selected plugin id remains stable through refresh with zero pane hosts and zero resize failures in both `Debug|x64` and `ASan Debug|x64`.
- [x] Require `Preferences.Plugins` custom-paths round-trip selection to survive explicit hidden legacy custom-paths listview selection clear on the active DX path; `cmd_preferences_dialog_plugins_custom_paths_selection_survives_legacy_list_clear` now proves the selected custom path remains stable through refresh with zero pane hosts and zero resize failures in both `Debug|x64` and `ASan Debug|x64`.
- [x] Require `Preferences.Plugins` main-list checkbox state to survive explicit hidden legacy row-state image overwrite on the active DX path; `cmd_preferences_dialog_plugins_main_checkbox_survives_legacy_row_state_change` now proves the toggled enabled/disabled state remains stable through refresh with zero pane hosts and zero resize failures in both `Debug|x64` and `ASan Debug|x64`.
- [x] Preserve `Preferences.Viewers` search text as explicit retained page state on the direct-host path instead of re-deriving it from the hidden Win32 search edit, and require round-trip validation that a no-match filter survives `Viewers -> General -> Viewers` with zero pane-host windows.
- [x] Preserve `Preferences.Keyboard` search text as explicit retained page state on the direct-host path instead of re-deriving it from the hidden Win32 search edit, and require round-trip validation that a no-match filter survives `Keyboard -> General -> Keyboard` with zero pane-host windows.
- [x] Preserve `Preferences.Themes` selected theme identity as retained page state on the direct-host path instead of resolving active refresh from hidden legacy combo selection, and require round-trip validation that the active theme id survives an explicit hidden legacy combo clear plus `Themes -> General -> Themes`.
- [x] Preserve retained per-page scroll anchors on the direct-host Preferences path instead of globally resetting scroll on every category switch; returning to a previously scrolled page now restores that page's scroll position while first-visited or non-scrollable pages still open at `0`.
- [x] Keep Preferences runtime/debug/self-test visibility accounting on effective visibility instead of raw `IsWindowVisible(...)`; off-screen helper HWNDs that stay `WS_VISIBLE` with `NULLREGION` no longer count as visible shell/page content on the active DX path.
- [x] Update Preferences debug snapshot accounting so direct-host pages measure the shared page host itself as the active DX page surface instead of assuming every active page still owns a child DX host window.
- [x] Introduce a retained shared page-host content root for the active Preferences page subtree and move `Preferences.Viewers` off its page-local DX child-host HWND onto that shared surface.
- [x] Move `Preferences.Keyboard` onto that same shared page-host retained content root and shared `PrefsDxHost` path so shortcut capture, list invalidation, and direct-host debug accounting no longer depend on a dedicated page-local DX child-host HWND.
- [x] Reuse shared graphics-device resources across `DxUi::WindowHost` children so migrated shells stop creating an isolated D3D11/D2D/DXGI/DWrite stack per control host.
- [x] Keep the shared `DxUi::WindowHost` graphics-resource pool single-UI-thread-owned; cross-thread access now logs and fails fast instead of sharing unsynchronized D3D11/D2D/DXGI/DirectWrite state.
- [x] Stop immediate swap-chain recreate storms after `ResizeBuffers(...)` failure; hidden or redraw-suppressed child hosts now defer size-dependent resource creation until visible again.
- [x] Gate child-host invalidation, swap-chain resize, and present work on effective visibility so hidden or minimized DX child hosts stop spamming resize failures during shell/layout churn.
- [x] Keep Preferences redraw-suppression helpers non-allocating in teardown/re-enable paths so cleanup-only batching cannot terminate from destructor allocation.
- [x] Keep Preferences category/page redraw scoped to the shell/page DX hosts instead of dialog-wide erase plus forced category-tree invalidation so page switches stop flashing white and repainting the tree; post-batch redraw now invalidates only the shell/page hosts instead of forcing immediate dialog-wide redraw passes.
- [x] Keep Preferences shell/page/active-pane layout refreshes from repositioning unchanged HWNDs so category switches and page-host relayout do not churn gratuitous repaint work.
- [x] Detach Preferences-owned `DxUi::WindowHost` instances before destroying their owned child HWNDs so nested editor teardown cannot reenter a stale host through destroy-path subclass callbacks, and resolve child-host subclass callbacks from the live window property instead of stale subclass `refData`.
- [x] Keep Connection Manager editor-load combo updates from reentering commit/layout work so DX form-input and form-action hosts stay visible after a list selection settles.
- [x] Keep Connection Manager protocol switches from leaving stale S3/SSH section headers visible after the selected row switches back to FTP-class protocols; the visible DX section-header set must collapse back to list + connection + authentication with zero visible legacy section labels.
- [x] Keep repeated same-row Connection Manager protocol churn (`FTP -> S3 -> FTP -> SFTP -> FTP`) stable so the selected row stays fixed, the DX form remains populated after each settle, the visible DX section-header set returns to the FTP-class baseline after each recovery step, and the live UI Automation subtree keeps visible input descendants plus value/toggle/range pattern support after each settle.
- [x] Remove staged child-host reveal as a cleanup blocker by batching Preferences transitions and by rolling back the broad multi-host page surfaces that were still churning `ShowWindow` / `SetWindowPos` work.
- [x] Unify DXGI present/device-loss recovery across `DxUi::WindowHost`, `FolderView`, and `NavigationView`.
- [x] Keep broad runtime sweeps and new migration breadth paused until validation restarts on this narrowed post-cleanup DX state.

Cleanup milestone status: complete. Remaining work now shifts to validation on the narrowed DX state and later one-host re-landings, not further broad rollback cleanup.

## Progress

- [x] Phase 0. Inventory, acceptance matrix, and full-window DX shell spec. The normative UI spec is published, pilot surfaces are captured, the broader deferred-window queue plus acceptance matrix are frozen in `UI_DxUiWindowMigrationPlan.md`, cross-doc validation is closed, the remaining report-surface plus non-report visible-native inventories are now explicit and CI-guarded through `Specs/UI/UI_VisibleComctlAudit.md`, `Specs/UI/UI_VisibleNativeAudit.md`, `Tools/Audit-ComctlReportSurfaces.ps1`, and `Tools/Audit-VisibleNativeSurfaces.ps1`, and the normative doc set has now been split so Preferences, Manage Plugins, ViewerSqlite, and the File Operations Issues pane no longer rely on this migration plan as their only written home.
- [ ] Phase 1. Shared `DxUi` library and swap-chain window host. In progress: the host now clears the previous D2D target before swap-chain `ResizeBuffers(...)`, reuses shared D3D11/D2D/DXGI-factory/DWrite resources, reuses a shared DWrite factory, rejects mixed-thread access to that shared graphics state, defers swap-chain recreation after `ResizeBuffers(...)` failure until a later visible render, gates invalidation/resize/present work on effective visibility, releases the shared graphics pool when the last attached host detaches so standalone plugin smoke no longer depends on module-unload ordering, now shares the broader DXGI present/device-loss cleanup with `FolderView` and `NavigationView`, serializes retained accessibility-target/UIA provider entrypoints through the shared accessibility-target lock, and now exposes a first shared UI Automation root-provider path through `WM_GETOBJECT` with validated button/toggle/text/focus plus tree/grid metadata, visible grid-header fragments, visible grid-row selection semantics, visible grid-cell grid/table metadata plus readonly value/toggle/range semantics, and provider-specific root runtime IDs in `DxUiTests` under `Debug|x64` and `ASan Debug|x64`, plus live migrated-window `WM_GETOBJECT` and dialog-input pattern validation in command self-tests. The narrowed post-cleanup validation restart is now green; the remaining work is later one-host re-landings, larger migration breadth, and performance/accessibility closure, not additional broad rollback cleanup.
- [ ] Phase 2. Base DirectX controls: buttons, toggles, checkboxes, text input, combo/dropdown, scrollbars. In progress: the shared control stack, input-modality-aware focus policy, modifier-aware keyboard routing, toggle/checkbox keyboard-focus parity with high-contrast fallback, default/cancel button routing, explicit combo variants, masked secret-entry fields, popup polish, repeated-click/Esc popup dismissal, overlay-first popup input ownership, shared control context-menu callback parity across buttons, toggles, checkboxes, text fields, and combo boxes including both non-editable and editable variants via both keyboard and pointer routes, pixel-accurate caret behavior, single-line selection parity, multiline clipboard parity for classic shortcuts (`Ctrl+Insert` / `Shift+Delete` / `Shift+Insert`) plus direct logical and wrapped `Ctrl+A`, `Ctrl+C`, `Ctrl+X`, and `Ctrl+V` routes with the covered no-selection copy/cut no-op behavior plus logical newline-spanning partial-selection copy/cut plus paste-replacement coverage and RichEdit-aware attached hidden-bridge trailing-edge mapping for that path plus wrapped multiline partial-selection copy/cut plus paste-replacement coverage together with attached wrapped hidden-bridge selection alignment and post-cut/post-paste host sync for that path plus direct logical and wrapped selected-range deletion through plain `Backspace` / `Delete`, including logical newline-spanning selections with RichEdit-aware attached hidden-bridge selection mapping and a trailing edge that may land on either the logical endpoint or the CRLF boundary just before it, plus direct logical and wrapped collapsed-caret interior deletion through plain `Backspace` / `Delete` with logical newline-boundary merge coverage, wrapped single-character coverage, and RichEdit-aware attached newline-boundary bridge mapping together with attached logical and wrapped hidden-bridge synchronization for those deletion paths plus direct logical and wrapped `Backspace`-at-start and `Delete`-at-end no-op handling together with attached logical and wrapped hidden-bridge collapsed-caret no-op coverage for those boundary paths, word-navigation/delete parity, caret blinking/reset behavior, bounded clipboard-open retries in the shared host clipboard path, themed combo popup scrollbars, the hidden RichEdit-backed text-input bridge with attached-window undo/redo coverage, native paste coverage, masked bridge clipboard suppression, IME-composition-owned special-key routing, explicit IME result-commit routing before `WM_IME_ENDCOMPOSITION`, client-anchored hidden helper rect tracking, explicit IME composition/candidate-window anchoring to the live hidden-bridge caret including focused-control moves, viewport-sized hidden multiline bridge behavior, multiline plain-text `TextField` bridge normalization plus attached-window undo/redo coverage, bridge-import invalidation plus no-post-callback-touch safety for tree-rebuilding text-change notifications, DX-side multiline pointer caret/selection behavior, wrapped visual-line `Up` / `Down` plus `Shift+Up` / `Shift+Down` selection sync plus `Home` / `End` navigation, viewport-based multiline `PageUp` / `PageDown` plus `Shift+Home` / `Shift+End` plus `Ctrl+Shift+Home` / `Ctrl+Shift+End` plus `Shift+PageUp` / `Shift+PageDown` behavior, multiline `Ctrl+Backspace` / `Ctrl+Delete` word-delete parity with hidden-bridge sync, including long wrapped paragraphs with no explicit newlines, multiline mouse-wheel viewport scrolling with accumulated high-resolution wheel deltas plus focused hidden-bridge viewport sync, wrapped multiline viewport counts driven by DWrite line metrics plus DWrite-derived fallback line-height estimates instead of newline counting or fixed `20.0f` guesses, explicit multiline IME-composition special-key coverage, observer-slot revalidation for host-owned default/cancel/mnemonic targets, and cross-pilot no-visible-child validation are in; remaining multiline polish and final control-polish work are still pending.
- [ ] Phase 3. Shared `DxGrid` control. In progress: the virtualized grid, custom scrollbars, busy renderers, null/empty-model safety hardening, visible-work instrumentation, and attached-host post-scroll bounded-work regressions for large plain, grouped, and checkable grids are in, attached popup-scrolling stability coverage is now in too, sustained repeated-scroll attached-host coverage now extends the large plain, grouped, and checkable grid surfaces plus the combo popup surface, and the widened real-window/app-window sustained-scroll matrix now covers `FindFilesWindow`, `FileOperationsIssuesPane`, `ConnectionManagerDialog`, `ShortcutsWindow`, `ViewerSqlite`, `Preferences.Plugins` main-list and custom-paths grids, `Preferences.Keyboard`, `Preferences.Viewers`, and `Preferences.Themes`; remaining work is broader app-replacement breadth rather than missing large-list baseline or live-window gate coverage.
- [ ] Phase 4. Theme, rainbow mode, lightweight animation, focus, tooltips, and popup layers. In progress: palette derivation, viewer-theme-derived control chrome, rainbow styling including focused/hovered shared-tree row tinting, popup/tooltip layers, lightweight animation, input-modality-aware focus visuals, explicit combo taxonomy, Preferences-aligned button chrome, muted primary/default button chrome, stronger shared hover feedback, Preferences-style switch chrome including compact switch-only geometry for empty-text hosts plus explicit checked/unchecked state-label support, standard checkbox chrome, shared-tree focus fallback parity for keyboard-visible focus versus high-contrast focused selection, shared DXUI localization/diagnostics hardening, reduced-motion-aware static fallback for non-essential grid animation, reduced-motion-aware shared-grid sort-glyph transitions, hot shared-grid plus shared-tree scrollbar hover/drag feedback, viewport-aware shared tooltip measurement plus wrapped text layout and edge-clamped placement, tracking-tooltip same-text no-op reuse plus same-cell pointer-follow updates, shared tree explicit-tooltip plus clipped-label fallback with pointer tracking, the shared grid’s clipped sticky-header/body paint plus clipped partial-column alignment with rainbow-aware selected-row tinting, and live Find results-grid, File Operations issues-grid, plus `ViewerSqlite` results-grid selected-row theme-cycle legibility coverage across dark/light/rainbow/high-contrast are in; broader theme/motion polish is still pending.
- [x] Phase 5. Pilot full-window rewrites: SQLite, Find, Issues/Errors. Completed for the validated pilot scope: all three pilot windows are now on the shared DX host/control path with explicit no-visible-child fallback validation. `ViewerSqlite` now has real-window `WM_GETOBJECT`/clean-close/unload coverage plus visible DX combo/edit/button/grid/data-row UIA descendants, repeated row-selection churn, sustained bounded scrolling, and repeated open/close churn in `Debug|x64`, `ASan Debug|x64`, and `Release|x64`; `FindFilesWindow` now has directory activation, `Open Parent`, default-button Enter, popup `Esc`, access keys, `Tab` traversal, typeahead/command-enable validation, live form/result UIA coverage, theme-cycle legibility, sustained bounded scrolling, and repeated open/close churn in `Debug|x64` and `ASan Debug|x64`; and `FileOperationsIssuesPane` now has repeated selected-row name tracking, theme-cycle legibility, sustained bounded scrolling, and repeated open/close churn in `Debug|x64` and `ASan Debug|x64`.
- [ ] Phase 6. Advanced parity and broader comctl replacement. In progress: the shared DX tree/navigation control now covers pointer and keyboard context menus plus optional icons and badges, the Preferences dialog category surface plus the re-landed one-host shell header/description/footer chrome plus DX page-host background/card surface plus the re-landed shared-page-host `Preferences.General` card surface plus the re-landed shared-page-host `Preferences.Viewers` full page surface plus the re-landed shared-page-host `Preferences.Panes` full page surface plus the re-landed shared-page-host `Preferences.HotPaths` full page surface plus the re-landed one-host `Preferences.Advanced` full page surface plus the re-landed one-host `Preferences.Themes` full page surface plus the re-landed one-host `Preferences.Keyboard` full page surface plus the re-landed one-host `Preferences.CompareDirectories` full page surface plus the re-landed one-host `Preferences.Editors` and `Preferences.Mouse` note surfaces plus the re-landed full `Preferences.Plugins` list-mode plus nested schema-editor surface plus the re-landed full `ConnectionManagerDialog` visible shell/list plus editor-form surface plus the re-landed full `ManagePluginsDialog` visible schema-driven modal surface plus the re-landed full visible `CompareDirectoriesWindow.Options` body plus footer command row are still in, along with explicit wrapper-host `SS_NOTIFY` and one-step click/arrow acceptance coverage plus batched initial-open and category/page redraw so selection, page title, visible page content, and newly created DX child hosts switch atomically without staged DX-host reveal or visible `ResizeBuffers(...)` failure spam. Child-host invalidation plus swap-chain work is now gated on effective visibility, initial Preferences open now creates only the active page container, destroy-on-leave now covers `Preferences.Editors`, `Preferences.Mouse`, `Preferences.General`, `Preferences.Panes`, `Preferences.Advanced`, `Preferences.HotPaths`, `Preferences.Themes`, `Preferences.Keyboard`, `Preferences.Viewers`, `Preferences.CompareDirectories`, and `Preferences.Plugins`, and the full current Preferences direct-host reset command-selftest sweep is now green in both `Debug|x64` and `ASan Debug|x64`. The shared page host now owns one retained DX content-root panel for the active page subtree across that full current page set, so category switches clear and rebuild one shared retained subtree instead of fanning out per-page DX child-host HWNDs. `Preferences.Viewers`, `Preferences.Panes`, `Preferences.HotPaths`, `Preferences.Keyboard`, and `Preferences.Plugins` now also attach through the shared `PrefsDxHost` path, page-height measurement now includes the active direct-host DX page root itself, the Viewers DX mappings-grid selection now resolves from retained selected-extension state instead of hidden legacy listview selection during refresh, Themes selected theme identity and selected color-row identity now resolve from retained state instead of hidden legacy combo or list selection, the Plugins DX main-grid selection now drives details-mode refresh directly instead of relying only on hidden legacy listview selection mirrors, the nested `Preferences.Plugins` schema editor no longer fans out per-field DX child-host HWNDs because its visible labels, descriptions, edits, buttons, combos, toggles, and checkbox choices now attach directly under one retained shared config panel on the page-host content root, `Preferences.Advanced` no longer builds hidden Win32 static/toggle backing controls on the direct-host DX path because only its temporary combo/edit bridges remain, `Preferences.Themes` no longer creates hidden note, label, search, theme/base combo, command-button, swatch, or name/key/color edit backers on the active direct-host DX path, and `Preferences.Plugins` no longer keeps hidden Win32 details-mode ID/error statics, hidden search bridges, hidden list-mode command-button bridges, or hidden listview bridges on the active direct-host DX path. Preferences page switches stay scoped to shell/page redraw without dialog-wide erase or forced tree repaint, and the current migration target is zero visible comctl across migrated surfaces, so any still-visible native remainder is tracked as deferred removal work rather than an accepted steady state. The first stabilization-era single-host refactor discovery is now frozen: opaque DX child hosts must own their visible card chrome instead of relying on GDI-painted cards behind them. Another reset-architecture discovery is now explicit too: per-page destroy-on-leave binding tables must exactly match the owned HWND inventory, because ASan caught an overcounted `Preferences.Panes` binding table that turned category churn into invalid `DestroyWindow(...)` calls. A second retained-root teardown discovery is now explicit too: pages that own retained DX grids must clear the grid model before shared-root `ClearChildren()` destroys the retained controls, because ASan caught `Preferences.Viewers` detaching its mappings grid through a dangling pointer after content-root teardown. The Plugins path work that exposed sender-aware shared-grid delegates, reentrant DX-to-legacy list-selection sync hardening, safe model rebuild/clear handling, dead-HWND release during nested editor teardown, and now stale per-field config-host removal remains landed as shared infrastructure, and the shared grid now covers interactive checkbox cells, dedicated checkbox/state-image columns, explicit infotip hooks, optional icon/badge cells, collapsible grouped rows with selection rehome, stable-id keyed grouped-state capture-restore, interactive header drag-reorder, persisted column order/width capture-restore, and degenerate tiny-scrollbar-track paint safety so embedded grids no longer assert in `std::clamp(...)` when the available scrollbar track is smaller than the minimum thumb size. Preferences shell/tree acceptance is now explicitly closed for the validated direct-host scope too: the real shell host exposes header/description/footer UI Automation descendants, the shared DX category tree exposes `SelectionPattern` plus wheel scrolling without selection/title churn, and the broader direct-host matrix no longer carries open page-model/subtree-collapse debt.
- [ ] Phase 7. Tests, performance gates, docs, and cleanup. In progress: deterministic tests plus build/test validation are in, including control visual-state coverage, host-level default/cancel routing coverage, the first shared UI Automation slice for root/button/toggle/text/focus plus tree/grid metadata provider behavior, visible tree-item `SelectionItem` plus `ExpandCollapse` pattern coverage in `DxUiTests`, tree/grid control `Selection` pattern coverage in `DxUiTests`, visible grid-row fragment plus `SelectionItem` pattern coverage in `DxUiTests`, visible grid-cell readonly value/toggle/range semantics in `DxUiTests`, provider-specific root runtime-id coverage in `DxUiTests`, live migrated-window `WM_GETOBJECT` coverage in command self-tests, live page-level UIA pattern checks on the re-landed `Preferences.General`, `Preferences.Viewers`, `Preferences.Panes`, `Preferences.HotPaths`, `Preferences.Advanced`, `Preferences.Themes`, `Preferences.Keyboard`, `Preferences.CompareDirectories`, and `Preferences.Plugins` surfaces, live `SelectionPattern` plus selected-row name-tracking checks on the re-landed `Preferences.Plugins` main-list and custom-paths grids plus the `Preferences.Viewers`, `Preferences.Keyboard`, and `Preferences.Themes` DX grids, live visible-provider acceptance on the note-only `Preferences.Editors` and `Preferences.Mouse` DX surfaces, live dialog-input UIA pattern checks on re-landed `ConnectionManagerDialog`, `ManagePluginsDialog`, and `ConnectionCredentialPromptDialog` surfaces, with `ConnectionManagerDialog` now also preserving list `SelectionPattern` plus edit `ValuePattern` after long-run list scrolling and repeated open/close churn, live one-host body-surface UIA value/toggle checks on `CompareDirectoriesWindow.Options`, live `FindFilesWindow` form value/toggle plus results-grid `SelectionPattern` checks across repeated real result-selection churn plus selected-row theme-cycle legibility coverage in dark/light/rainbow/high-contrast, live `FileOperationsIssuesPane` grid `SelectionPattern` checks across repeated real task-selection churn plus selected-row theme-cycle legibility coverage in dark/light/rainbow/high-contrast, live `ShortcutsWindow` search-value plus repeated selected-row name-tracking checks across real row-selection churn, rainbow selected-row tinting, clipped partial-column geometry, sticky-header/body separation, and now preserved `SelectionPattern` plus preserved search-field `ValuePattern` after long-run grouped-grid scrolling in both `Debug|x64` and `ASan Debug|x64`, attached-host post-scroll bounded-work regression coverage for large plain, grouped, and checkable grids, sustained repeated-scroll attached-host coverage for the large plain, grouped, and checkable grids plus the combo popup, attached popup-scrolling stability coverage, attached-host DPI-change handling coverage, attached simulated device-loss recovery coverage, the real-window sustained-scroll bounded-work gates for `FindFilesWindow`, `FileOperationsIssuesPane`, `ConnectionManagerDialog`, `ManagePluginsDialog`'s scrollable DX schema-form panel, `ShortcutsWindow`, `ViewerSqlite`, `FolderWindow.ItemProperties`, plus the `Preferences.Plugins` main-list and custom-paths DX list surfaces, `Preferences.Keyboard`, `Preferences.Viewers`, and `Preferences.Themes`, guarded Release PoC validation for `DxUiTests` / `ViewerSqliteTests` so debug-only counters do not leak into Release builds, Release-safe `ViewerSqlite` real-window validation for provider exposure through `WM_GETOBJECT` plus clean close/unload, clean sequential `Debug|x64` and `ASan Debug|x64` `ViewerSqlite` validation for visible DX combo/edit/button/grid/data-row descendants, visible `ValuePattern`, visible grid `SelectionPattern`, repeated row-selection churn, sustained bounded scrolling, and selected-row theme-cycle legibility coverage in dark/light/rainbow/high-contrast, stable real-window viewer combo-host UIA validation in `ViewerPETests` for `ViewerPE`, `ViewerWeb`, `ViewerImgRaw`, and `ViewerText` in `Debug|x64`, `ASan Debug|x64`, and `Release|x64`, and a broader `Debug|x64` plus `ASan Debug|x64` app-window self-test matrix covering `ConnectionManagerDialog`, `ManagePluginsDialog`, `ConnectionCredentialPromptDialog`, `FindFilesWindow`, `FileOperations.IssuesPane`, the re-landed Preferences page set, `CompareDirectoriesWindow.Options`, the Preferences category tree/page-host repeated rapid-switch stability path, the retained Preferences page-scroll round-trip path, repeated Connection Manager row-switch stability, repeated same-row Connection Manager protocol churn (`FTP -> S3 -> FTP -> SFTP -> FTP`) with live UI Automation input-pattern availability after each settle, repeated long-run `ConnectionManagerDialog` list scrolling with preserved `SelectionPattern` and preserved edit `ValuePattern`, repeated long-run `ConnectionManagerDialog` open/close churn with preserved list `SelectionPattern`, preserved edit `ValuePattern`, and zero visible legacy list/form chrome on reopen, repeated long-run `ManagePluginsDialog` scroll-panel wheel scrolling with stable DX command/form host counts and preserved UIA input patterns, repeated long-run `ManagePluginsDialog` open/close churn across both OK and Cancel flows with zero visible legacy command/form chrome on reopen, repeated long-run `ConnectionCredentialPromptDialog` modal churn across both user+password accept and secret-only cancel flows, repeated long-run `FolderView` pane-filter prompt churn across both enabled / disabled states and both OK / Escape flows, repeated long-run `FolderView` selection-mask prompt churn across both `Select` / `Unselect` captions and both OK / Escape flows, repeated long-run `FolderView` rename prompt churn across both confirm and cancel flows, repeated long-run `FolderView` change-case prompt churn across both confirm and cancel flows, repeated long-run `FolderWindow.ItemProperties` read-only surface scrolling with stable DX host counts and preserved read-only `ValuePattern`, repeated long-run `Preferences.Plugins` main-list scrolling with stable bounded visible work and no category-tree repaint churn, repeated long-run `Preferences.Plugins` custom-paths list scrolling with stable bounded visible work and no category-tree repaint churn, repeated long-run `Preferences.Keyboard` shortcuts-grid scrolling with stable bounded visible work and no category-tree repaint churn, repeated long-run `Preferences.Viewers` mappings-grid scrolling with stable bounded visible work and no category-tree repaint churn, repeated long-run `Preferences.Themes` colors-grid scrolling with stable bounded visible work and no category-tree repaint churn, repeated long-run `ShortcutsWindow` grouped-grid scrolling with stable bounded visible work, preserved `SelectionPattern`, preserved search-field `ValuePattern`, and no DX host resize churn, repeated long-run `AlertOverlayWindow` prompt open/close churn on the real host-services path, and owned DX app-shell/modal coverage for pane-filter, selection-mask, `About`, fatal-error, splash, rename, change-case, create-directory, the file-operations custom speed-limit prompt, `FolderWindow.ItemProperties`, and the shared `AlertOverlayWindow` prompt surface, including explicit live editable `ValuePattern` checks on pane-filter, selection-mask, rename, create-directory, and custom-speed-limit prompts, explicit live toggle-state checks on the pane-filter enable control and the change-case prompt, explicit read-only value/message checks on fatal-error and item-properties surfaces, visible text-surface checks on About and splash, the stronger Shortcuts search/value plus selected-row `DataItem` semantics, repeated Shortcuts row-selection name tracking, and current-process-only prompt HWND lookup so overlapping automation instances cannot cross-target DX owned prompts. The current `AlertOverlayWindow` prompt regressions now explicitly prove both one-shot Enter/Escape behavior and repeated open/close churn with zero visible child fallback on the DX alert-overlay child surface under the main window. The current credential prompt regressions now also explicitly prove repeated open/close churn across both accept and cancel flows on the real DX modal surface. The current Connection Manager window regressions now also explicitly prove repeated open/close churn on the real owned DX list/form shell while preserving visible list `SelectionPattern`, editable form `ValuePattern`, and zero visible legacy list/form chrome on reopen. The current Manage Plugins dialog regressions now also explicitly prove repeated open/close churn across both OK and Cancel flows on the real owned DX schema-form shell while preserving visible UI Automation input/button descendants and zero visible legacy command/form chrome on reopen. The current pane-filter prompt regressions now also explicitly prove repeated open/close churn across both enabled / disabled states and both OK / Escape flows on the real DX prompt surface. The current selection-mask prompt regressions now also explicitly prove repeated open/close churn across both `Select` / `Unselect` captions and both OK / Escape flows on the real DX prompt surface. The current rename prompt regressions now also explicitly prove repeated open/close churn across both confirm and cancel flows on the real DX prompt surface while preserving the expected on-disk item state after each close. The current change-case prompt regressions now also explicitly prove repeated open/close churn across both confirm and cancel flows on the real DX prompt surface while preserving the expected on-disk item state after each close. The full current Preferences direct-host reset command-selftest sweep is now green in both `Debug|x64` and `ASan Debug|x64`, and the page-level Preferences UIA assertions now intentionally bind to the active page surface instead of the outer dialog HWND so shell/tree/footer descendants do not pollute page-local checks. Non-debug app builds now reject `--selftest*` / `--commands-selftest` arguments up front with a nonzero exit instead of opening the normal UI and hanging automation callers. One validation harness note is now explicit too: duplicate parallel `DxUiTests.exe` runs can trip unrelated bridge/default-button assertions, so the authoritative UIA/bridge validation remains the clean sequential `Debug|x64` plus `ASan Debug|x64` runs. The `ViewerSqliteTests` harness note is now explicit too: clean sequential `Debug|x64`, `ASan Debug|x64`, and `Release|x64` runs are authoritative, because overlapping viewer-window runs can interfere with plugin-backed UIA and bounded-scroll probes. The viewer combo-host harness note is now explicit too: `ViewerPETests` may wait briefly and retry once for `WM_GETOBJECT`/subtree readiness on an embedded viewer combo host, but the authoritative validation remains the clean sequential `Debug|x64`, `ASan Debug|x64`, and `Release|x64` runs. The active helper-retirement slice now also covers the live `ConnectionManagerDialog` path, the live `CompareDirectoriesWindow` scan-progress spinner path, the live shared viewer combo-host path, the live `Ui::AlertOverlayWindow` parent/anchor tracking path, the live `NavigationView` path-edit and full-path-popup edit hosts, the live `FolderWindow` pane status-bar replacement plus change-case prompt toggle hosts, the shared owner-drawn themed button hover hook, the shared themed listview-header repaint hook, the shared modern-combo popup-list hook, and the shared hidden `DxUi` text-input bridge. `ConnectionManagerDialog`'s DX command-button hosts, DX static hosts, DX interactive form hosts, and DX list host plus the `CompareDirectoriesWindow` scan-progress spinner host plus the `ViewerPE`, `ViewerWeb`, `ViewerImgRaw`, and `ViewerText` file-combo hosts plus the shared prompt-surface parent/anchor hooks plus the shared address-edit hooks plus the owned pane status-bar child window plus the owner-drawn change-case prompt toggle hooks plus the shared owner-drawn button hot-tracking hook plus the shared themed listview-header repaint hook plus the shared combo-popup list interaction hook plus the shared hidden text-input bridge now use owned window procedures instead of `SetWindowSubclass` / `DefSubclassProc`, and `Tools/Verify-NoSubclassManager.ps1` now keeps that repo-wide invariant green under `Common/`, `Plugins/`, and `RedSalamander/` on every CI push and pull request. The visible-native audit is now green too: `Tools/Audit-VisibleNativeSurfaces.ps1` reports no remaining non-report visible native exceptions after the pane status-bar retirement, and a pane status-bar sort-click regression now locks the replacement behavior. Higher-level integration coverage, broader app-window accessibility acceptance beyond that currently proven window set, and any future editable-cell semantics are still pending.

## Current Focus Checklist

- [x] Finish the first stabilization cleanup slice for `DxUi::WindowHost`: shared graphics-device resources, visible-only swap-chain allocation, deferred resize-failure recovery, and DXGI present/device-loss hardening.
- [x] Collapse the currently live high-fanout migrated pages and dialogs by rollback so the cleanup milestone ends on the narrow stable DX state instead of on broken per-control child-host fanout.
- [x] Pause new runtime sweeps until the stabilization checklist above reaches a coherent cleanup milestone.
- [x] Resume targeted builds, tests, and manual smoke validation against the narrowed cleaned-up DX state before re-landing more surfaces. `RedSalamander` now builds in `Debug|x64`, `ASan Debug|x64`, and `Release|x64`, `DxUiTests.exe` now passes in all three configs, `ViewerSqliteTests.exe` now passes in all three configs including the real plugin-backed viewer-window smoke on the DLL-unload path, and narrowed Preferences DX shell self-tests now pass on the live category-tree plus page-host surface in both debug configs.
- [x] Deduplicate the mixed legacy/DX dialog-shell palette helpers used by `ConnectionManagerDialog`, `ManagePluginsDialog`, and `CompareDirectoriesWindow.Options` so later theme changes do not drift across those shells.
- [x] Switch `CompareDirectoriesWindow.Options` from a mixed DX body plus legacy footer split to a zero-visible-native visible surface by moving the `OK` / `Cancel` command row onto shared DX buttons and keeping the legacy footer buttons hidden.
- [x] Harden `DxUi::WindowHost` accessibility registration and provider lifetime by replacing raw `WindowHost*` window properties with retained per-HWND accessibility targets.
- [x] Harden embedded viewer and dialog child-host teardown so `WM_NCDESTROY` always clears DX host bookkeeping and HWND ownership even when message forwarding leaves the message unconsumed.
- [x] Move shared keyboard/accessibility-visible shortcut strings such as the conflict mark and unassigned label onto resources instead of hardcoded literals.
- [x] Keep the plugin-backed SQLite viewer debug async counter lifetime-safe across asynchronous completion and close/unload paths.
- [x] Keep SQLite journal replay correct and performant when a persisted volume row is missing by batching delete statements and reseeding from the current in-memory snapshot instead of creating an empty volume and replaying only the delta.
- [x] Start the first safe one-host re-land after cleanup validation by moving the Preferences shell header/description/footer chrome onto one shared shell host so later page re-landings have a stable shell target.
- [x] Start the first page-owned one-host re-land after the stabilized shell, beginning with the `Preferences.General` toggle-card surface.
- [x] Freeze the pilot acceptance matrix and deferred-window inventory so the remaining migration queue is explicit.
- [x] Finish splitting `Common/DxUi/DxUi.cpp` into solution-style translation units so the shared DX layer follows the rest of the solution’s file naming and review boundaries.
- [ ] Finish the remaining text-input bridge parity sweep: the remaining richer multiline polish after the client-anchored hidden helper rect tracking, explicit IME composition/candidate-window anchoring, hidden multiline helper viewport sizing, IME-composition-owned special-key routing, IME result-commit routing work, and the landed multiline clipboard parity for classic shortcuts plus keyboard `Ctrl+A` / `Ctrl+C` / `Ctrl+X` / `Ctrl+V` routes plus bounded clipboard-open retry hardening.
- [x] Land the first shared DX tree/navigation foundation with selection, expand/collapse, keyboard navigation, typeahead, ensure-visible behavior, and themed vertical scrollbars.
- [x] Land the first interactive shared-grid checkbox semantics: pointer hit-target toggles and `Space` toggles for the active checkbox column or the first enabled checkbox cell in the current row.
- [ ] Complete the remaining shared-control polish: keyboard-focus parity and broader theme/motion legibility validation.
- [x] Land the first shared UI Automation slice on `DxUi::WindowHost`: attached hosts now answer `WM_GETOBJECT`, `DxUiTests` validate root/button/toggle/labeled-text-field/focus plus tree/grid metadata provider behavior together with tree/grid control `Selection` patterns, visible tree-item `SelectionItem` plus `ExpandCollapse` patterns, and visible grid-row `SelectionItem` semantics, and live command self-tests validate visible `WM_GETOBJECT` coverage on the Preferences category host and migrated dialog DX hosts in `Debug|x64` and `ASan Debug|x64`.
- [x] Extend live Preferences accessibility acceptance beyond provider presence so the category-tree host now proves `SelectionPattern`, a selected visible tree item, `SelectionItemPattern`, and selected-name tracking against the active category in both `Debug|x64` and `ASan Debug|x64`.
- [x] Extend the live Preferences page-host acceptance path from one-off navigation to repeated rapid category switches and repeated scroll/reset cycles so the category tree stays repaint-stable, active pages stay on one rendered DX host, and scroll state resets cleanly across repeated churn in both `Debug|x64` and `ASan Debug|x64`.
- [x] Keep routed Preferences mouse-wheel delivery non-recursive across the dialog, shared page host, and DX category tree. The live regression now sends a real `WM_MOUSEWHEEL` through the dialog-level routing path and proves the active page scrolls without category churn or wheel-forwarding recursion in both `Debug|x64` and `ASan Debug|x64`.
- [x] Remove `SetWindowSubclass` / `DefSubclassProc` from the live Preferences dialog wheel-routing path, shared page host, shared shell host, and DX category host. Those surfaces now use owned `GWLP_WNDPROC` chaining, and the routed-wheel regression remains green in both `Debug|x64` and `ASan Debug|x64`.
- [x] Remove `SetWindowSubclass` / `DefSubclassProc` from the remaining Preferences-owned helper wrappers in `Preferences.Internal.cpp`, including owned DX host wrappers, pane forwarders, and centered-edit wrappers.
- [x] Remove the last remaining comctl subclass-manager usage on the live Preferences path by converting the shared `ThemedInputFrames` wrappers and the retained `Preferences.Keyboard` list capture hook to owned `GWLP_WNDPROC` chaining, and delete the stale dead `Preferences.Plugins` host-subclass removal path. The full `cmd_preferences_dialog_` sweep remains green in both `Debug|x64` and `ASan Debug|x64`, and there are now no `SetWindowSubclass` / `DefSubclassProc` / `RemoveWindowSubclass` references left under `Preferences*.cpp`.
- [x] Fix shared page-host scrolling so direct-host Preferences pages move retained DX content and bridge HWND children together, instead of scrolling only the hidden/bridge child HWND layer and leaving cards, labels, toggles, grids, and buttons behind on stale absolute bounds.
- [x] Fix shared page-host input dispatch so live pointer, click, and wheel messages are offered to the attached `DxUi::WindowHost` before shell-level page-scroll fallback. This closes the post-scroll hit-test drift on direct-host pages and keeps inner DX controls such as toggles and grids interactive.
- [x] Fix direct-host page extent/layout accounting so Preferences page-height measurement includes the retained DX page subtree itself and the `Preferences.Viewers` DX grid rebinding tracks viewport width changes. The validated result is that scrollbars appear only when needed, scrollable pages regain their vertical range, and the Viewers grid no longer paints with stale half-width columns after relayout.
- [x] Fix shared-host Preferences shutdown/resource teardown so dialog close explicitly detaches the page host, shell host, and DX category host before destroying dialog state, and `DxUi::WindowHost` clears its active D2D target before releasing device-bound resources. This closes the D2D debug-layer leak/break path that appeared at process exit after closing Preferences.
- [x] Extend live migrated-window accessibility acceptance beyond generic input-pattern counts so the re-landed `ConnectionManagerDialog` DX list now proves `SelectionPattern`, a selected visible `DataItem`, `SelectionItemPattern`, selected-row name tracking, matching visible DX edit `ValuePattern` tracking after a real DX row click, and stable DX section-header collapse across live protocol switches in both `Debug|x64` and `ASan Debug|x64`.
- [x] Move the active `ManagePluginsDialog` plugin-configuration path off the comctl subclass manager: the shared `ThemedInputFrames` framed edit/combo/toggle/button wrappers, the DX field hosts, the DX command-button hosts, and the scroll panel now all use owned `GWLP_WNDPROC` chaining. The `cmd_plugin_configuration_dialog_` command-selftest sweep remains green in both `Debug|x64` and `ASan Debug|x64` after that conversion.
- [x] Move the active `CompareDirectoriesWindow.Options` path off the comctl subclass manager too: the dialog host, body host, footer button hosts, and wheel-routing path now all use owned `GWLP_WNDPROC` chaining, and the `cmd_compare_directories_options_` command-selftest sweep remains green in both `Debug|x64` and `ASan Debug|x64` after that conversion.
- [x] Harden migrated dialog and process-exit teardown so `ManagePluginsDialog` and `CompareDirectoriesWindow.Options` proactively detach their live DX child-host trees before bulk child-window destruction, and `DxUi::ShutdownAllWindowHostsForProcessExit()` sweeps any still-attached `WindowHost` instances before COM / CRT teardown. The targeted `cmd_plugin_configuration_dialog_` and `cmd_compare_directories_options_` sweeps remain green in both `Debug|x64` and `ASan Debug|x64` after that shutdown hardening.
- [x] Extend live migrated-window accessibility acceptance to `FindFilesWindow` so the DX form exposes visible `ValuePattern` / `TogglePattern` descendants and the results grid exposes `SelectionPattern` plus a selected visible `DataItem` whose accessible name tracks the selected result in both `Debug|x64` and `ASan Debug|x64`.
- [x] Keep `FindFilesWindow` on the long-run verified matrix. The command self-tests now prove repeated open/close churn on the real owned DX search/results shell while preserving visible results-grid `SelectionPattern`, visible editable form `ValuePattern`, zero visible child fallback, and clean close in both `Debug|x64` and `ASan Debug|x64`.
- [x] Extend live migrated-window accessibility acceptance to `FileOperationsIssuesPane` so the visible DX issues grid exposes `SelectionPattern` plus a selected visible `DataItem` whose accessible name tracks the selected task id in both `Debug|x64` and `ASan Debug|x64`.
- [x] Keep `FileOperationsIssuesPane` on the long-run verified matrix. The command self-tests now prove repeated open/close churn on the real DX issues pane while preserving visible grid `SelectionPattern`, selected-row name tracking, zero visible child fallback, and clean close in both `Debug|x64` and `ASan Debug|x64`.
- [x] Keep `CompareDirectoriesWindow.Options` on the long-run verified matrix. The command self-tests now prove repeated open/close churn on the real DX options dialog while preserving visible editable/toggle UIA descendants, zero visible legacy body/footer chrome, one visible body host, and clean close in both `Debug|x64` and `ASan Debug|x64`.
- [x] Keep PoC validation green in `Release|x64` by guarding debug-only `WindowHost` and `ViewerSqlite` test hooks so `DxUiTests` and `ViewerSqliteTests` build and run without `_DEBUG`-only counters or window messages.
- [x] Keep `ViewerPETests` stable across `Debug|x64`, `ASan Debug|x64`, and `Release|x64` by waiting for embedded viewer combo hosts to expose `WM_GETOBJECT` plus a traversable UIA subtree instead of assuming accessibility is ready on the first visible tick.
- [x] Extend `ViewerSqliteTests` beyond provider-only smoke so the real plugin window now proves provider exposure plus clean close/unload in `Release|x64`, while clean sequential `Debug|x64` and `ASan Debug|x64` runs also prove the visible DX combo/edit/button/grid/data-row UIA subtree, visible `ValuePattern`, visible grid `SelectionPattern`, repeated row-selection churn, and sustained bounded scrolling.
- [x] Replace the `FolderView` rename modal with an owned DX prompt window and validate the zero-visible-fallback contract end to end. The command self-test now proves the prompt opens as an owned DX window, answers `WM_GETOBJECT`, exposes visible DX edit/button descendants plus `ValuePattern`, keeps visible child fallback at zero, performs a real rename on accept, and keeps the renamed file stable on cancel in both `Debug|x64` and `ASan Debug|x64`.
- [x] Replace the `FolderView` change-case modal with an owned DX prompt window and validate the zero-visible-fallback contract end to end. The command self-test now proves the prompt opens as an owned DX window, answers `WM_GETOBJECT`, exposes visible DX combo/toggle/button descendants plus live value/toggle patterns, keeps visible child fallback at zero, performs a real case-change on accept, keeps the file set stable on cancel, and stays stable across repeated open/close churn in both `Debug|x64` and `ASan Debug|x64`.
- [x] Replace the app `About` dialog with an owned DX window and validate the zero-visible-fallback contract end to end. The command self-tests now prove the dialog opens as an owned DX window, answers `WM_GETOBJECT`, exposes visible DX descendants plus live button `InvokePattern` and visible text descendants, keeps visible child fallback at zero, closes cleanly, and stays stable across repeated open/close churn in both `Debug|x64` and `ASan Debug|x64`; the newer `cmd_app_about_live_dx_interaction` case also proves the visible DX footer button can close the window through live UIA `InvokePattern` in `Debug|x64`.
- [x] Replace the app fatal-error dialog with an owned DX window and validate the zero-visible-fallback contract end to end. The command self-tests now prove the dialog opens as an owned DX window, answers `WM_GETOBJECT`, exposes visible DX descendants plus live button `InvokePattern` and message-surface `ValuePattern`, keeps visible child fallback at zero, closes cleanly, and stays stable across repeated open/close churn in both `Debug|x64` and `ASan Debug|x64`; the newer `cmd_app_fatal_error_live_dx_interaction` case also proves the visible DX footer button can close the window through live UIA `InvokePattern` in `Debug|x64`.
- [x] Replace the remaining native `FolderView` create-directory modal with an owned DX prompt window and validate the zero-visible-fallback contract end to end. The command self-tests now prove the prompt opens as an owned DX window, answers `WM_GETOBJECT`, exposes a live editable `ValuePattern` for the default and edited directory name, keeps visible child fallback at zero, performs the expected create/cancel behavior, and stays stable across repeated open/close churn in both `Debug|x64` and `ASan Debug|x64`.
- [x] Replace the file-operations custom speed-limit prompt with an owned DX prompt window and validate the zero-visible-fallback contract end to end. The command self-tests now prove the prompt opens as an owned DX window, answers `WM_GETOBJECT`, exposes visible DX edit/button descendants plus live editable `ValuePattern` for the initial, edited, and invalid-submit text states, keeps visible child fallback at zero, closes cleanly, and stays stable across repeated open/close churn while preserving the expected task-bandwidth state in both `Debug|x64` and `ASan Debug|x64`; the newer `cmd_pane_fileops_speedLimit_prompt_live_dx_interaction` case also proves a real UIA-driven edit mutation across both confirm and cancel flows on the owned DX prompt surface in `Debug|x64`.
- [x] Validate the shared `AlertOverlayWindow` prompt surface on the real host-services path. The command self-tests now prove the prompt opens as the DX alert-overlay child surface under the main window with zero visible child fallback, answers `WM_GETOBJECT` with a root provider, preserves both Enter-accept and Escape-cancel behavior, exposes visible DX prompt buttons with live `InvokePattern` on both the accept and cancel paths, and stays stable across repeated open/close churn in `Debug|x64` and `ASan Debug|x64`.
- [x] Replace the `FolderWindow.ItemProperties` window with an owned DX window and validate the zero-visible-fallback contract end to end. The command self-test now proves the window opens owned by the main app window, answers `WM_GETOBJECT`, exposes a visible DX read-only message surface plus live button `InvokePattern`, keeps visible child fallback at zero, closes cleanly, stays stable across repeated open/close churn, and preserves its read-only `ValuePattern` in both `Debug|x64` and `ASan Debug|x64`; the built-in file-system path no longer escapes to `SHObjectProperties(...)`, and the newer `cmd_pane_itemProperties_window_live_dx_interaction` case also proves the visible DX close button can close the owned window through live UIA `InvokePattern` in `Debug|x64`.
- [x] Finish the shared-grid large-list validation and performance gates beyond the current visible-work instrumentation. The attached-host large plain, grouped, and checkable grid post-scroll bounded-work regressions are landed, sustained repeated-scroll attached-host coverage is landed for the large plain, grouped, and checkable grids plus the combo popup, the attached popup-scrolling stability regression is landed, and the broader real-window/app-window scrolling and throughput gates now cover `FindFilesWindow`, `FileOperationsIssuesPane`, `ConnectionManagerDialog`, `ShortcutsWindow`, `ViewerSqlite`, `Preferences.Plugins` main-list and custom-paths grids, `Preferences.Keyboard`, `Preferences.Viewers`, and `Preferences.Themes`.
- [x] Extend the shared DX tree/navigation control with optional icons and badge pills so later navigation shells do not need owner-drawn Win32 adornments.
- [x] Land the first shared grid cell-adornment foundation with interactive checkbox presentation plus optional icon/badge cells.
- [x] Land the first shared grouped-row foundation with collapsible group headers, bounded visible-work math, selection rehome, and deterministic layout/input coverage.
- [x] Land the first shared persisted grouped-state foundation with stable-id keyed capture-restore on top of collapsible grouped rows.
- [x] Integrate the shared DX tree/navigation control into the existing Preferences category-navigation surface.
- [x] Re-land the Preferences title/description and footer command chrome on a stabilized shared DX shell surface after the per-control shell-host rollback used by the current cleanup. The live shell now keeps the legacy title/description/footer controls hidden on each visibility refresh instead of only at initial attachment, reserves a stable visible DX footer band for `OK` / `Cancel` / `Apply`, and the narrowed shell self-tests are green again in both `Debug|x64` and `ASan Debug|x64`.
- [x] Stabilize Preferences initial open plus category/page transitions so tree selection, page title, visible page content, and newly created DX child hosts switch under one batched redraw instead of revealing migrated child hosts one by one or logging visible-host `ResizeBuffers(...)` failures. Shared-root transitions now also reject stale page `_dxState` reuse after the shared retained root has been cleared, which closes the blank/broken revisited-page regressions on `Viewers`, `Keyboard`, `Themes`, `Hot Paths`, and `Advanced`.
- [x] Keep tree-driven Preferences pane switches on a single visible page host through plugin child navigation so shell text and pane content do not diverge or reveal white-flash intermediate states during category-tree selection.
- [x] Keep Preferences redraw-suppression batching non-allocating across nested redraw blocks so cleanup-era batched page/theme transitions do not risk `std::terminate()` from destructor allocations.
- [x] Keep Preferences nested DX child-host teardown safe after native `WM_NCDESTROY` by releasing dead child-HWND ownership before later page-clear/reset paths run. The Plugins details/configuration editor now exercises that switch-away path cleanly in both `Debug|x64` and `ASan Debug|x64`.
- [x] Hold Preferences shell scroll-host acceptance coverage that a reduced-height Viewers page can scroll without repainting the category tree and that switching back to General resets page scroll state and clears stray vertical-scroll chrome in both `Debug|x64` and `ASan Debug|x64`.
- [x] Keep the shared Preferences page host safe across retained-subtree rebuilds and wheel fallback: the host now resets stale focus/hover/capture/text-bridge observers before clearing shared retained children, excludes the viewport-sized content-root container from overflow measurement so non-overflow pages do not show phantom vertical scrollbars, and falls back to whole-page scrolling when the DX control under the pointer does not consume wheel input.
- [x] Keep the rolled-back `Preferences.General` page limited to its real three-card legacy surface by removing schema-demo/prototype-generated controls from the live dialog and asserting exact card counts while the one-host re-land is pending.
- [x] Re-land the `Preferences.Viewers` visible page surface on the stabilized shared Preferences page-host DX surface after the current rollback of the earlier multi-host slice. The live Viewers page now uses the shared page host plus retained page content root for its labels, text fields, grid, combo, hint, and command buttons, and narrowed Preferences self-tests now pass for that page in both `Debug|x64` and `ASan Debug|x64`.
- [x] Re-land the `Preferences.General` toggle-card surface on the stabilized one-host page pattern after the current rollback of the first broken template. The live page-owned toggle callbacks now re-resolve dialog state from the host HWND instead of capturing raw `PreferencesDialogState&`.
- [x] Re-land the `Preferences.Panes` visible page surface on the stabilized shared Preferences page-host DX path after the current rollback of the earlier multi-host slice. The live toggle callbacks now re-resolve dialog state from the host HWND instead of capturing raw `PreferencesDialogState&`.
- [x] Re-land the `Preferences.HotPaths` visible page surface on the stabilized shared Preferences page-host DX path after the current rollback of the earlier multi-host slice. The live page now uses the shared page host plus retained page content root for the visible slot headers, card chrome, text fields, browse buttons, and toggles, the toggle callbacks now re-resolve dialog state from the host HWND instead of capturing raw `PreferencesDialogState&`, and narrowed Preferences self-tests pass for that page in both `Debug|x64` and `ASan Debug|x64`, including the current direct-host acceptance path after the refresh-time visibility fix.
- [x] Re-land the `Preferences.Advanced` visible page surface on a stabilized DX path after the current rollback of the earlier multi-host slice. The live page now uses the shared Preferences page host for its visible section headers, card chrome, toggles, combo, and edit fields, and narrowed Preferences self-tests pass for that page in both `Debug|x64` and `ASan Debug|x64`, including the current zero-pane-host direct-host acceptance path.
- [x] Re-land the `Preferences.Themes` visible page surface on the stabilized shared Preferences page-host DX path after the current rollback of the earlier multi-host slice. The live page now uses the shared page host plus retained page content root for its visible labels, text fields, combos, command buttons, and swatch preview, and narrowed Preferences self-tests pass for that page in both `Debug|x64` and `ASan Debug|x64`, including the current direct-host bounded-scroll/zero-resize-failure acceptance path.
- [x] Re-land the `Preferences.Editors` visible note surface on the stabilized shared Preferences page-host DX path after the current rollback of the earlier placeholder-host slice.
- [x] Re-land the `Preferences.Mouse` visible note surface on the stabilized shared Preferences page-host DX path after the current rollback of the earlier placeholder-host slice.
- [x] Re-land the `Preferences.Keyboard` visible page surface on the stabilized shared Preferences page-host DX path after the current rollback of the earlier multi-host slice. The live page now uses the shared page host plus retained page content root for its visible search/scope inputs, shortcuts grid, hint text, and command buttons, keeps shortcut-capture key ownership on that shared host, and narrowed Preferences self-tests pass for that page in both `Debug|x64` and `ASan Debug|x64`, including the current direct-host/zero-resize-failure acceptance path.
- [x] Re-land the `Preferences.Plugins` visible list-mode page surface on the stabilized shared Preferences page-host DX surface after the cleanup rollback of the earlier multi-host slice. The live page now uses the shared page host plus retained page content root for the visible note/search/grid/button/custom-paths list-mode surface, and narrowed Preferences self-tests now prove the current direct-host/zero-resize-failure acceptance path in both `Debug|x64` and `ASan Debug|x64`.
- [x] Re-land the nested `Preferences.Plugins` plugin-details/configuration editor statics slice on the shared DX path as the first stabilization-era recovery step before the later full nested schema-editor re-land.
- [x] Re-land the nested `Preferences.Plugins` plugin-details/configuration editor interactive input surface on the shared DX path after the current rollback of the earlier per-field child-host slice. The full visible nested schema-editor surface is now back on the shared DX path with `Debug|x64` and `ASan Debug|x64` self-test coverage, and the input callbacks now re-resolve live dialog state from the host window instead of capturing raw dialog-state references.
- [x] Harden the landed `Preferences.Plugins` DX grid selection sync against reentrant hidden-list `LVN_ITEMCHANGED` model rebuilds, keep the shared grid safe when a click-driven selection delegate rebuilds or clears the model mid-callback, and cover the Plugins path with real DX-grid click-driven ASan-visible self-test selection instead of only a debug selection shortcut.
- [x] Re-land `CompareDirectoriesWindow.Options` on a stabilized one-host/dialog-owned DX surface after the cleanup rollback of the earlier multi-host options slice. The visible headers, cards, switches, ignore-pattern edits, and footer command row now render through shared DX hosts with zero visible legacy body or footer chrome.
- [x] Close the shared empty-text switch geometry gap that `CompareDirectoriesWindow.Options` exposed so embedded switch-only DX hosts render compact chrome instead of full-row slabs.
- [x] Land the first shared persisted grid-layout foundation with stable-id keyed column order/width capture-restore plus real-window settings restore coverage in `FindFilesWindow`.
- [ ] Complete the remaining advanced shared list/navigation parity: richer list-detail polish beyond the now-migrated `ShortcutsWindow`, and the later one-host re-landings that replace the current cleanup-era legacy rollbacks.
- [ ] Expand harness and integration coverage beyond the current deterministic tests and pilot self-tests.
- [x] Extend the live Preferences shell acceptance path so the shared DX header/description/footer surface also proves visible UI Automation descendants plus live footer-button `InvokePattern` coverage instead of only proving the host exists and the legacy shell chrome is hidden.
- [x] Keep Preferences page switches repainting the active pane subtree after redraw batching releases so returning to one-host pages such as `Preferences.General` does not reveal white first-paint flashes or leave nested DX toggles missing until a later incidental repaint.
- [x] Extend the live Preferences one-host round-trip acceptance path so both pointer-driven (`General -> Viewers -> General`) and keyboard-driven (`Keyboard -> General`) navigation must restore the General toggle-card UI Automation subtree after returning from another migrated page.
- [x] Extend the live Preferences rapid-switch acceptance path from host/render counters to page-local UI Automation signatures so switching between grid pages, note-only pages, and toggle-card pages proves the old page subtree is gone instead of only proving that some DX host rendered.
- [x] Extend the tree-driven `Preferences.Plugins` root/details/root acceptance path from pane-visibility counters to page-local UI Automation signatures so plugin child navigation must clear stale edit/combo/value descendants when returning from details mode to the DX list surface.
- [x] Extend the live Preferences rapid-switch and Plugins-root acceptance paths to exact shell-description resources so shell chrome drift is caught alongside stale pane content during tree/category navigation.
- [x] Extend the live `Preferences.CompareDirectories` acceptance path to a real round-trip (`Compare Directories -> General -> Compare Directories`) so the mixed toggle/edit/combo one-host DX surface must repaint, restore its page-local UIA subtree, and keep legacy body chrome hidden after returning.
- [x] Extend the live `Preferences.Themes` acceptance path to a real round-trip (`Themes -> General -> Themes`) so the mixed input/button/grid/swatch one-host DX surface must repaint, restore its page-local UIA subtree, and keep legacy body chrome hidden after returning.
- [x] Extend the live `Preferences.Advanced` acceptance path to a real round-trip (`Advanced -> General -> Advanced`) so the mixed toggle/edit/combo one-host DX surface must repaint, restore its page-local UIA subtree, and keep legacy body chrome hidden after returning.
- [x] Extend the live `Preferences.HotPaths` acceptance path to a real round-trip (`Hot Paths -> General -> Hot Paths`) so the mixed toggle/input/button one-host DX surface must repaint, restore its page-local UIA subtree, and keep legacy body chrome hidden after returning.
- [x] Extend the live `Preferences.Panes` acceptance path to a real round-trip (`Panes -> General -> Panes`) so the mixed toggle/edit/combo one-host DX surface must repaint, restore its page-local UIA subtree, and keep legacy body chrome hidden after returning.
- [x] Extend the live note-only `Preferences.Editors` and `Preferences.Mouse` acceptance paths to real round-trips through `General` so those DX note surfaces must repaint, restore the note-only UIA subtree, and keep stale edit/combo/toggle descendants absent after returning.
- [x] Extend the live `Preferences.Viewers` acceptance path to a real round-trip (`Viewers -> General -> Viewers`) so the mixed edit/combo/grid one-host DX surface must repaint, restore its page-local UIA subtree, and keep legacy body chrome hidden after returning.
- [x] Extend the live `Preferences.Keyboard` acceptance path to a real round-trip (`Keyboard -> General -> Keyboard`) so the mixed edit/combo/grid/button one-host DX surface must repaint, restore its page-local UIA subtree, and keep legacy body chrome hidden after returning.
- [x] Extend the live `Preferences.Plugins` list-mode acceptance path to a real round-trip (`Plugins -> General -> Plugins`) so the search/list/button one-host DX surface must repaint, restore its page-local UIA subtree, and keep legacy list-mode chrome hidden after returning.
- [x] Clear the remaining `ViewerSqliteTests` viewer-window harness exit-code bug so the post-cleanup validation sweep can treat the standalone plugin smoke test as fully green again.

## Goal

Move the targeted windows to a shared DirectX UI toolkit so the entire visible window content, not only the data grid, uses the same rendering, theming, and animation model as the rest of the app’s DirectX surfaces.

The first pilot targets remain:
- `ViewerSqlite`
- `FindFilesWindow`
- `FileOperationsIssuesPane`

## Current Implementation Snapshot

The current implementation passes have landed the shared foundation and three production pilots:

- `Specs/UI/UI_DxUiSharedGrid.md` now serves as the authoritative shared `DxUi` and `DxGrid` behavior contract; this WIP file remains the execution checklist.
- `Common/DxUi/` now provides a swap-chain-backed `WindowHost`, retained control tree, theme palette derivation, a shared `Tree`, and a shared virtualized `Grid`.
- `Common/DxUi/DxUi.WindowHost.cpp`, `Common/DxUi/DxUi.ComboBox.cpp`, `Common/DxUi/DxUi.Grid.cpp`, `Common/DxUi/DxUi.Theme.cpp`, `Common/DxUi/DxUi.Controls.cpp`, `Common/DxUi/DxUi.TextInput.cpp`, `Common/DxUi/DxUi.Tree.cpp`, and `Common/DxUi/DxUi.Internal.h` now own the extracted split slices for host/device plumbing, combo behavior, shared grid behavior, palette conversion, localized DXUI empty-state lookup, shared color helpers, shared geometry/text draw helpers, retained container behavior, hidden RichEdit-backed text-input bridge synchronization, extracted `TextField` editing/caret/selection behavior, and shared tree/navigation behavior.
- `Common/DxUi/DxUi.cpp` is now reduced to base `Control` glue and shared non-owning observer defaults instead of remaining a second behavior monolith.
- The control set currently includes `Panel`, `Label`, `Button`, `Toggle`, `Checkbox`, `ColorSwatch`, `TextField`, `ComboBox`, `StatusStrip`, `PopupLayer`, `TooltipLayer`, and `Tree`.
- `ComboBox` now supports the three shared scope-driven variants: `Window` for regular tool/dialog windows, `Modern` for Preferences-style shells, and `Edit` for editable history/search entry fields.
- Combo popups are now capped, scrollable, width-aware for longer entries, and render themed popup scrollbars for longer history lists instead of expanding linearly with item count.
- Overlay-rendered combo popups now also win hover and pointer hit-testing above overlapping sibling controls instead of only painting above them.
- Shared DX controls now support mnemonic dispatch, including label-to-target focus routing for form fields.
- `Tree` now provides the first shared DX navigation foundation with stable-item selection, expand/collapse callbacks, keyboard navigation, pointer and keyboard context menus, typeahead, double-click leaf invocation, ensure-visible behavior, themed vertical scrollbars, and optional icon/badge adornments with deterministic layout metrics.
- Shared DX controls now track keyboard-versus-pointer input modality so keyboard focus chrome is stronger than pointer focus chrome where the host owns focus-visible behavior.
- Shared DX controls now also inherit host-tracked modifier-key state so `Shift+Tab`, default/cancel routing, popup shortcuts, and keyboard context-menu invocation do not depend on ad hoc `GetKeyState(...)` calls.
- Shared DX buttons now use the flatter Preferences-style idle/hover/focus treatment, support explicit primary/default-action chrome, `Toggle` now uses the Preferences-style switch treatment, and `Checkbox` now uses a standard checkbox treatment instead of the earlier surrogate boolean-row chrome.
- `Grid` currently supports sticky headers, sort-state cycling, column resize, pointer-driven header drag-reorder, selection, explicit infotip text, tooltips, clipboard TSV copy, custom themed scrollbars, animated spinner/marquee cell renderers with reduced-motion-aware static fallback, interactive checkbox cells, dedicated checkbox/state-image columns, and optional icon/badge cell adornments with deterministic layout metrics.
- `Grid` now also supports collapsible grouped rows from model-owned group descriptors, with group headers participating in paint, hit-testing, and scroll extent math without becoming selectable rows and with selection rehoused onto the nearest visible row when collapse hides the active selection.
- `Grid` now also supports pointer-driven header drag-reorder plus stable-id keyed column order/width capture-restore, and `FindFilesWindow` persists and restores that layout through shared settings storage and a command self-test.
- `Grid` now also exposes visible-work metrics for visible rows, visible columns, visible cells, and scrollbar state so large-result regressions can be asserted without a paint profiler.
- `WindowHost` now routes the Menu key and `Shift+F10` into focused-control context-menu hooks, and `Grid` uses the same shared path for pointer and keyboard context-menu invocation.
- `FolderWindow.FileOperations.IssuesPane` now renders as a DirectX-hosted window using the shared `DxUi` grid instead of a visible report-style common control.
- `ViewerSqlite` now runs on the same `DxUi` host/control stack, including source-side table-preview sorting from grid headers, DX-hosted query/input controls, and shared default-button wiring for the Run Query action.
- `FindFilesWindow` now runs as a full DirectX-hosted search window using shared editable combos, mode dropdowns, standard checkboxes, command buttons, status strip, and the shared results grid instead of visible common controls.
- `ConnectionCredentialPromptDialog` now runs as a full DirectX-owned modal shell using shared DX labels, masked text fields, buttons, and host-level default/cancel routing instead of a native dialog template with Win32 child controls.
- The existing Preferences dialog now uses the shared DX tree/navigation host for category selection, a one-host DX shell surface for the page title/description plus OK/Cancel/Apply footer chrome, and a shared DX page-host background/card surface that owns the full current direct-host page set: `Preferences.General`, `Preferences.Viewers`, `Preferences.Panes`, `Preferences.HotPaths`, `Preferences.Advanced`, `Preferences.Themes`, `Preferences.Keyboard`, `Preferences.CompareDirectories`, `Preferences.Plugins`, `Preferences.Editors`, and `Preferences.Mouse`. Command self-test coverage now proves the legacy visible treeview is gone, the shell chrome uses one DX host without visible legacy shell controls, the real shell host exposes the expected visible UI Automation descendants, the page host attaches to a shared DX host, the category tree accepts wheel scrolling without selection/title churn, and the full direct-host page set restores as one DX page surface during click-navigation transitions. The validated direct-host Preferences scope no longer carries open subtree-collapse or shell scroll-host/accessibility debt.
- Command self-test coverage now also proves initial Preferences open creates only the active page container instead of pre-creating all pane containers, which is the first architectural reset slice toward active-page-only retained-page ownership. The next landed slices now also prove `Preferences.Editors` and `Preferences.Mouse` destroy their pane container when leaving the page and recreate only the active note pane on demand when revisited, that leaving `Preferences.General` for `Preferences.Viewers` destroys the inactive General pane container instead of keeping it alive as hidden steady-state scaffolding, and that the current settings-backed `Preferences.Panes`, `Preferences.Advanced`, and `Preferences.HotPaths` slices follow the same destroy-on-leave rule across round-trips through `General`.
- Command self-test coverage now also proves the reduced-height Preferences Viewers page can scroll without repainting the category tree and that switching back to General resets page scroll state and clears vertical-scroll chrome in both `Debug|x64` and `ASan Debug|x64`.
- Command self-test coverage now also proves routed wheel scrolling still moves the active page when the pointer is over non-scrolling DX content on the shared page host, and the current direct-host Preferences host no longer keeps stale focus/hover/capture/text-bridge observers across shared content-root rebuilds.
- Command self-test coverage now also proves the live Preferences category-tree host exposes UI Automation `SelectionPattern` plus a selected visible tree item whose accessible name tracks the active category in both `Debug|x64` and `ASan Debug|x64`.
- Command self-test coverage now also proves the live `ConnectionManagerDialog` DX list exposes UI Automation `SelectionPattern` plus a selected visible `DataItem` whose accessible name tracks the post-click connection name, that a visible DX edit descendant reports that same selected name through `ValuePattern`, and that switching the selected connection protocol from S3 back to FTP collapses the visible DX section-header set without revealing stale S3/SSH headers, in both `Debug|x64` and `ASan Debug|x64`.
- `Preferences.General` is now re-landed on the shared Preferences page-host DX surface that owns its visible card chrome, labels, and toggles through the retained page content root instead of relying on GDI-painted cards behind multiple child hosts or a page-local DX child-host HWND.
- `Preferences.CompareDirectories` is now re-landed on the shared Preferences page-host DX surface that owns the visible section headers, card chrome, toggles, combo, and edit fields through the retained page content root. Runtime self-tests now prove the page keeps visible legacy statics, toggles, combos, and edits hidden in both `Debug|x64` and `ASan Debug|x64`, and the page now stays on the shared direct-host path with zero pane-host windows while preserving the current round-trip restore and retained per-page scroll-anchor invariants.
- Real-window Preferences coverage now also proves the active `Preferences.General` shell/page flow can first discard an uncommitted visible DX toggle change through the shared shell `Cancel` action, then round-trip that visible toggle through live UIA `TogglePattern` mutation on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- Real-window Preferences coverage now also proves the shared DX shell footer can persist a live General-page toggle mutation through the visible `Apply` action while keeping the dialog open, open the real reset-confirmation prompt through the visible `Reset All to Defaults` action while the `No` path keeps the shell alive and the `Yes` path restores the live General-page toggle state to defaults without implicitly persisting it, then close through the visible `OK` action via live `InvokePattern`, and still close a reopened shell through the visible `Cancel` action via live `InvokePattern`, with zero visible legacy footer buttons.
- `Preferences.Panes` is now re-landed on the shared Preferences page-host DX surface instead of the earlier per-control host fanout, and narrowed Preferences self-tests now pass for that page in both `Debug|x64` and `ASan Debug|x64`.
- Real-window Preferences coverage now also proves the active `Preferences.Panes` shell/page flow can first discard an uncommitted visible DX edit/toggle change through the shared shell `Cancel` action, then round-trip one visible editable field through live UIA `ValuePattern` mutation and one visible toggle through live UIA `TogglePattern` mutation on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- `Preferences.HotPaths` is now re-landed on the shared Preferences page-host DX surface instead of the earlier per-control host fanout, and narrowed Preferences self-tests now pass for that page in both `Debug|x64` and `ASan Debug|x64`.
- Real-window Preferences coverage now also proves the active `Preferences.HotPaths` shell/page flow can first discard an uncommitted visible DX edit/toggle change through the shared shell `Cancel` action, then round-trip one visible editable field through live UIA `ValuePattern` mutation and one visible toggle through live UIA `TogglePattern` mutation on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.HotPaths` command row can take the canceled browse path through the visible DX `Browse...` button via live UIA `InvokePattern` without changing the visible `Path` field or shared page state, then populate a slot path through the same visible action, discard that pending browsed directory through the shared shell `Cancel` action on reopen, and finally populate the same slot path again while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Advanced` shell/page flow can first discard an uncommitted visible DX edit/toggle change through the shared shell `Cancel` action, then round-trip one visible editable field through live UIA `ValuePattern` mutation and one visible toggle through live UIA `TogglePattern` mutation on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.CompareDirectories` shell/page flow can first discard an uncommitted visible DX edit/toggle change through the shared shell `Cancel` action, then round-trip one visible editable field through live UIA `ValuePattern` mutation and one visible toggle through live UIA `TogglePattern` mutation on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Keyboard` DX page can round-trip its visible search field through live UIA `ValuePattern` mutation while preserving the live shortcuts-grid selection semantics on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Keyboard` command row can enter the live shortcut-capture state through the visible DX `Assign...` button via live UIA `InvokePattern`, and the same page-local DX button can then cancel that capture through live UIA `InvokePattern`, while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Keyboard` command row can commit a pending non-conflicting captured shortcut through the visible DX `Assign` button via live UIA `InvokePattern`, while the selected live shortcuts row updates on the shared page and the shell stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Keyboard` command row can resolve a conflicting captured shortcut through the visible DX `Replace` button via live UIA `InvokePattern`, moving that chord onto the selected live shortcuts row and clearing the replaced conflicting row back to the explicit unassigned state while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Keyboard` command row can resolve a conflicting captured shortcut through the visible DX `Swap` button via live UIA `InvokePattern`, moving that chord onto the selected live shortcuts row and moving the original selected chord onto the conflicting row while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Keyboard` command row can take the canceled browse path through the visible DX `Export...` button via live UIA `InvokePattern` without writing a file or changing the shared page state, then export the current shortcuts set through the same visible action, writing a shortcuts file that preserves the filtered selected command binding while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Keyboard` command row can take the canceled browse path through the visible DX `Import...` button via live UIA `InvokePattern` without changing the filtered selected command binding or shared page state, then import a browsed shortcuts file through the same visible action, discard that pending imported binding through the shared shell `Cancel` action on reopen, and finally import the same shortcuts file again while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Keyboard` command row can first clear a filtered live shortcuts row back to the explicit unassigned state through the visible DX `Remove` button via live UIA `InvokePattern`, then discard that pending removal through the shared shell `Cancel` action on reopen, and finally clear that same filtered row again while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Keyboard` command row can first restore the default shortcut chord on a filtered live shortcuts row through the visible DX `Reset Defaults` button via live UIA `InvokePattern`, then discard that pending reset through the shared shell `Cancel` action on reopen, and finally restore that same default shortcut chord again while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Themes` DX page can round-trip its visible search field through live UIA `ValuePattern` mutation while preserving the live themes-grid selection semantics on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Themes` command row can first clone the selected builtin theme through the visible DX `Duplicate` button via live UIA `InvokePattern`, then discard that pending duplicated-theme switch through the shared shell `Cancel` action on reopen, and finally clone the selected builtin theme again while preserving the selected live color-row editor state on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Themes` command row can first clear a selected user-theme color override through the visible DX `Clear` button via live UIA `InvokePattern`, then discard that pending clear through the shared shell `Cancel` action on reopen, and finally clear that same selected override again while the shared page keeps the selected key and refreshed visible color editor on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Themes` shell/page flow can first discard an uncommitted visible DX color edit through the shared shell `Cancel` action, then commit that edited selected-key override through the visible DX `Set` button via live UIA `InvokePattern`, flipping the selected row from inherited to overridden while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Themes` command row can push the selected theme into live preview mode through the visible DX `Apply Temporarily` button via UIA `InvokePattern`, then discard that preview-applied state through the shared shell `Cancel` action on reopen, while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Themes` command row can take the canceled browse path through the visible DX `Load From File...` button via live UIA `InvokePattern` without changing the selected builtin theme or shared page state, then import a browsed user theme through the same visible action, discard that pending imported-theme switch through the shared shell `Cancel` action on reopen, and finally import the same browsed user theme again while exposing the imported override data on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Themes` command row can take the canceled browse path through the visible DX `Save Theme...` button via live UIA `InvokePattern` without writing a file or changing the shared page state, then export the selected user theme through the same visible action, the shared shell `Cancel` action can close and reopen the shared DX shell without losing the selected user-theme state, and a reopened pass can export that same theme again while preserving the selected theme id and override data on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Themes` command row can first clear a selected user-theme color override through the visible DX `Reset Defaults` button via live UIA `InvokePattern`, then discard that pending reset through the shared shell `Cancel` action on reopen, and finally clear that same selected override again while the shared page keeps the selected key and refreshed visible color editor on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Viewers` DX page can round-trip its visible search field through live UIA `ValuePattern` mutation while preserving the live mappings-grid selection semantics on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Viewers` shell/page flow can first discard an uncommitted visible DX extension edit through the shared shell `Cancel` action, then commit that edited extension mapping through the visible DX `Add / Update` button via live UIA `InvokePattern`, updating the selected live row while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Viewers` command row can first remove a selected extension mapping through the visible DX `Remove` button via live UIA `InvokePattern`, then discard that pending removal through the shared shell `Cancel` action on reopen, and finally remove the same mapping again through the visible DX `Remove` button while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Viewers` command row can first restore the default extension mappings through the visible DX `Reset Defaults` button via live UIA `InvokePattern`, then discard that pending reset through the shared shell `Cancel` action on reopen, and finally restore those default mappings again through the visible DX `Reset Defaults` button while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Plugins` DX page can round-trip its visible search field through live UIA `ValuePattern` mutation while preserving the live main-list grid selection semantics on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Plugins` command row can first open the selected plugin details/config surface through the visible DX `Configure...` button via live UIA `InvokePattern`, then discard that pending details-mode switch through the shared shell `Cancel` action on reopen, and finally open the selected plugin details/config surface again while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Plugins` command row can invoke a selected loadable plugin through the visible DX `Test` button via live UIA `InvokePattern`, surface inline plugin-status feedback, discard that pending inline status state through the shared shell `Cancel` action on reopen, and finally invoke the same visible action again while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Plugins` command row can invoke the shared plugin test sweep through the visible DX `Test All` button via live UIA `InvokePattern`, surface inline plugin-status feedback, discard that pending inline status state through the shared shell `Cancel` action on reopen, and finally invoke the same visible action again while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Plugins` custom-paths grid explains its empty state on the real shared page surface: when no custom plugin DLL paths are configured, the visible DX grid exposes the explicit placeholder text instead of a blank list body.
- Real-window Preferences coverage now also proves the active `Preferences.Plugins` custom-paths action row can take the canceled browse path through the visible DX `Add...` button via live UIA `InvokePattern` without changing the custom-paths list or shared page state, then append a browsed custom plugin DLL path through the same visible action, discard that pending appended path through the shared shell `Cancel` action on reopen, and finally append the same browsed custom plugin DLL path again while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the active `Preferences.Plugins` custom-paths action row can first remove a selected custom plugin path through the visible DX `Remove` button via live UIA `InvokePattern`, then discard that pending removal through the shared shell `Cancel` action on reopen, and finally remove that same custom plugin path again while the shared page stays on the direct-host DX path with zero pane-host recreation or visible legacy chrome.
- Real-window Preferences coverage now also proves the note-only `Preferences.Editors` and `Preferences.Mouse` DX pages expose visible named text descendants with no lingering editable, combo, or toggle descendants on the real shared page surface without recreating a pane host or surfacing visible legacy chrome.
- `Preferences.Advanced` is now re-landed on the shared Preferences page-host DX surface that owns the visible section headers, card chrome, toggles, combo, and edit fields through the retained page content root. Runtime self-tests now prove the page keeps visible legacy statics, toggles, combos, and edits hidden in both `Debug|x64` and `ASan Debug|x64`, and the page now stays on the shared direct-host path with zero pane-host windows while preserving the current round-trip restore and retained per-page scroll-anchor invariants.
- `Preferences.Themes` is now re-landed on the shared Preferences page-host DX surface that owns the visible labels, text fields, combos, command buttons, and swatch preview through the retained page content root. Runtime self-tests now prove the page keeps visible legacy statics, edits, combos, buttons, and the legacy swatch surface hidden in both `Debug|x64` and `ASan Debug|x64`, and the page now stays on the shared direct-host path with zero pane-host windows while preserving the same bounded-scroll and one-visible-DX-surface acceptance path. The old hidden label HWND backers are gone on that direct-host path.
- `Preferences.Keyboard` is now re-landed on the shared Preferences page-host DX surface that owns the visible search/scope inputs, shortcuts grid, hint text, and command buttons. Runtime self-tests now prove the page keeps visible legacy statics, edit frames, combo chrome, list chrome, and command buttons hidden in both `Debug|x64` and `ASan Debug|x64`, and the page now satisfies the current direct-host/zero-resize-failure acceptance path while preserving capture-mode key ownership on the shared page host.
- `Preferences.Plugins` is now re-landed on the shared Preferences page-host DX surface for its visible list-mode plus nested plugin-details/configuration editor surface. The sender-aware shared-grid delegate work and reentrant selection/model-clear hardening it exposed remain landed as shared infrastructure, and the nested interactive schema-editor input callbacks now re-resolve live dialog state from the host window instead of capturing raw dialog-state references.
- The latest Preferences direct-host stabilization fixes are now landed too: shared-page initialization now keys off real per-page DX/backing readiness instead of generic child-HWND presence, previously visited pages now rebuild if the shared retained content root was cleared instead of blindly trusting stale `_dxState`, category switches now hard-reset the shared retained page surface before binding the next category, `Preferences.Viewers` clears its retained subtree on detach instead of leaving stale descendants alive for the next page, the DX footer now reserves a minimum visible band so `OK` / `Cancel` / `Apply` stay visible, state-only toggle labels now right-align beside the switch track instead of drifting as ordinary left-aligned text, and label-less switch rows now use a compact trailing slot instead of the full text-bearing switch width.
- `CompareDirectoriesWindow.Options` is now re-landed on a stabilized one-host dialog-owned DX surface. The earlier compact switch-slot/shared toggle work remains useful shared infrastructure, and the validated current contract is: one rendered visible DX body host, shared DX footer buttons, zero visible legacy body/footer chrome, zero host resize failures, plus an explicit visible DX toggle `TogglePattern` descendant and visible DX edit `ValuePattern` descendant with stable accessible names on the real dialog.
- `ConnectionManagerDialog` is now re-landed on the shared DX path for its full visible shell/list plus editor-form surface in both `Debug|x64` and `ASan Debug|x64`. Narrowed self-tests now prove the command rows, section headers, connection list, form labels, form inputs, and the three editor-form action buttons stay on the DX path with zero visible legacy shell/list or form chrome, and the main shell now also has a real-window UIA-driven interaction regression proving the visible DX Name edit mutates the selected row live, a visible DX toggle round-trips through `TogglePattern`, and the visible DX Close action closes the shell through `InvokePattern`. The earlier `ASan Debug` teardown blocker was cleared by removing raw dialog-state captures from the DX form-input callbacks and re-resolving live dialog state from the host window at callback time.
- Real-window `ConnectionManagerDialog` coverage now also proves the visible DX command row owns real list mutation: the `New...` action creates and selects a new row through live `InvokePattern`, the `Remove` action removes that same row and restores the list, and the `Close` action still closes the shell through live `InvokePattern`, all without surfacing visible legacy shell/list or form chrome.
- `ManagePluginsDialog` full visible schema-driven modal surface is now re-landed on the shared DX path in both `Debug|x64` and `ASan Debug|x64`. Narrowed self-tests now prove the dialog keeps visible legacy `OK` / `Cancel` buttons plus visible legacy form statics and inputs hidden while the DX form surface stays active, the visible DX schema form can round-trip a live edit and toggle mutation, the visible DX nested `Browse...` action can first preserve visible editable-field state on a canceled browse path and then drive a real browsed-folder update back into a visible editable field through live `InvokePattern`, the visible DX `Cancel` action can close the modal shell through live `InvokePattern` while discarding the live edit/toggle/browse mutations on reopen, and a reopened pass still closes through the visible DX `OK` action, while long-run wheel scrolling on the shared DX schema-form panel keeps the DX command/form host counts stable and preserves live UIA input patterns.
- `ShortcutsWindow` now uses an owned DX window with a DX search field plus grouped/icon-bearing DX grid instead of a visible native search edit plus owner-drawn listview, and command self-test coverage now proves ownership, `WM_GETOBJECT`, zero visible child fallback, visible DX edit/grid descendants, live editable search `ValuePattern`, real UIA-driven search mutation that refilters the grouped grid on the owned shell, grid `SelectionPattern`, clean close, retained grouped/icon semantics across DX search filtering, sustained bounded scrolling, and repeated open/close churn in `Debug|x64` and `ASan Debug|x64`.
- Retained-control animation ticks now propagate through container controls so animated descendants do not stall below the root host.
- Deterministic tests exist for sort-state transitions, virtualization math, grouped-row metrics/layout/hit-testing/collapse-rehome behavior, stable-id grouped-state capture-restore, header drag-reorder in both directions, stable-id layout roundtrip after drag-reorder capture, selection behavior, focus traversal, input-modality/default-button/cancel-button routing, form-submit behavior, shared tree navigation behavior, tree icon/badge layout metrics, animated-grid tick detection, reduced-motion animation gating, and visual-state resolution for Preferences-aligned buttons, switch-style toggles, standard checkboxes, and explicit combo variants.
- Reduced-motion-aware defaults now come from the current Windows client-area animation setting, so deterministic animation tests must pin `ThemePalette.reducedMotion` explicitly instead of assuming a machine-wide default.
- Command self-test coverage now also proves the migrated `FindFilesWindow` attaches to `DxUi::WindowHost` and exposes no visible child-control fallback.
- Command self-test coverage now also proves the migrated `FileOperationsIssuesPane` attaches to the shared DX host path and exposes no visible child-control fallback.
- Command self-test coverage now also proves the migrated `FileOperationsIssuesPane` skips no-op diff refreshes and preserves stable-row selection across changed refreshes.
- Real-window plugin coverage now also proves `ViewerSqlite` opens through the built plugin DLL, becomes visible, exposes no visible child controls, and closes cleanly.
- Real-window plugin coverage now also proves `ViewerPE` opens through the built plugin DLL, becomes visible, exposes a shared DX combo host for file selection, and no longer exposes a visible legacy `ComboBox`.
- Real-window plugin coverage now also proves `ViewerWeb` opens through the built plugin DLL, becomes visible, exposes a shared DX combo host for file selection, and no longer exposes a visible legacy `ComboBox`.
- Real-window plugin coverage now also proves `ViewerImgRaw` opens through the built plugin DLL against generated JPEG inputs, becomes visible, exposes a shared DX combo host for alternate-file selection, and no longer exposes a visible legacy `ComboBox`.
- Real-window plugin coverage now also proves `ViewerText` opens through the built plugin DLL against generated text inputs, becomes visible, exposes a shared DX combo host for alternate-file selection, and no longer exposes a visible legacy `ComboBox`.
- Real-window plugin coverage now also proves those viewer DX combo hosts use mouse-input-capable `Static` host styles and first-click `WM_MOUSEACTIVATE` behavior, so the DX dropdown opens reliably when embedded in legacy viewer shells.
- Command self-test coverage now also proves `FindFilesWindow` directory-result activation navigates into the selected directory and `Open Parent` returns to the parent folder with the directory entry focused.
- Command self-test coverage now also proves `FindFilesWindow` preserves real-window `Tab` / `Shift+Tab` traversal, selection-only combo typeahead, and idle/running/selection command-enable transitions.
- The later-wave viewer-chrome migration now also includes one shared DX file-selection combo-chrome path in `ViewerPE`, `ViewerWeb`, `ViewerImgRaw`, and `ViewerText`: all four use the same `ComboBoxVariant::Window` surface, the same `SS_NOTIFY` child-host wrapper contract, and the same viewer-theme palette plumbing, with runtime plugin coverage proving those viewers no longer expose a visible legacy `ComboBox`; `ViewerText` `Find` / `Go To` remain on the DX-owned prompt path, and prompt-specific validation now runs as dedicated fresh-process `ViewerPETests` cases outside both the shared viewer-shell combo harness and the default no-filter viewer suite, with explicit filtered prompt runs routed through the same child-process isolation plus bounded timeout and kill-on-close process-tree cleanup so a wedged prompt cannot block the outer harness or leave modal windows behind. CI now also runs the uploaded Debug build output through `DxUiTests`, `ViewerSqliteTests`, the default `ViewerPETests` suite, the explicit `ViewerText` `Find` / `Go To` prompt probes, and the report-surface audit guard.
- The later-wave native modal/app-shell removal bucket now also includes the file-operations custom speed-limit prompt and the owned DX `FolderWindow.ItemProperties` window, both with command self-test validation in `Debug|x64` and `ASan Debug|x64`.

The migration is intentionally incomplete in this pass:
- text input and combo editing now use a hidden RichEdit-backed text-input bridge with explicit undo/redo coverage, native paste coverage, masked bridge clipboard suppression, classic edit clipboard shortcut parity (`Ctrl+Insert` / `Shift+Delete` / `Shift+Insert`) plus direct logical and wrapped multiline `Ctrl+A` full-range replacement, `Ctrl+C` copy, `Ctrl+X` cut, and `Ctrl+V` paste coverage, with logical newline-spanning partial-selection copy/cut plus paste-replacement coverage plus RichEdit-aware attached hidden-bridge trailing-edge mapping for that clipboard/cut/paste path plus wrapped multiline partial-selection copy/cut plus paste-replacement coverage plus attached wrapped hidden-bridge selection alignment and post-cut/post-paste host bridge synchronization for that clipboard/cut/paste path, with no-selection copy/cut no-op behavior including logical and wrapped multiline `Ctrl+Insert`, `Ctrl+C`, `Ctrl+X`, and `Shift+Delete` no-selection coverage plus attached logical and wrapped `Ctrl+Insert` and `Ctrl+C` copy coverage with bridge-selection alignment plus attached logical newline-spanning partial-selection `Ctrl+Insert` / `Ctrl+C` copy coverage with RichEdit-aware bridge-selection mapping plus attached wrapped multiline partial-selection `Ctrl+Insert` / `Ctrl+C` copy coverage with exact bridge-selection alignment plus attached logical and wrapped `Ctrl+X` and `Shift+Delete` cut coverage with pre-cut bridge-selection alignment and post-cut host bridge synchronization plus attached logical newline-spanning partial-selection `Ctrl+X` / `Shift+Delete` cut coverage with the same RichEdit-aware bridge-selection mapping plus post-cut host bridge synchronization plus attached wrapped multiline partial-selection `Ctrl+X` / `Shift+Delete` cut coverage with exact bridge-selection alignment plus post-cut host bridge synchronization plus attached logical newline-spanning partial-selection `Shift+Insert` / `Ctrl+V` paste coverage with RichEdit-aware bridge-selection mapping plus post-paste host bridge synchronization plus attached wrapped multiline partial-selection `Shift+Insert` / `Ctrl+V` paste coverage with exact bridge-selection alignment plus post-paste host bridge synchronization plus attached logical and wrapped `Ctrl+Insert`, `Ctrl+C`, `Ctrl+X`, and `Shift+Delete` no-selection collapsed-caret no-op coverage plus attached logical newline-spanning and wrapped partial-selection native bridge `WM_CLEAR` deletion coverage plus attached logical and wrapped native bridge `WM_CLEAR` no-selection collapsed-caret no-op coverage plus attached logical and wrapped `Shift+Insert` paste coverage with post-paste host bridge synchronization plus attached logical and wrapped `Ctrl+V` paste coverage with post-paste host bridge synchronization, and the direct and attached logical multiline keyboard paste routes now explicitly normalize CRLF clipboard text into LF text state before that bridge sync, bounded clipboard-open retries in the shared host clipboard path, IME-composition-owned `Enter` / `Esc` / `Tab` routing, explicit IME result-commit routing before `WM_IME_ENDCOMPOSITION`, a regionless helper rect anchored to the visible focused DX field, explicit IME composition/candidate-window anchoring to the live hidden-bridge caret including focused-control moves, helper viewport sizing that tracks the visible focused DX field, exact no-wrap first-visible-line regression coverage for hidden multiline helper sync, RichEdit-aware CRLF selection mapping, and multiline plain-text `TextField` newline normalization plus attached-window selection/paste/undo/redo coverage; DX-side multiline pointer caret placement, drag selection, double-click word selection with exact hidden-bridge range alignment plus replace-through-bridge round-trips for both logical and wrapped multiline layout, explicit attached hidden-bridge caret sync for logical multiline pointer placement, logical multiline `Left` / `Right`, logical multiline `Up` / `Down`, logical multiline `Home` / `End`, logical multiline `PageUp` / `PageDown`, explicit attached hidden-bridge selection sync with replace-through-bridge round-trips for logical multiline drag selection, explicit attached hidden-bridge selection sync with replace-through-bridge round-trips for logical multiline `Shift+Click` extension, explicit attached hidden-bridge selection sync with replace-through-bridge round-trips for logical multiline `Shift+Left` / `Shift+Right` extension, explicit attached hidden-bridge selection sync with replace-through-bridge round-trips for logical multiline `Shift+Up` / `Shift+Down` extension, and explicit attached hidden-bridge caret sync for wrapped later-line pointer placement, wrapped `Shift+Click` selection sync with replace-through-bridge round-trips, wrapped drag selection sync with replace-through-bridge round-trips, wrapped visual-line `Shift+Left` / `Shift+Right` selection sync with replace-through-bridge round-trips, wrapped visual-line `Up` / `Down` plus `Shift+Up` / `Shift+Down` bridge selection sync with replace-through-bridge round-trips plus `Home` / `End`, logical and wrapped `Shift+PageUp` / `Shift+PageDown` selection with replace-through-bridge round-trips, wrapped `Shift+Home` / `Shift+End` selection with replace-through-bridge round-trips, wrapped `Ctrl+Home` / `Ctrl+End` collapsed document-boundary navigation, wrapped `Ctrl+Shift+Home` / `Ctrl+Shift+End` document-boundary selection with replace-through-bridge round-trips, `Ctrl+Backspace` / `Ctrl+Delete` parity with hidden-bridge sync including long wrapped paragraphs with no explicit newlines, multiline mouse-wheel viewport scrolling with accumulated high-resolution wheel deltas plus focused hidden-bridge viewport sync, DWrite line-metrics-based wrapped viewport counts, and lazy prepaint DWrite setup for text measurement are now in, but richer multiline polish is still open
- advanced parity work such as richer list-detail polish beyond the current rollback/re-land cycle is not implemented yet

## Phase 0 - Inventory, acceptance matrix, and full-window DX shell spec

- [x] Publish the authoritative shared `DxUi` / `DxGrid` UI contract under `Specs/UI/` and keep this file execution-only.
- [x] Audit every current `SysListView32` and other visible comctl-heavy report surface and classify each as `pilot`, `simple later`, or `advanced parity required`. `Specs/UI/UI_VisibleComctlAudit.md` plus `Tools/Audit-ComctlReportSurfaces.ps1` now lock the current report-surface inventory, and every remaining `SysListView32` / `WC_LISTVIEWW` hit is classified as migrated fallback or hidden-backing scaffolding rather than an accepted visible native exception.
- [x] Audit the remaining non-report visible-native surface inventory and classify the current deferred exceptions. `Specs/UI/UI_VisibleNativeAudit.md` plus `Tools/Audit-VisibleNativeSurfaces.ps1` now report an empty non-report visible-native inventory after retiring the old `FolderWindow` pane status-bar class.
- [x] Capture the full visible control surface for `ViewerSqlite`, including buttons, toggles, dropdowns, editable fields, popup lists, status strips, and result surfaces.
- [x] Capture the full visible control surface for `FindFilesWindow`, including root/name/content history combos, name/content mode dropdowns, option toggles, action buttons, status text, and the results surface.
- [x] Freeze the parity matrix for the three pilot windows: focus traversal, keyboard behavior, popup behavior, selection, sorting, resizing, copy, and theme/rainbow behavior.
- [x] Record which existing windows depend on grouped rows, persisted layout, or other remaining advanced list features so they do not migrate before the required parity lands.
- [x] Validation: this plan, the migrated code, and the remaining migration queue agree on which windows are pilots and which are deferred.

## Phase 1 - Shared `DxUi` library and swap-chain window host

- [x] Add the `Common/DxUi/` static library project and wire it into `RedSalamander`.
- [x] Wire the shared `DxUi` library into plugin consumers, not only the main `RedSalamander` executable.
- [x] Implement `DxUi::WindowHost` with D3D11 hardware-first creation, WARP fallback, DXGI swap chain, Direct2D device context rendering, and DirectWrite setup.
- [x] Implement UI-thread rendering/input dispatch, focus tracking, hover/capture state, resize handling, and device-dependent resource recreation.
- [x] Clear or invalidate host interaction state when swapping or detaching the retained root tree so control-tree replacement cannot leave dangling focus, hover, or capture pointers behind.
- [x] Constrain host observer-slot registration so focus, capture, default-button, and cancel-button pointers stay within the installed retained root tree.
- [x] Hook the host to `Ui::AnimationDispatcher` for lightweight animation ticks.
- [x] Propagate animation ticks through retained control containers so animated descendants continue ticking below the root host.
- [x] Update `Common/DxUi/DxUi.vcxproj` with shared-PDB-safe compilation flags so the split multi-file `DxUi` project still builds reliably in Debug and ASan Debug.
- [x] Add the project to `RedSalamander.sln`.
- [x] Validation: `Common/DxUi/DxUi.vcxproj` builds successfully in `Debug|x64`.

## Phase 2 - Base DirectX controls

- [x] Add retained-control primitives for panel, label, button, toggle, checkbox, status strip, popup layer, and tooltip layer.
- [x] Add v1 `TextField` and `ComboBox` controls sufficient for the shared control tree and pilot experimentation.
- [x] Add masked text-field support for secret-entry prompts and suppress clipboard copy from masked DX fields.
- [x] Add shared themed scrollbar rendering usable by scrollable controls and the grid.
- [x] Add generic `Tab` and `Shift+Tab` traversal across the retained DX control tree so full-window form navigation works.
- [x] Close DX combo popups on focus loss so keyboard focus changes do not leave stale dropdown state behind.
- [x] Add single-line Enter-submit behavior for DX text fields so pilot forms can trigger actions without visible Win32 edit controls.
- [x] Add editable-history combo support with typed text, dropdown selection sync, and Enter-submit hooks for Find-style inputs.
- [x] Ensure DX combo boxes participate in generic focus traversal and mnemonic targeting instead of being mouse-only focus targets.
- [x] Add mnemonic dispatch across retained DX controls, including button/toggle activation and label-to-target focus routing.
- [x] Cap DX combo popup height and add popup scrolling so longer history lists remain usable.
- [x] Add selection-only combo typeahead so long mode/history lists can jump by prefix without a full edit field.
- [x] Split shared DX combos into explicit `Window`, `Modern`, and `Edit` variants so migration code can choose chrome by scope instead of by ad hoc styling.
- [x] Add visual mnemonic cues for mnemonic-enabled labels, buttons, toggles, and checkboxes.
- [x] Improve DX toggle/checkbox label layout for option-heavy forms such as `FindFilesWindow`.
- [x] Add host-level input-modality tracking so keyboard-focused controls can render stronger focus chrome than pointer-focused controls.
- [x] Add immediate host invalidation on hover-target changes so hover feedback appears on controls that rely on inherited hover state.
- [x] Track modifier-key state at the host level so keyboard routing does not depend on transient `GetKeyState(...)` calls.
- [x] Add shared default-button and cancel-button routing at the host level so Enter and Esc behave like a modern dialog without overriding control-owned Enter/Esc behavior.
- [x] Update single-line text fields and editable combos so host default-button routing runs only when those controls do not currently own Enter.
- [x] Route `WM_SYSKEYDOWN` through the retained DX host and support `Alt+Down` / `Alt+Up` combo popup behavior for keyboard parity with Find-style forms.
- [x] Route the Menu key and `Shift+F10` through the retained DX host so focused controls can expose keyboard context menus without per-window message forks.
- [x] Complete the remaining keyboard focus parity polish for toggle and checkbox controls, including pointer-vs-keyboard focus handling and high-contrast fallback.
- [x] Add the hidden single-line text-input bridge for `DxUi::TextField` and editable `DxUi::ComboBox`, including host-routed Enter/Tab/Esc handling and attached-window sync coverage.
- [x] Add deterministic attached-window undo coverage for the hidden single-line text-input bridge on both `TextField` and editable `ComboBox`.
- [x] Upgrade the hidden single-line bridge to prefer a RichEdit-backed helper and add deterministic attached-window redo coverage for `TextField` and editable `ComboBox`.
- [ ] Finish the remaining bridge parity sweep for richer multiline polish.
- [x] Add multiline mouse-wheel viewport scrolling plus focused hidden-bridge viewport sync so multiline text fields stop relying only on keyboard page movement for vertical viewport control.
- [x] Add deterministic attached-window multiline IME composition coverage proving `Esc` and `Tab` stay bridge-owned until composition ends and then resume host cancel/focus routing.
- [x] Add deterministic attached-window IME anchor coverage proving the hidden bridge updates both composition and candidate forms to the live caret rect for single-line and multiline DX text controls.
- [x] Extend attached-window IME anchor coverage so composition and candidate forms keep tracking the live caret rect even when the focused DX control moves during composition.
- [x] Add editable-history popup filtering, filtered no-match empty-state handling, and keep filtered hover/keyboard navigation aligned with the visible popup rows.
- [x] Move DX combo dropdown rendering into an overlay paint pass so open popups render above later sibling controls in dense forms such as `FindFilesWindow`.
- [x] Route overlay-rendered combo popup hit-testing ahead of normal sibling hit-testing so open dropdowns block underlying hover and pointer input in dense forms such as `FindFilesWindow`.
- [x] Add combo popup viewport placement so DX dropdowns flip above when needed and clamp visible rows to the available host space in smaller Find-style windows.
- [x] Expand DX combo popups for long visible history or mode entries while clamping widened dropdowns back into the host viewport.
- [x] Add themed vertical popup scrollbars for long DX combo dropdowns, including track paging and thumb dragging for Find-style history lists.
- [x] Make repeated clicks on an already open DX combo field or drop button toggle the popup closed instead of leaving stale popup state behind.
- [x] Route combo-popup `Esc` dismissal through the shared popup-close path so open combos close before host-level cancel handling.
- [x] Ensure child `DxUi::WindowHost` surfaces embedded inside legacy shells return `MA_ACTIVATE` and use mouse-input-capable `Static` host styles where needed so first-click combo open behavior works in migrated viewer shells.
- [x] Replace fixed-width caret assumptions in DX text fields and editable combos with pointer hit-testing, horizontal caret scrolling, and `Ctrl+Backspace` word deletion.
- [x] Add `Ctrl+Left`, `Ctrl+Right`, and `Ctrl+Delete` parity plus caret blink/reset behavior for DX text fields and editable combos.
- [x] Add single-line selection parity for DX text fields and editable combos: `Shift+Arrow` / `Shift+Home` / `Shift+End`, `Ctrl+A`, `Shift+Click`, double-click word selection, mouse drag selection, and replace-on-type/delete when a selection exists.
- [x] Add real-window Find self-test coverage for `Tab` / `Shift+Tab`, selection-only combo typeahead, and idle/running/selection command-enable transitions.
- [x] Finish the remaining Find-window-specific control polish beyond the traversal/typeahead/command-state coverage that is now validated. The live migrated window now also has real command self-test coverage for focused DX action-button activation, default-button Enter routing, grid-focused Enter activation, combo-popup `Esc` dismissal, access-key focus routing, directory activation, and `Open Parent` parent-focus behavior.
- [x] Validation: the shared controls cover the visible controls needed by `ViewerSqlite`, `FindFilesWindow`, and `FileOperationsIssuesPane` without falling back to visible Win32 child controls.

## Phase 3 - Shared `DxGrid` control

- [x] Add `IDxGridModel`, `IDxGridDelegate`, `GridSelectionModel`, `GridColumnDesc`, `GridCellData`, `GridSortSpec`, and row-style hooks.
- [x] Implement virtualized row/column viewport math with sticky headers and fixed-height multiline clamp rendering.
- [x] Implement header interaction, sort-state cycling, column resize, selection, keyboard navigation, tooltip support, and TSV copy.
- [x] Route grid context menus through one shared pointer/keyboard path so right-click and keyboard invocation target the same row-level command surface.
- [x] Implement themed custom scrollbars and bounded visible-row/visible-column painting.
- [x] Add animated cell busy renderers for spinner and marquee/progress states.
- [x] Add header busy indicators for long-running grid operations.
- [x] Treat empty or cleared models as non-hit-testable and cancel active resize state safely when a model changes mid-interaction.
- [x] Add explicit visible-work instrumentation for visible rows, visible columns, visible cells, and scrollbar state so large-list regressions are measurable without a paint-time profiler.
- [x] Validation: the grid performs only visible-content work and remains suitable for large result sets. Attached-host bounded-work regressions now cover large plain, grouped, and checkable grids plus combo-popup scrolling, and the authoritative live-window/app-window sustained-scroll matrix now covers `FindFilesWindow`, `FileOperationsIssuesPane`, `ConnectionManagerDialog`, `ShortcutsWindow`, `ViewerSqlite`, `Preferences.Plugins` main-list and custom-paths grids, `Preferences.Keyboard`, `Preferences.Viewers`, and `Preferences.Themes`.

## Phase 4 - Theme, rainbow mode, motion, focus, and popup polish

- [x] Add `ThemePalette`, default palette derivation, and app/viewer-theme-derived palette conversion hooks.
- [x] Support severity styling and rainbow row tinting in the shared grid path.
- [x] Keep partially visible grid columns clipped to the live viewport, keep scrolled body content clipped below sticky headers, and preserve rainbow-selected row tinting on shared-grid surfaces such as `ShortcutsWindow`.
- [x] Support tooltip and popup layers in the shared control tree.
- [x] Render combo dropdowns and shared popup-layer content in an overlay pass so transient surfaces are not overdrawn by later siblings.
- [x] Align shared DX buttons with the flatter Preferences-dialog button chrome.
- [x] Add explicit primary/default button chrome aligned with modern Windows dialog expectations.
- [x] Tune primary/default button fill to a muted accent-tinted surface aligned more closely with Preferences dialog chrome.
- [x] Strengthen hover-visible feedback on buttons and input controls so pointer interaction state is obvious without focus changes.
- [x] Align `DxUi::Toggle` with the Preferences switch treatment as the shared switch-style boolean control.
- [x] Align `DxUi::Checkbox` with a standard checkbox treatment instead of the earlier quiet boolean-row surrogate.
- [x] Keep shared-tree focused-row chrome aligned with the keyboard-focus policy while preserving a visible high-contrast fallback when keyboard-focus visibility is off.
- [x] Keep shared-tree selected and hovered rows aligned with rainbow mode so navigation surfaces tint only when rainbow mode is active and fall back cleanly in high-contrast mode.
- [x] Codify scope-specific combo chrome so ordinary windows, Preferences-like shells, and edit combos no longer share one visual treatment.
- [x] Localize shared DXUI empty-state strings and stop mutating shared DirectWrite format singletons during draw-time helper calls.
- [x] Add targeted diagnostics and fallback handling for shared DWrite/D2D/DX resource creation failures instead of silently swallowing them.
- [x] Make reduced-motion policy explicit in `ThemePalette`, derive the default from Windows client-area animation preference, and freeze non-essential shared grid animation while keeping native text-caret behavior intact.
- [x] Add reduced-motion-aware sort glyph transitions for shared grid headers.
- [x] Add hot shared-grid scrollbar hover/drag feedback that tints track/thumb state and clears stale hot state on leave or drag release.
- [x] Add matching shared-tree scrollbar hover/drag feedback so navigation surfaces keep the same hot track/thumb behavior and leave/reset cleanup as the shared grid.
- [x] Derive viewer-backed header/button/input/scrollbar/tooltip chrome from live `ViewerTheme` colors instead of leaving shared controls on generic palette defaults.
- [ ] Expand palette derivation so all shared controls match the app’s existing DirectX visual language more closely.
- [x] Derive shared toggle knob chrome from `ThemePalette` so viewer-backed switches use live neutral/selection colors instead of fixed black/white knobs. `ThemePalette` now carries explicit neutral and checked toggle knob colors, `MakeThemePaletteFromViewerTheme(...)` derives those from the active neutral/selection palette, and `PoC/DxUiTests/DxUiTests.cpp` now covers both palette derivation and resolver usage with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_133356/`.
- [x] Route shared button and window-combo border chrome through `ThemePalette::buttonBorder` so viewer-backed palettes can steer visible edge chrome instead of leaving buttons on synthesized text/fill borders. `PoC/DxUiTests/DxUiTests.cpp` now covers viewer-theme button-border derivation plus shared button/window-combo resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_134310/`.
- [x] Route checked checkbox glyph chrome through `ThemePalette::selectionText` so viewer-backed palettes can steer the visible check mark instead of falling back to generic contrast heuristics. `PoC/DxUiTests/DxUiTests.cpp` now covers viewer-theme selection-text derivation plus shared checkbox resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_135033/`.
- [x] Route primary-button text chrome through `ThemePalette::selectionText` so viewer-backed palettes can steer accent-surface button labels instead of falling back to generic contrast heuristics. `PoC/DxUiTests/DxUiTests.cpp` now covers shared primary-button resolver usage plus a viewer-derived primary-button text case, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_135705/`.
- [x] Route accent-adornment text chrome through `ThemePalette::selectionText` so viewer-backed palettes can steer accent badge labels instead of falling back to generic contrast heuristics. `PoC/DxUiTests/DxUiTests.cpp` now covers direct accent/info/warning/error adornment resolver usage plus a viewer-derived accent/alert case, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_140303/`.
- [x] Route checked-toggle track-border chrome through `ThemePalette::selectionText` so viewer-backed palettes can steer accent-surface switch edge chrome instead of falling back to neutral text heuristics. `PoC/DxUiTests/DxUiTests.cpp` now covers both preferences-style and viewer-derived checked-toggle track-border resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_140827/`.
- [x] Route checked-checkbox indicator-border chrome through `ThemePalette::selectionText` so viewer-backed palettes can steer accent-surface checkbox edge chrome instead of falling back to neutral text heuristics. `PoC/DxUiTests/DxUiTests.cpp` now covers both default and viewer-derived checked-checkbox indicator-border resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_141422/`.
- [x] Route pressed standard-button border chrome through `ThemePalette::focusStroke` so viewer-backed palettes can steer pressed edge chrome instead of falling back to neutral text heuristics. `PoC/DxUiTests/DxUiTests.cpp` now covers both default and viewer-derived pressed-button border resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_142008/`.
- [x] Route primary-button hover and pressed fill chrome through `ThemePalette::selectionText` so viewer-backed palettes can steer accent-surface fill darkening instead of falling back to neutral text heuristics. `PoC/DxUiTests/DxUiTests.cpp` now covers both default and viewer-derived primary-button hover/pressed fill resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_142454/`.
- [x] Route modern-combo popup-open button fill chrome through `ThemePalette::buttonPressedFill` so viewer-backed palettes can steer open-popup button chrome instead of synthesizing it from hover and focus blends. `PoC/DxUiTests/DxUiTests.cpp` now covers both default and viewer-derived modern-combo popup-open button fill resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_143024/`.
- [x] Route modern-combo hovered button fill chrome through `ThemePalette::buttonHotFill` so viewer-backed palettes can steer hovered open-button chrome instead of leaving that state on the idle fill. `PoC/DxUiTests/DxUiTests.cpp` now covers both default and viewer-derived modern-combo hovered button fill resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_143542/`.
- [x] Route viewer-derived `ThemePalette::focusStroke` through `selectionText` so accent-driven focus chrome follows the same accent-surface contract as the rest of the shared control family instead of blending against neutral text. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark and light viewer-palette focus-stroke derivation, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_144128/`.
- [x] Route primary-button hover and pressed border chrome through `ThemePalette::selectionText` so viewer-backed palettes can steer accent-surface edge contrast instead of falling back to raw accent blending. `PoC/DxUiTests/DxUiTests.cpp` now covers both default and viewer-derived primary-button hover/pressed border resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_144804/`.
- [x] Route viewer-derived `ThemePalette::buttonHotFill` and `ThemePalette::buttonPressedFill` through `focusStroke` so neutral button hover and pressed fills follow the shared focus-chrome contract instead of blending directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark/light viewer-palette fill derivation and viewer-derived standard-button fill resolver usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_145533/`.
- [x] Route viewer-derived `ThemePalette::buttonBorder` through `focusStroke` so neutral button edge chrome follows the same shared focus-chrome contract as the related hover and pressed fills instead of blending directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark/light viewer-palette button-border derivation, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_150110/`.
- [x] Route viewer-derived `ThemePalette::inputBorder` through `focusStroke` so neutral field, toggle, and checkbox border chrome follows the shared focus-chrome contract instead of blending directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark/light viewer-palette input-border derivation plus viewer-derived neutral toggle, checkbox, and combo field-border usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_150729/`.
- [x] Route viewer-derived `ThemePalette::hoverFill` and `ThemePalette::pressedFill` through `focusStroke` so shared overlay chrome follows the same focus-surface contract instead of keeping raw accent RGB. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark/light viewer-palette hover/pressed overlay derivation, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_151346/`.
- [x] Route toggle row tint and checkbox hover tint through the shared `hoverFill` / `pressedFill` overlay tokens so those controls stop blending directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both default-theme and viewer-derived hover/pressed overlay usage for toggle and checkbox visuals, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_152054/`.
- [x] Route hovered combo field fill through the shared `hoverFill` overlay token so combo field hover chrome stops blending directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both default-theme and viewer-derived hovered field-fill usage, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_152700/`.
- [x] Route viewer-derived `headerHovered` and `headerPressed` through the shared `hoverFill` / `pressedFill` overlay tokens so header interaction chrome stops blending directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark/light viewer-palette header hover/pressed derivation, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_153012/`.
- [x] Route checked toggle track fill and checked checkbox indicator fill through the shared `selectionFill` token so checked-state fill chrome stops reading raw accent directly. `PoC/DxUiTests/DxUiTests.cpp` now covers both default-theme and viewer-derived checked toggle/checkbox fill plus border derivation with distinct viewer `accentArgb` and `selectionBackgroundArgb` values, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_153621/`.
- [x] Route primary-button idle fill through the shared `selectionFill` token so the primary accent-surface fill stack stops blending directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both default-theme and viewer-derived primary-button idle/hover/pressed fill derivation with distinct viewer `accentArgb` and `selectionBackgroundArgb` values, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_154114/`.
- [x] Route `AdornmentTone::Accent` fill through the shared `selectionFill` token so accent badges and adornments stop reading raw accent directly. `PoC/DxUiTests/DxUiTests.cpp` now covers both default-theme and viewer-derived accent-adornment fill derivation with distinct viewer `accentArgb` and `selectionBackgroundArgb` values, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_154607/`.
- [x] Route viewer-derived `ThemePalette::buttonFill` through the shared `selectionFill` token so neutral button surfaces stop deriving directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark/light viewer-palette button-fill derivation with distinct viewer `accentArgb` and `selectionBackgroundArgb` values, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_155112/`.
- [x] Route viewer-derived `ThemePalette::headerBackground` through the shared `selectionFill` token so header base chrome stops deriving directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark/light viewer-palette header-background derivation with distinct viewer `accentArgb` and `selectionBackgroundArgb` values, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_155656/`.
- [x] Route viewer-derived `ThemePalette::surfaceBackground` through the shared `selectionFill` token so base DXUI surfaces stop deriving directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark/light viewer-palette surface-background derivation with distinct viewer `accentArgb` and `selectionBackgroundArgb` values, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_160127/`.
- [x] Route viewer-derived `ThemePalette::focusStroke` through `selectionFill` plus `selectionText` so shared focus chrome stops deriving directly from raw accent. `PoC/DxUiTests/DxUiTests.cpp` now covers both dark/light viewer-palette focus-stroke derivation with distinct viewer `accentArgb` and `selectionBackgroundArgb` values, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_160611/`.
- [x] Route shared `TextField` hover fill chrome through `ThemePalette::hoverFill` so neutral text-entry hover chrome stops blending directly from raw accent. `Common/DxUi/DxUi.Theme.cpp` now centralizes `TextFieldVisualStyle` resolution, `Common/DxUi/DxUi.TextInput.cpp` consumes that resolver, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived text-field hover/focus border-fill usage with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_161513/`.
- [x] Route shared `ColorSwatch` hover and pressed fill chrome through `ThemePalette::hoverFill` and `ThemePalette::pressedFill` so neutral swatch interaction chrome stops blending directly from raw accent. `Common/DxUi/DxUi.Theme.cpp` now centralizes `ColorSwatchVisualStyle` resolution, `Common/DxUi/DxUi.Controls.cpp` consumes that resolver, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived swatch hover/pressed/focus chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_162105/`.
- [x] Route shared grid/tree nonselected row-icon tint chrome through `ThemePalette::selectionFill` so list icon chrome stops blending directly from raw accent. `Common/DxUi/DxUi.Theme.cpp` now centralizes `ResolveListIconColor(...)`, `Common/DxUi/DxUi.Grid.cpp` plus `Common/DxUi/DxUi.Tree.cpp` consume that resolver, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived list-icon tint usage with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_162757/`.
- [x] Route shared grid spinner text and marquee fill chrome through `ThemePalette::selectionFill` so busy/progress row chrome stops reading raw accent directly on nonselected rows. `Common/DxUi/DxUi.Theme.cpp` now centralizes `ResolveGridBusyColor(...)`, `Common/DxUi/DxUi.Grid.cpp` consumes that resolver for both spinner and marquee fill states, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived busy-chrome usage with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_163450/`.
- [x] Route the shared grid header busy indicator chrome through `ThemePalette::selectionFill` so header activity chrome stops reading raw accent directly. `Common/DxUi/DxUi.Theme.cpp` now centralizes `ResolveGridHeaderBusyColor(...)`, `Common/DxUi/DxUi.Grid.cpp` consumes that resolver for the header spinner, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived header-busy usage with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_164016/`.
- [x] Remove the stale raw-accent dependency from shared button focus setup so `ResolveButtonVisualStyle(...)` no longer references `theme.accent` in a code path that primary buttons immediately overwrite. `PoC/DxUiTests/DxUiTests.cpp` now asserts both default-theme and viewer-derived primary-button focus-ring colors directly, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_164556/`.
- [x] Expand `TextFieldVisualStyle` so shared text-entry paint consumes explicit text, placeholder, and selection colors instead of ad hoc theme reads during paint. `Common/DxUi/DxUi.TextInput.cpp` now uses the resolved style for multiline text, placeholder text, single-line text, and selection chrome, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived text, placeholder, inactive selection, and focused selection values with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_165146/`.
- [x] Expand `ComboBoxVisualStyle` so editable combo paint consumes explicit selection fill and selection text colors instead of ad hoc theme reads during paint. `Common/DxUi/DxUi.ComboBox.cpp` now uses the resolved style for editable selection chrome, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived inactive and focused combo selection values with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_165642/`.
- [x] Expand `ComboBoxVisualStyle` so combo popup paint consumes explicit popup surface, border, hovered-row, row-text, and empty-state colors instead of direct theme reads during popup rendering. `Common/DxUi/DxUi.ComboBox.cpp` now uses the resolved style for popup surface and row chrome, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived popup fill, border, hover fill, row text, and empty-state text values with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_170155/`.
- [x] Expand `ComboBoxVisualStyle` so combo popup scrollbar paint consumes explicit track, thumb, and hot-thumb colors instead of direct theme reads during popup rendering. `Common/DxUi/DxUi.ComboBox.cpp` now uses the resolved style for popup scrollbar chrome, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived popup scrollbar track, thumb, and hot-thumb values with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_170647/`.
- [x] Expand `ComboBoxVisualStyle` so combo popup paint consumes explicit active-row text instead of reusing the generic popup row text during highlighted-row rendering. `Common/DxUi/DxUi.ComboBox.cpp` now uses the resolved style for active popup row text, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived popup active-row text values with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_182501/`.
- [x] Expand `ComboBoxVisualStyle` so combo popup paint consumes explicit active-row fill instead of reusing the generic popup hover token during highlighted-row rendering. `Common/DxUi/DxUi.ComboBox.cpp` now uses the resolved style for active popup row background fill, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived popup active-row fill values with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_183237/`.
- [x] Remove the redundant `ComboBoxVisualStyle::popupHoverFill` token now that combo popup highlighted rows consume the explicit `popupActiveFill` token. `Common/DxUi/DxUi.h` and `Common/DxUi/DxUi.Theme.cpp` now expose only the live popup item-state colors, `PoC/DxUiTests/DxUiTests.cpp` trims the default-theme and viewer-derived combo popup assertions to match that contract, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_183849/`.
- [x] Split combo popup pointer-hover state from the keyboard-targeted popup row so pointer leave restores the keyboard target instead of clearing the visible highlight. `Common/DxUi/DxUi.ComboBox.cpp` now keeps separate pointer-hover and active popup-row state while preserving same-row hover repaint stability, `PoC/DxUiTests/DxUiTests.cpp` now covers keyboard-target restore after pointer leave, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_184655/`.
- [x] Expand `ComboBoxVisualStyle` so combo popup paint also carries explicit selected-row fill and text tokens when a different row is temporarily highlighted. `Common/DxUi/DxUi.ComboBox.cpp` now paints the selected popup row distinctly from the highlighted row, `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived selected-row popup colors, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_185319/`.
- [x] Tighten combo popup highlighted-row contrast so the active popup row uses `selectionFill` instead of the softer hover fill while the selected-but-not-highlighted row keeps `selectionInactiveFill`. `Common/DxUi/DxUi.Theme.cpp` now resolves that stronger active-row fill, `PoC/DxUiTests/DxUiTests.cpp` proves it for both default-theme and viewer-derived palettes, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_185918/`.
- [x] Soften combo popup selected-row text so the selected-but-not-highlighted row keeps the regular body text color while the highlighted row retains `selectionText`. `Common/DxUi/DxUi.Theme.cpp` now resolves that lower-emphasis selected-row text, `PoC/DxUiTests/DxUiTests.cpp` proves it for both default-theme and viewer-derived palettes, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_190512/`.
- [x] Align the shared grid's trailing vertical scroll extent to the next visible item boundary so bottom scrolling no longer leaves a partially clipped first visible row under the header. `Common/DxUi/DxUi.Grid.cpp` now snaps the trailing scroll remainder to a full row or group-header boundary, `PoC/DxUiTests/DxUiTests.cpp` proves an attached plain grid lands with its first visible row flush under the header at bottom scroll, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_191933/`.
- [x] Suppress bottom-clipped trailing body rows so the shared grid only exposes and paints fully visible body rows above the footer/scrollbar edge. `Common/DxUi/DxUi.Grid.cpp` now stops the body pass at the last fully visible row, `PoC/DxUiTests/DxUiTests.cpp` adds an explicit trailing-row suppression regression and updates plain/grouped/checkbox plus accessibility fixtures to the full-row contract, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_193207/`.
- [x] Expand shared card-surface chrome into an explicit `CardPanelVisualStyle` contract so card shells stop blending surface fill and border ad hoc during paint. `Common/DxUi/DxUi.Theme.cpp` now resolves shared card fill and border colors, `Common/DxUi/DxUi.Controls.cpp` consumes that resolver in `CardPanel::Paint(...)`, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived card chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_194055/`.
- [x] Expand shared tooltip chrome into an explicit `TooltipVisualStyle` contract so popup/tooltip surfaces stop reading tooltip colors and hardcoding their border tint inline during paint. `Common/DxUi/DxUi.Theme.cpp` now resolves shared tooltip fill, border, and text colors, `Common/DxUi/DxUi.Controls.cpp` consumes that resolver in `TooltipLayer::Paint(...)`, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived tooltip chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_194911/`.
- [x] Expand `ToggleVisualStyle` so shared switches consume an explicit knob-border token instead of blending knob outline chrome inline during paint. `Common/DxUi/DxUi.Theme.cpp` now resolves `style.knobBorder`, `Common/DxUi/DxUi.Controls.cpp` consumes that token in `Toggle::Paint(...)`, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived knob-border derivation with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_195515/`.
- [x] Expand `TextFieldVisualStyle` so shared text-entry paint consumes an explicit caret token instead of reading caret chrome directly from the theme during paint. `Common/DxUi/DxUi.TextInput.cpp` now draws the caret from `style.caret`, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived caret derivation with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_200204/`.
- [x] Expand `ColorSwatchVisualStyle` so shared swatch paint consumes an explicit focus-ring token instead of reading focus chrome directly from the theme during paint. `Common/DxUi/DxUi.Controls.cpp` now draws the swatch focus ring from `style.focus`, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived swatch focus derivation with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_200817/`.
- [x] Expand `LabelVisualStyle` into an explicit contract so shared label paint consumes a resolved text token instead of reading palette text inline. `Common/DxUi/DxUi.Controls.cpp` now draws labels from `ResolveLabelVisualStyle(...)`, and `PoC/DxUiTests/DxUiTests.cpp` covers default-theme, viewer-derived, and explicit override label text chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_201443/`.
- [x] Expand `ColorSwatchVisualStyle` so shared swatch paint consumes explicit inner `swatchFill` and `swatchBorder` tokens instead of blending disabled assigned-color content and inner border chrome inline. `Common/DxUi/DxUi.Theme.cpp` now resolves those inner swatch colors, `Common/DxUi/DxUi.Controls.cpp` consumes them in `ColorSwatch::Paint(...)`, and `PoC/DxUiTests/DxUiTests.cpp` covers default-theme and viewer-derived assigned-color plus disabled assigned-color chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_202417/`.
- [x] Expand shared grid chrome into an explicit `GridSurfaceVisualStyle` contract so grid paint stops reading outer surface, header strip, separator, and empty-state text colors inline from the palette. `Common/DxUi/DxUi.Theme.cpp` now resolves shared grid surface chrome, `Common/DxUi/DxUi.Grid.cpp` consumes that resolver in `Grid::Paint(...)`, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived grid surface chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_203322/`.
- [x] Expand shared tree chrome into an explicit `TreeSurfaceVisualStyle` contract so tree paint stops reading outer surface, border, and empty-state text colors inline from the palette. `Common/DxUi/DxUi.Theme.cpp` now resolves shared tree surface chrome, `Common/DxUi/DxUi.Tree.cpp` consumes that resolver in `Tree::Paint(...)`, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived tree surface chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_204032/`.
- [x] Expand shared grid header chrome into an explicit `GridHeaderVisualStyle` contract so grid header and group-header paint stop reading fill, separator, title, sort-glyph, and grouped-header chrome inline from the palette. `Common/DxUi/DxUi.Theme.cpp` now resolves shared grid header chrome, `Common/DxUi/DxUi.Grid.cpp` consumes that resolver in the header and group-header paint paths, and `PoC/DxUiTests/DxUiTests.cpp` covers both default-theme and viewer-derived header chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_204832/`.
- [x] Expand the shared tree row visuals helper so tree row focus chrome consumes a resolved focus token instead of reading `theme.focusStroke` inline during paint. `Common/DxUi/DxUi.Tree.cpp` now resolves the focus-ring color alongside row fill/text state, `PoC/DxUiTests/DxUiTests.cpp` extends tree debug-row coverage to assert default-theme, high-contrast, and viewer-derived focus-ring chrome, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_205551/`.
- [x] Expand the shared tree row visuals helper so tree row icon chrome consumes a resolved icon token instead of calling `ResolveListIconColor(...)` inline during paint. `Common/DxUi/DxUi.Tree.cpp` now resolves icon tint alongside row fill/text/focus state, `PoC/DxUiTests/DxUiTests.cpp` extends tree debug-row coverage to assert selected and unselected icon chrome for default-theme and viewer-derived palettes, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_210328/`.
- [x] Expand the shared grid row visuals helper so grid row icon chrome consumes a resolved icon token instead of calling `ResolveListIconColor(...)` inline during cell paint. `Common/DxUi/DxUi.Grid.cpp` now resolves icon tint alongside row fill/text/rainbow state, `PoC/DxUiTests/DxUiTests.cpp` adds grid debug-row coverage for selected and unselected icon chrome in default-theme and viewer-derived palettes, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_211149/`.
- [x] Expand grid swatch chrome into an explicit `GridSwatchVisualStyle` contract so grid swatch cells stop blending fill and border chrome inline from the palette and row colors. `Common/DxUi/DxUi.Theme.cpp` now resolves shared grid swatch fill/border chrome, `Common/DxUi/DxUi.Grid.cpp` consumes that resolver during swatch-cell paint, and `PoC/DxUiTests/DxUiTests.cpp` covers default-theme and viewer-derived swatch fill alpha plus selected/unselected border chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_212040/`.
- [x] Expand grid badge chrome into an explicit `GridBadgeVisualStyle` contract so grid badge cells stop overriding selected-row badge fill/text inline during paint. `Common/DxUi/DxUi.Theme.cpp` now resolves shared grid badge fill/text chrome, `Common/DxUi/DxUi.Grid.cpp` consumes that resolver during badge-cell paint, and `PoC/DxUiTests/DxUiTests.cpp` covers default-theme and viewer-derived selected/unselected badge chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_212919/`.
- [x] Expand grid progress chrome into an explicit `GridProgressVisualStyle` contract so marquee and determinate progress cells stop blending track chrome inline during paint. `Common/DxUi/DxUi.Theme.cpp` now resolves shared grid progress track/fill chrome, `Common/DxUi/DxUi.Grid.cpp` consumes that resolver during marquee/progress paint, and `PoC/DxUiTests/DxUiTests.cpp` covers default-theme and viewer-derived selected/unselected progress chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_213926/`.
- [x] Expand grid checkbox chrome into an explicit `GridCheckboxVisualStyle` contract so grid checkbox cells stop overriding selected-row checkmark color inline during paint. `Common/DxUi/DxUi.Theme.cpp` now resolves shared grid checkbox indicator and checkmark chrome, `Common/DxUi/DxUi.Grid.cpp` consumes that resolver during checkbox-cell paint, and `PoC/DxUiTests/DxUiTests.cpp` covers default-theme and viewer-derived selected/unselected checkbox chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_215259/`.
- [x] Expand tree badge chrome into an explicit `TreeBadgeVisualStyle` contract so tree rows stop resolving badge fill/text chrome inline during paint. `Common/DxUi/DxUi.Theme.cpp` now resolves shared tree badge chrome, `Common/DxUi/DxUi.Tree.cpp` consumes that resolver during badge paint, and `PoC/DxUiTests/DxUiTests.cpp` covers default-theme and viewer-derived tree badge chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_220133/`.
- [x] Expand the shared tree row visuals helper so expander glyph paint consumes a resolved expander token instead of reusing row text color inline during `DrawTreeRow(...)`. `Common/DxUi/DxUi.Tree.cpp` now resolves expander chrome alongside row fill/text/icon/focus state, `PoC/DxUiTests/DxUiTests.cpp` extends tree debug-row coverage to assert default-theme and viewer-derived expander chrome stays aligned with the resolved row-text contract, and the same-machine Release baseline is archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_220932/`.
- [x] Expand `GridSurfaceVisualStyle` so grid body row and column separators stop reading `theme.gridLine` inline during paint. `Common/DxUi/DxUi.Theme.cpp` now resolves shared row/column separator chrome, `Common/DxUi/DxUi.Grid.cpp` consumes those tokens during body row and cell-separator paint, and `PoC/DxUiTests/DxUiTests.cpp` extends grid surface coverage for default-theme and viewer-derived separator chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_221622/`.
- [x] Fold the shared grid header busy-indicator chrome into `GridHeaderVisualStyle` so header paint consumes `headerStyle.busyGlyph` instead of a standalone `ResolveGridHeaderBusyColor(...)` helper. `Common/DxUi/DxUi.Theme.cpp` now resolves `busyGlyph` as part of `ResolveGridHeaderVisualStyle(...)`, `Common/DxUi/DxUi.Grid.cpp` uses that token for header spinner paint, and `PoC/DxUiTests/DxUiTests.cpp` covers default-theme and viewer-derived header busy-glyph chrome with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_222602/`.
- [x] Fold shared tree badge chrome into the shared tree row-visuals helper so `DrawTreeRow(...)` consumes resolved badge tokens instead of resolving `TreeBadgeVisualStyle` inline during paint. `Common/DxUi/DxUi.Tree.cpp` now resolves badge fill/text alongside row fill/text/icon/expander/focus state, the tree debug-row export now exposes badge tokens too, and `PoC/DxUiTests/DxUiTests.cpp` extends the existing tree focus-visual coverage for selected and viewer-derived rows with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_223933/`.
- [x] Fold shared grid spinner chrome into the shared grid row-visuals helper so spinner cells consume `rowVisuals.busy` instead of calling `ResolveGridBusyColor(...)` inline during paint. `Common/DxUi/DxUi.Grid.cpp` now resolves busy chrome alongside row fill/text/icon state, the grid debug-row export now exposes a busy token too, and `PoC/DxUiTests/DxUiTests.cpp` extends the existing grid row-visual coverage for default-theme and viewer-derived rows with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_224539/`.
- [x] Fold shared grid marquee/progress chrome into the shared grid row-visuals helper so progress cells consume `rowVisuals.progressTrack` and `rowVisuals.progressFill` instead of resolving `GridProgressVisualStyle` inline during paint. `Common/DxUi/DxUi.Grid.cpp` now resolves progress track/fill chrome alongside row fill/text/icon/busy state, the grid debug-row export now exposes progress tokens too, and `PoC/DxUiTests/DxUiTests.cpp` extends the existing grid row-visual coverage for default-theme and viewer-derived rows with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_225322/`.
- [x] Fold shared grid checkbox, swatch, and badge chrome into a shared cell-visuals helper so grid cell paint stops resolving those cell-state contracts inline. `Common/DxUi/DxUi.Grid.cpp` now resolves checkbox/swatch/badge chrome through `ResolveGridCellVisuals(...)`, the debug grid export exposes integrated cell chrome through `DebugGetCellVisualState(...)`, and `PoC/DxUiTests/DxUiTests.cpp` adds default-theme and viewer-derived cell-visual coverage with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_230637/`.
- [x] Expand `ComboBoxVisualStyle` with an explicit caret token so editable combo paint stops reusing `focusAccent` for caret chrome. `Common/DxUi/DxUi.Theme.cpp` now resolves `style.caret`, `Common/DxUi/DxUi.ComboBox.cpp` consumes that token during caret paint, and `PoC/DxUiTests/DxUiTests.cpp` extends default-theme and viewer-derived combo style coverage with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_231413/`.
- [x] Expand `ComboBoxVisualStyle` with an explicit glyph token so combo chevron paint stops piggybacking on generic text chrome. `Common/DxUi/DxUi.Theme.cpp` now resolves `style.glyph`, `Common/DxUi/DxUi.ComboBox.cpp` consumes that token during chevron paint, and `PoC/DxUiTests/DxUiTests.cpp` extends default-theme and viewer-derived combo style coverage with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_232028/`.
- [x] Expand `ComboBoxVisualStyle` with an explicit split-stroke token so split-divider paint stops borrowing `fieldBorder` directly. `Common/DxUi/DxUi.Theme.cpp` now resolves `style.splitStroke`, `Common/DxUi/DxUi.ComboBox.cpp` consumes that token during split-divider paint, and `PoC/DxUiTests/DxUiTests.cpp` extends default-theme and viewer-derived combo style coverage with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_232923/`.
- [x] Validate focused toggle and checkbox keyboard activation so `Space` and `Enter` toggle the control directly, keep keyboard-focus visuals alive, and do not fall through to the host default button path. `PoC/DxUiTests/DxUiTests.cpp` now exercises both control types through `DxUi::WindowHost` with a same-machine Release baseline archived under `Specs/TestRuns/44da12cf5574/DxUi/2026-03-28_233846/`.
- [x] Validate focused button keyboard activation so `Space` and `Enter` invoke the focused button directly, preserve keyboard-focus visuals, and do not fall through to a different host default button. `PoC/DxUiTests/DxUiTests.cpp` now exercises that flow through `DxUi::WindowHost` with a same-machine Release baseline archived under `Specs/TestRuns/44da12cf5574/DxUi/2026-03-28_234532/`.
- [x] Validate focused combo keyboard activation so non-editable combos keep `Space` and `Enter` on popup toggle/commit behavior and editable combos keep `Enter` on submit or popup commit instead of falling through to the host default button path. `PoC/DxUiTests/DxUiTests.cpp` now exercises both combo variants through `DxUi::WindowHost` with a same-machine Release baseline archived under `Specs/TestRuns/44da12cf5574/DxUi/2026-03-28_235142/`.
- [x] Add a mixed dialog-shell keyboard-flow regression so one shared host can tab through text entry, combo, checkbox, toggle, and command button rows while each focused control keeps the expected key ownership and only the text field falls back to the default button path. `PoC/DxUiTests/DxUiTests.cpp` now exercises that end-to-end flow through `DxUi::WindowHost` with a same-machine Release baseline archived under `Specs/TestRuns/44da12cf5574/DxUi/2026-03-28_235748/`.
- [x] Add a mixed dialog-shell mouse-flow regression so one shared host can route text-entry focus, combo popup selection, checkbox toggles, toggle switches, and command-button clicks to the hit DX control, switch back to pointer modality, and avoid any default-button fallthrough. `PoC/DxUiTests/DxUiTests.cpp` now exercises that end-to-end flow through `DxUi::WindowHost` with a same-machine Release baseline archived under `Specs/TestRuns/09419719ef8e/DxUi/2026-03-29_115839/`.
- [x] Add a real-window `CompareDirectoriesWindow.Options` interaction regression so the visible DX toggle and DX edit descendants on the one-host options surface accept live UIA-driven toggle/value mutation, the visible DX `Cancel` action can close the dialog through live `InvokePattern` while discarding that mutation, and the visible DX `OK` action can still close the dialog through live `InvokePattern`, all while keeping the dialog on the DX path with zero visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow through the real options window in `Debug|x64` with the `cmd_compare_directories_options_live_dx_body_interaction` case.
- [x] Add a real-window plugin-configuration interaction regression so a visible DX edit and visible DX toggle on the re-landed schema-form shell can round-trip through live UIA `ValuePattern` / `TogglePattern` mutation and the visible DX `Cancel` action can close the modal shell through live `InvokePattern`, all while the surface stays fully DX with zero visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_plugin_configuration_dialog_live_dx_interaction` case.
- [x] Add a real-window credential-prompt interaction regression so a visible DX edit and visible DX secret-visibility toggle can round-trip through live UIA `ValuePattern` / `TogglePattern` mutation, the visible DX secret edit can accept a live password value, and the visible DX `OK` action can close the modal shell through live `InvokePattern`, all while the surface stays fully DX with zero visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_connection_credential_prompt_live_dx_interaction` case.
- [x] Add a real-window pane-filter prompt interaction regression so the visible DX filter edit and DX enable toggle can round-trip through live UIA `ValuePattern` / `TogglePattern` mutation, the visible DX `Cancel` action can close the prompt through live `InvokePattern` without activating the filter, and the visible DX `OK` action can close the prompt through live `InvokePattern` while activating the real left-pane name filter, all without surfacing visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_pane_filter_prompt_live_dx_interaction` case.
- [x] Add a real-window selection-mask prompt interaction regression so the visible DX mask edit can round-trip through live UIA `ValuePattern` mutation on both the `Select` and `Unselect` prompt variants, the visible DX `Cancel` action can close the prompt through live `InvokePattern` without changing the real left-pane selection set, and the visible DX `OK` action can close the prompt through live `InvokePattern` while applying the expected selection changes, all without surfacing visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_pane_selection_mask_prompt_live_dx_interaction` case.
- [x] Add a real-window rename prompt interaction regression so the visible DX rename edit can round-trip through live UIA `ValuePattern` mutation, the visible DX `OK` and `Cancel` actions can close the prompt through live `InvokePattern`, the real file system rename succeeds, and then canceling a second live edit still leaves the renamed item untouched, all without surfacing visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_pane_rename_prompt_live_dx_interaction` case.
- [x] Add a real-window create-directory prompt interaction regression so the visible DX folder-name edit can round-trip through live UIA `ValuePattern` mutation, the visible DX `OK` and `Cancel` actions can close the prompt through live `InvokePattern`, confirm successfully against the real file system, and then cancel a second live edit without creating the directory, all without surfacing visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_pane_createDirectory_prompt_live_dx_interaction` case.
- [x] Add a real-window change-case prompt interaction regression so the visible DX include-subdirectories toggle can round-trip through live UIA `TogglePattern` mutation, the visible DX `OK` and `Cancel` actions can close the owned prompt through live `InvokePattern`, and both confirm/cancel flows preserve the expected on-disk rename state without surfacing visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_pane_changeCase_prompt_live_dx_interaction` case.
- [x] Add a real-window custom speed-limit prompt interaction regression so the visible DX limit edit can round-trip through live UIA `ValuePattern` mutation, the visible DX `OK` and `Cancel` actions can close the owned prompt through live `InvokePattern`, and both confirm/cancel flows preserve the expected task bandwidth state without surfacing visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_pane_fileops_speedLimit_prompt_live_dx_interaction` case.
- [x] Add a real-window Item Properties interaction regression so the visible DX read-only content surface remains exposed through `ValuePattern` and the visible DX close button can close the owned window through live UIA `InvokePattern` while the shell stays fully DX with zero visible legacy controls. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_pane_itemProperties_window_live_dx_interaction` case.
- [x] Add a real-window About interaction regression so the visible DX footer button can close the independent owned shell through live UIA `InvokePattern` while the window stays fully DX with zero visible legacy controls and preserves its visible text surface. `RedSalamander/Commands.SelfTest.cpp` now exercises that flow in `Debug|x64` with the `cmd_app_about_live_dx_interaction` case.
- [x] Add lightweight motion for hover/focus fades, sort glyph transitions, and scrollbar feedback. Shared buttons, toggles, and checkboxes now animate hover/focus chrome through the existing `WindowHost` animation dispatcher, and shared grid/tree scrollbars now fade track/thumb warmth through that same dispatcher instead of popping immediately, all with reduced-motion snap behavior plus deterministic style/tick-lifetime and scrollbar-progress coverage in `PoC/DxUiTests/DxUiTests.cpp`.
- [x] Tighten selected-row state-image contrast so checked grid checkboxes, selected grid badge pills, and selected swatch borders invert against the selected-row fill instead of softening back toward accent-derived chrome. `Common/DxUi/DxUi.Theme.cpp` now resolves those selected cell visuals from selected row fill/text contrast, and `PoC/DxUiTests/DxUiTests.cpp` carries matching default-theme and viewer-derived chrome assertions.
- [x] Add tracking-tooltip reuse so unchanged tooltip text/origin updates become no-ops while same-text pointer tracking can still move the visible tooltip. `TooltipLayer`, `WindowHost`, and shared-grid hover now invalidate only on real tooltip state changes, with deterministic tooltip-layer same-text move/no-op coverage plus same-cell grid pointer-tracking coverage and a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_123346/`.
- [x] Add shared tree item tooltip support so explicit per-item tooltip text and clipped-label fallback both flow through the shared tooltip layer with the same low-churn pointer-tracking behavior. The tree hover path now invalidates only on real tooltip state changes, with deterministic same-row tracking coverage plus clipped-label fallback coverage and a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_125116/`.
- [x] Add attached combo-popup hover stability coverage so opening a popup can keep its pre-highlighted row stable, repeated pointer movement within an already-highlighted row does not request a new host invalidation or repaint, and real row changes still repaint. `PoC/DxUiTests/DxUiTests.cpp` now uses exact popup-row geometry plus a debug invalidate counter on `DxUi::WindowHost`, with a same-machine Release baseline archived under `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_131643/`.
- [x] Add deterministic animation timing coverage for retained-tree tick propagation and animated grid-cell detection.
- [x] Fix the later shared DX hot-path review findings so `TooltipLayer` caches DirectWrite layout/bounds instead of allocating two layouts per paint, shared buttons advance hover/focus transitions on the first frame, shared grid paint guards optional checkbox/swatch/badge visuals and resolves icon/busy/progress chrome only when the visible cell kind needs it, and command selftest UIA helpers reuse thread-local `IUIAutomation` state with property-filtered descendant queries. Validation passed in `Debug|x64` plus sanitized `Release|x64`, with a fresh same-machine `DxUiTests` baseline archived under `Specs/TestRuns/09419719ef8e/DxUi/2026-03-29_141907/`.
- [x] Validation: dark, light, rainbow, and high-contrast remain legible across the fully DirectX pilot windows.

## Phase 5 - Pilot full-window rewrites

- [x] Rewrite `FileOperationsIssuesPane` as a full DirectX-hosted window using `DxUi::WindowHost` and `DxUi::Grid`.
- [x] Preserve timer-driven refresh, stable-row selection preservation, source-side sorting in the pane model, and themed warning/error styling for the issues pilot.
- [x] Rewrite `ViewerSqlite` as a full DirectX window using the shared control tree and shared grid.
- [x] Drive `ViewerSqlite` table-preview sort requests from shared grid headers into source-side SQLite `ORDER BY` queries.
- [x] Wire `ViewerSqlite` query submission to DX text-field Enter handling in addition to the explicit Run button.
- [x] Surface `ViewerSqlite` loading state through shared grid header busy indicators during page/query refresh.
- [x] Wire `ViewerSqlite` Run Query as the shared host default action so non-owning Enter flows converge on the same command path.
- [x] Rewrite `FindFilesWindow` as a full DirectX window using the shared control tree and shared grid.
- [x] Preserve the pilot Find result-activation contract for directory rows, including `Open Parent` parent-folder focus behavior.
- [x] Snap the `FindFilesWindow` DX layout to device pixels during resize to reduce rounded-border shimmer from fractional-DIP bounds.
- [x] Wire `FindFilesWindow` field mnemonics and debug focus/popup snapshot hooks so the pilot can be validated through real host-message self-tests instead of control-only assumptions.
- [x] Remove visible comctl controls from all three pilot windows only after parity checks pass.
- [x] Validation: all three pilot windows run as visible DirectX windows with no visible comctl fallback.

## Phase 6 - Advanced parity and broader comctl replacement

- [x] Add checkbox-cell interaction semantics: pointer hit-target toggles and keyboard `Space` toggles on the active checkbox column or the first enabled checkbox cell in the current row.
- [x] Add dedicated checkbox/state-image column support and the remaining checkbox-column parity on top of the current checkbox-cell interaction foundation.
- [x] Add explicit grid infotip hooks so later list migrations can preserve `LVN_GETINFOTIP`-style behavior without visible comctl fallback.
- [x] Add grouped rows and grouped rendering.
- [x] Add optional icon/badge cells.
- [x] Add checkbox-style grid cell presentation with deterministic layout metrics and copy/export state formatting.
- [x] Add shared pointer and keyboard context-menu routing for the first DX tree/navigation control.
- [x] Add optional icon and badge adornments to the shared DX tree/navigation control with deterministic layout metrics for regression coverage.
- [x] Add interactive column reordering and persisted width/order behavior where required.
- [x] Land the first Preferences page-content slice by moving the full visible `Preferences.Viewers` page surface onto shared DX hosts while keeping the current backing state intact. Later stabilization work rolled that full page surface back to the proven legacy path, and the current one-host re-land has now restored that visible Viewers page surface on the live DX path.
- [ ] Migrate `simple later` windows only after the required parity exists.
- [ ] Validation: each migrated later window matches the capability bucket it depended on before replacement.

## Phase 7 - Tests, performance gates, docs, and cleanup

- [x] Add `PoC/DxUiTests/` for deterministic shared-layer verification.
- [x] Add tests covering sort transitions, virtualization helpers, and selection-model behavior.
- [x] Add deterministic SQLite engine coverage for source-side table-preview sorting and sorted paging behavior.
- [x] Add deterministic shared-control tests for focus traversal, combo popup close-on-focus-loss, and single-line Enter-submit behavior.
- [x] Add deterministic editable-combo coverage for typed history text, dropdown selection sync, and combo Enter-submit behavior.
- [x] Add deterministic mnemonic coverage for button activation, label-to-field focus routing, and unmatched-key passthrough.
- [x] Add deterministic shared-tree context-menu coverage for pointer right-click, Menu key, and `Shift+F10`, plus attached-window hidden-bridge undo/redo coverage for text fields and editable combos.
- [x] Add deterministic attached-window hidden-bridge clipboard coverage for native paste and masked secret copy/cut suppression.
- [x] Add deterministic attached-window multiline IME and multiline `Shift+PageUp` coverage so multiline bridge composition and viewport navigation parity stop depending on manual retesting.
- [x] Add deterministic attached-window hidden-helper rect coverage so single-line and multiline bridge tests prove the helper tracks the visible DX field position as well as its viewport size.
- [x] Add deterministic shared-tree layout coverage for optional icon and badge adornments so tree text, badge, and expander regions stay non-overlapping.
- [x] Add deterministic mnemonic-display helper coverage for visible underline placement.
- [x] Add deterministic focus coverage proving combo boxes participate in retained-tree tab traversal.
- [x] Add deterministic host-level coverage for input-modality tracking and keyboard-focus visibility policy.
- [x] Add deterministic host-level coverage for default-button and cancel-button routing, including popup-owned Esc behavior.
- [x] Add deterministic host-level coverage for `Alt+Down` / `Alt+Up` combo popup handling via `WM_SYSKEYDOWN`.
- [x] Add deterministic host-level and grid-level coverage for Menu-key / `Shift+F10` context-menu routing and row-anchor selection.
- [x] Add deterministic popup-scaling and combo typeahead coverage for longer lists.
- [x] Add deterministic editable-popup filtering coverage for prefix-matched history lists, filtered keyboard navigation, and no-match empty-state behavior.
- [x] Add deterministic overlay paint-order coverage for combo dropdowns and shared popup-layer content.
- [x] Add deterministic overlay-input coverage proving open combo popups do not leak hover or clicks to overlapping controls underneath.
- [x] Add deterministic host-level coverage proving child `DxUi::WindowHost` surfaces return `MA_ACTIVATE` so embedded DX combo hosts do not lose the first click to shell activation.
- [x] Add deterministic constrained-popup placement coverage for flipped-above combos and `PageUp` / `PageDown` behavior against clamped visible row counts.
- [x] Add deterministic widened-popup coverage proving long combo entries expand hit-testing beyond the base control width without breaking selection.
- [x] Add deterministic popup-scrollbar coverage proving track paging on long combo dropdowns reveals later visible rows without breaking item selection.
- [x] Add deterministic combo-popup dismissal coverage proving second-click toggle-close and mouse-opened `Esc` dismissal both use the shared popup-close path.
- [x] Add deterministic text-input coverage for hover routing, `Ctrl+Backspace` word deletion, and pointer caret placement in DX text/edit-combo controls.
- [x] Add deterministic single-line selection coverage for DX text fields and editable combos, including `Shift+Arrow`, `Ctrl+A`, double-click word selection, host-routed mouse drag selection, and replacement of the selected range on input.
- [x] Add deterministic shared-tree coverage for pointer selection, expander toggles, left/right parent traversal, typeahead, double-click leaf invocation, page navigation, wheel scrolling, and selection invalidation on model change.
- [x] Add deterministic retained-tree/grid animation tests for animated-cell detection and container tick propagation.
- [x] Add deterministic grid header-busy coverage so header spinners request animation ticks only when the indicator is actually drawable.
- [x] Add deterministic shared-grid sort-glyph transition coverage, including reduced-motion fallback.
- [x] Add deterministic shared-grid scrollbar visual-state coverage for idle, track-hover, thumb-hover, drag, and leave reset behavior.
- [x] Add deterministic shared-tree scrollbar visual-state coverage for idle, track-hover, thumb-hover, drag, and leave reset behavior.
- [x] Add deterministic shared-tree focus-modality coverage for pointer focus, keyboard-visible focus, and high-contrast focused-row fallback behavior.
- [x] Add deterministic shared-tree rainbow-row coverage proving selected rows tint only in rainbow mode and suppress rainbow tint in high-contrast mode.
- [x] Add deterministic viewer-theme palette coverage proving shared DX control chrome derives from both dark and light `ViewerTheme` inputs.
- [x] Add deterministic toggle/checkbox focus-modality coverage for pointer focus, keyboard-visible focus, and high-contrast fallback behavior.
- [x] Add deterministic visual-state coverage for Preferences-aligned DX button, switch, standard checkbox, and explicit combo-variant style resolution.
- [x] Add deterministic regression coverage for root replacement clearing interaction state, empty-grid hit-testing, and model-clear-during-resize behavior.
- [x] Add deterministic regression coverage proving outsider controls/buttons cannot replace installed-root focus, capture, default-button, or cancel-button observers.
- [x] Add deterministic visible-work coverage proving the shared grid clamps million-row and wide-column datasets to visible-row and visible-column work only.
- [x] Add deterministic grid-cell layout coverage for checkbox, icon, and badge adornments so non-overlap is regression-tested without screenshots.
- [x] Add deterministic grid-checkbox interaction coverage for indicator-only click toggles, non-toggle text clicks, and `Space` toggling on the active checkbox column across rows.
- [x] Add deterministic dedicated-column coverage for centered checkbox/state-image layout plus pointer and keyboard toggle behavior on dedicated checkbox columns.
- [x] Add deterministic explicit-infotip coverage proving shared-grid cell hints can override generic long-text tooltip fallback.
- [x] Add deterministic header drag-reorder coverage, including reverse-direction reorder and stable-id layout roundtrip after a drag-reordered capture.
- [x] Add deterministic shared UI Automation coverage for tree/grid metadata so attached hosts prove tree control-type plus associated-label naming and grid control-type plus associated-label naming plus row/column count exposure.
- [x] Add live migrated-window command self-test coverage proving visible DX hosts answer `WM_GETOBJECT` in Preferences, `ConnectionManagerDialog`, and `ManagePluginsDialog`.
- [x] Add harness-style coverage for default-button routing, popup dismissal, tree navigation, grouped rows, checkbox columns, and icon-cell rendering.
- [x] Add harness-style coverage for popup placement, DPI changes, and device-loss recovery.
- [x] Add targeted command self-test coverage for the migrated DX credential prompt, including validation, visibility-toggle, live DX cancel-discard-on-reopen semantics, accept, and escape-cancel flows.
- [x] Add targeted command self-test coverage proving the migrated `FindFilesWindow` uses `DxUi::WindowHost` and exposes no visible child-control fallback.
- [x] Add targeted command self-test coverage proving the migrated `FileOperationsIssuesPane` uses `DxUi::WindowHost` and exposes no visible child-control fallback.
- [x] Add real-window plugin validation coverage proving the migrated `ViewerSqlite` window exposes no visible child-control fallback.
- [x] Add targeted command self-test coverage for `FindFilesWindow` directory-result activation and `Open Parent` parent-focus behavior.
- [x] Add targeted command self-test coverage for `FindFilesWindow` default-button Enter routing, combo-popup `Esc` dismissal, field access keys, and grid-focused Enter activation.
- [x] Strengthen `FindFilesWindow` action-button coverage so the visible DX `Find`, `Append`, `Intersect`, `Subtract`, `Cancel`, `Open`, and `Open Parent` actions execute through live UIA `InvokePattern` on the owned shell instead of only focused keyboard-space activation.
- [x] Add remaining pilot integration coverage for SQLite paging/sort UI flows, including real-window viewer paging and header-sort cycling coverage across repeated in-process plugin reloads.
- [x] Add remaining broader Find command parity coverage beyond the current self-tests, including focused DX action-button keyboard activation for Append/Intersect/Subtract/Cancel/Open/Parent on the live migrated window.
- [x] Add live `SelectionPattern` coverage for the re-landed `Preferences.Plugins` main-list and custom-paths grids plus the `Preferences.Viewers`, `Preferences.Keyboard`, and `Preferences.Themes` DX grids, including selected-row accessible-name tracking across repeated real selection churn in both `Debug|x64` and `ASan Debug|x64`.
- [x] Add `Preferences.Viewers` to the widened real-window/app-window long-run scrolling gate matrix by exposing bounded-work debug metrics for the live mappings grid and covering repeated real list scrolling without category-tree repaint churn or DX host resize churn.
- [x] Add `Preferences.Themes` to the widened real-window/app-window long-run scrolling gate matrix by exposing bounded-work debug metrics for the live colors grid and covering repeated real list scrolling without category-tree repaint churn or DX host resize churn.
- [x] Add the secondary `Preferences.Plugins` custom-paths DX list to the widened real-window/app-window long-run scrolling gate matrix by exposing bounded-work debug metrics for that live list and covering repeated real list scrolling without category-tree repaint churn or DX host resize churn.
- [x] Add the broader real-window/app-window long-run scrolling and throughput gates beyond the landed attached-host large plain/grouped/checkable bounded-work regressions, sustained repeated-scroll attached-host large-grid/combo coverage, and attached popup-scrolling stability coverage. The widened real-window sustained-scroll matrix now covers `FindFilesWindow`, `FileOperationsIssuesPane`, `ConnectionManagerDialog`, `ShortcutsWindow`, `ViewerSqlite`, `Preferences.Plugins` main-list and custom-paths grids, `Preferences.Keyboard`, `Preferences.Viewers`, and `Preferences.Themes`.
- [x] Add repeated open/close churn coverage for `ShortcutsWindow` on top of its grouped/icon selection/search/scroll matrix. The real owned DX window now reopens cleanly across 12 cycles while preserving zero visible child fallback, editable search `ValuePattern`, visible grid `SelectionPattern`, and a named selected `DataItem` in both `Debug|x64` and `ASan Debug|x64`.
- [x] Add repeated open/close churn coverage for `FolderWindow.ItemProperties` on top of its owned-window and long-run scroll matrix. The real owned DX window now reopens cleanly across 12 cycles while preserving zero visible child fallback, read-only `ValuePattern`, visible button `InvokePattern`, and clean close in both `Debug|x64` and `ASan Debug|x64`.
- [x] Split `Common/DxUi/DxUi.cpp` into solution-style translation units and keep private helpers scoped to the owning translation unit:
- [x] Add `Common/DxUi/DxUi.Internal.h` as the private helper declaration surface shared by split `DxUi` translation units.
  - [x] `Common/DxUi/DxUi.WindowHost.cpp`
  - [x] `Common/DxUi/DxUi.Controls.cpp`
  - [x] `Common/DxUi/DxUi.TextInput.cpp`
  - [x] `Common/DxUi/DxUi.ComboBox.cpp`
  - [x] `Common/DxUi/DxUi.Tree.cpp`
  - [x] `Common/DxUi/DxUi.Grid.cpp`
  - [x] `Common/DxUi/DxUi.Theme.cpp`
- [x] Update end-user or contributor docs once the remaining pilot windows are migrated.
- [x] Validation: `DxUi`, `ViewerSqlite`, `RedSalamander`, `DxUiTests`, and `ViewerSqliteTests` build green in the validated configs, and the test executables pass.
- [x] Validation: `RedSalamander.vcxproj` is warning-clean again in `Release|x64` after trimming dead internal self-test helpers from the command/dialog integration files.
- [x] Validation: clipboard-sensitive hidden-bridge runtime tests are rerun sequentially when Debug clipboard contention produces false failures, so shared-clipboard timing does not leave misleading red runs in the validation log.
- [x] Validation: app consumers of `DxUi.lib` now build cleanly after the IME anchor work because the shared text-input slice links `imm32.lib` directly instead of depending on test-only linkage.
- [x] Validation: clipboard-sensitive hidden-bridge tests now verify clipboard write visibility before proceeding, reducing false failures on bridge-paste assertions even when the system clipboard is slow to settle.
- [x] Validation: separate MSBuild invocations that consume the same split `DxUi` intermediates are rerun sequentially when needed, so shared object/PDB contention does not produce false failures during split-work verification.

## Exit Evidence So Far

- `Common/DxUi/DxUi.vcxproj` builds successfully in `Debug|x64`.
- `Plugins/ViewerSqlite/ViewerSqlite.vcxproj` builds successfully in `Debug|x64` against the shared `DxUi` library.
- `Plugins/ViewerWeb/ViewerWeb.vcxproj`, `Plugins/FileSystem/FileSystem.vcxproj`, and `RedSalamanderMonitor/RedSalamanderMonitor.vcxproj` also build successfully in `Debug|x64` after the review-driven cleanup sweep touching optional usage, casts, and DXUI-backed resource strings.
- `RedSalamander/RedSalamander.vcxproj` builds successfully in `Debug|x64` with the issues-pane migration linked against `DxUi`.
- `RedSalamander/RedSalamander.vcxproj` also builds successfully in `Debug|x64` with `FindFilesWindow` migrated to the shared `DxUi` host/control stack.
- `PoC/DxUiTests/DxUiTests.vcxproj` builds successfully in `Debug|x64`.
- `PoC/DxUiTests/DxUiTests.vcxproj` also builds successfully in `ASan Debug|x64`.
- `PoC/ViewerSqliteTests/ViewerSqliteTests.vcxproj` builds successfully in `Debug|x64`.
- `.build/x64/Debug/DxUiTests.exe` passes, covering sort transitions, virtualization helpers, explicit visible-work metrics for million-row and wide-column datasets plus attached-host post-scroll bounded-work checks for large plain, grouped, and checkable grids, sustained repeated-scroll attached-host coverage for the large plain grid and combo popup, attached popup-scrolling stability, explicit attached-host DPI-change handling, attached simulated device-loss recovery, root-swap interaction cleanup, empty-grid hit-testing, model-clear-during-resize safety, selection preservation, focus traversal, combo focus-loss cleanup, `Alt+Down` / `Alt+Up` popup handling, flipped/clamped/widened combo popup placement, themed popup-scrollbar paging, editable combo history/dropdown behavior, capped popup scrolling, combo typeahead, editable popup filtering and filtered keyboard navigation, overlay popup paint ordering, popup-layer overlay rendering, single-line Enter-submit behavior, text/edit-combo single-line selection parity, mnemonic dispatch, mnemonic-display helper behavior, shared-tree selection/expand/typeahead/page-scroll behavior, retained-tree tick propagation, animated grid-cell detection, grid header-busy tick behavior, grid header drag-reorder in both directions with stable-id layout roundtrip coverage, and shared UI Automation root/button/toggle/text/focus plus tree/grid metadata exposure together with tree/grid selection-container semantics, visible tree-item selection and expand/collapse pattern coverage, visible grid-header fragments with `HeaderItem` and `ITableProvider::GetColumnHeaders(...)` coverage, visible grid-row selection semantics, and visible grid-cell `IGridItemProvider` / `ITableItemProvider` metadata coverage.
- `.build/x64/Debug/ViewerPETests.exe`, `.build/x64/ASan Debug/ViewerPETests.exe`, and `.build/x64/Release/ViewerPETests.exe` pass, and the real-window plugin harness now also proves `ViewerPE`, `ViewerWeb`, `ViewerImgRaw`, and `ViewerText` use mouse-input-capable DX combo hosts that activate on the first click instead of exposing a visible legacy `ComboBox`, answer `WM_GETOBJECT`, expose a traversable UI Automation subtree with a visible `ComboBox` descendant plus live combo patterns, and keep that acceptance green after rebuilding the affected viewer plugin DLLs in each tested configuration; the combo-host accessibility probe now waits and retries once for provider/subtree readiness so clean sequential `ASan Debug` runs stay green.
- `ViewerPETests` now also proves the shared viewer-shell combo-host slice survives repeated open/close churn: clean sequential fresh-process `Debug|x64`, `ASan Debug|x64`, and `Release|x64` runs reopen `ViewerPE`, `ViewerWeb`, `ViewerImgRaw`, and `ViewerText` across repeated cycles while preserving zero visible legacy `ComboBox` fallback, visible DX combo hosts, first-click activation, `WM_GETOBJECT`, and a traversable combo-host UI Automation subtree on each reopen; the no-filter `ViewerPETests` suite now uses that same fresh child-process isolation for its one-shot viewer probes because sequential in-process viewer/plugin reuse proved flaky for `ViewerImgRaw` and `ViewerText`; one-shot clean close/unload remains covered separately by the per-viewer harness.
- `.build/x64/Debug/DxUiTests.exe` also now covers observer-slot hardening so outsider controls/buttons cannot silently replace installed-root focus, capture, default-button, or cancel-button targets.
- `.build/x64/Debug/DxUiTests.exe` and `.build/x64/ASan Debug/DxUiTests.exe` now also cover provider-specific root runtime IDs so the shared accessibility root no longer reports only `UiaAppendRuntimeId`.
- `PoC/DxUiTests/DxUiTests.cpp` now also covers deterministic input-modality tracking, default/cancel routing, popup-owned escape handling, shared button/toggle/checkbox/text/combo context-menu parity across Menu-key, `Shift+F10`, and right-click routes including both non-editable and editable combo variants, Preferences-aligned button/checkbox visual-state resolution, shared-tree navigation behavior, word-navigation/delete parity, caret-animation tick behavior for DX text and editable-combo controls, IME-composition-owned bridge special-key routing, IME result-commit routing before `WM_IME_ENDCOMPOSITION`, client-anchored hidden-helper rect tracking, explicit IME composition/candidate-window anchoring to the live bridge caret including focused-control moves, RichEdit-aware CRLF selection mapping for multiline bridge sync, `Shift+Home` / `Shift+End` plus `Ctrl+Shift+Home` / `Ctrl+Shift+End` multiline selection extension, viewport-sized multiline bridge creation and resize tracking, exact no-wrap first-visible-line validation for the hidden multiline helper viewport, compact switch-only toggle-track centering, and the rule that plain hosts do not prune focus/caret state as stale when no retained root tree is installed.
- `PoC/DxUiTests/DxUiTests.cpp` now also covers shared button/toggle/checkbox hover/focus fade interpolation plus button animation tick lifetime and reduced-motion snap behavior, shared grid/tree scrollbar hover-feedback motion progress plus reduced-motion snap behavior, shared tooltip preferred-placement / edge-flip / wrap-and-clamp geometry, tooltip tracking/no-op reuse through `TestTooltipLayerTrackingUpdateReusesVisibleTooltip()` plus `TestGridTooltipTracksPointerWithinSameCell()`, shared tree explicit-tooltip plus clipped-label fallback behavior through `TestTreeTooltipTracksPointerWithinSameRow()` plus `TestTreeTooltipFallsBackToClippedItemText()`, and attached combo-popup same-item hover stability through exact popup-row geometry plus debug invalidate-count coverage; the current same-machine Release baselines for those slices are archived under `Specs/TestRuns/4cb089111a23/DxUi/2026-03-28_112644/`, `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_115439/`, `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_120940/`, `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_123346/`, `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_125116/`, and `Specs/TestRuns/c3e80704db9c/DxUi/2026-03-28_131643/`.
- `PoC/DxUiTests/DxUiTests.cpp` now also covers direct-root semantic accessibility so a root-installed shared semantic control still resolves through root child navigation, point hit-testing, and focused-control lookup instead of requiring an intermediate wrapping panel.
- `RedSalamander.exe --commands-selftest` now also proves live migrated-window `WM_GETOBJECT` provider exposure for the Preferences category host plus visible DX host sets in the Preferences shell, `ConnectionManagerDialog`, and `ManagePluginsDialog` in both `Debug|x64` and `ASan Debug|x64`.
- `RedSalamander.exe --commands-selftest` remains authoritative in `Debug|x64` and `ASan Debug|x64` for the app-window DX/UIA acceptance matrix. Non-debug app builds now reject `--selftest*` / `--commands-selftest` arguments up front with a nonzero exit instead of silently opening the normal UI, so Release validation for this milestone stays limited to the guarded PoC test binaries, real-window viewer combo-host coverage, and the Release-safe `ViewerSqlite` provider-plus-clean-close/unload path.
- `.build/x64/Debug/ViewerSqliteTests.exe`, `.build/x64/ASan Debug/ViewerSqliteTests.exe`, and `.build/x64/Release/ViewerSqliteTests.exe` pass, including deterministic coverage for source-side sorted paging.
- `.build/x64/Debug/ViewerSqliteTests.exe` now also covers real-window `ViewerSqlite` table paging plus header-sort cycling flows end-to-end, including next/previous page transitions, ascending/descending/none sort cycling on the live DX grid, and repeated in-process plugin reload/open cycles without stale window-class collisions between viewer-window tests.
- `ViewerSqliteTests` now proves the real `Plugins/ViewerSqlite.dll` window opens through the plugin factory, becomes visible, exposes no visible child controls, answers `WM_GETOBJECT`, closes cleanly, and unloads cleanly in `Debug|x64`, `ASan Debug|x64`, and `Release|x64`; clean sequential `Debug|x64` and `ASan Debug|x64` runs also prove the visible DX combo/edit/button/grid/data-row UIA subtree, visible `ValuePattern`, visible grid `SelectionPattern`, repeated row-selection churn, sustained bounded scrolling, and repeated open/close churn with zero visible child fallback on each reopen.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_fileops_issues_pane_uses_dxui_host_without_visible_child_controls` passes, proving the migrated issues pane stays on the shared DX host path with no visible child-control fallback.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_fileops_issues_pane_diff_refresh_preserves_selection` passes, proving no-op issues-pane refreshes do not advance applied refresh state and changed refreshes preserve stable-row selection.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_find_dialog_uses_dxui_host_without_visible_child_controls` passes, proving the migrated Find window is attached to `DxUi::WindowHost` and has no visible child-control fallback.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_find_dialog_directory_activation_navigates_into_selection` passes, proving Find directory activation navigates into the selected result.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_find_dialog_open_parent_keeps_directory_focused_in_parent` passes, proving `Open Parent` restores the parent folder and keeps the directory entry focused.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_find_dialog_enter_from_checkbox_invokes_default_search` passes, proving host-level default-button Enter routing survives the full DX Find window rewrite.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_find_dialog_escape_closes_popup_before_cancel` passes, proving Find combo popups close on `Esc` before host-level cancel routing.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_find_dialog_access_keys_focus_expected_fields` passes, proving Find field mnemonics route focus to the expected DX controls.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_find_dialog_grid_enter_activates_selection` passes, proving grid-focused Enter still activates the selected Find result after the DX migration.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_pane_find_dialog_action_buttons_activate_expected_commands` passes, proving the remaining Find DX action buttons keep keyboard parity on the live window: focused `Space` activation now covers Append/Intersect/Subtract/Cancel/Open/Parent through the real button route rather than debug-only search helpers.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_preferences_dialog_viewers_long_run_list_scrolling_stays_bounded` passes, proving the live `Preferences.Viewers` mappings grid now exposes bounded visible-work metrics, keeps the category tree from repainting during repeated real wheel scrolling, preserves one visible/rendered DX page host, and stays free of DX resize churn in `Debug|x64`.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_preferences_dialog_themes_long_run_list_scrolling_stays_bounded` passes, proving the live `Preferences.Themes` colors grid now exposes bounded visible-work metrics, keeps the category tree from repainting during repeated real wheel scrolling, preserves one visible/rendered DX page host, and stays free of DX resize churn in `Debug|x64`.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_preferences_dialog_plugins_custom_paths_long_run_list_scrolling_stays_bounded` passes, proving the live `Preferences.Plugins` custom-paths grid now exposes bounded visible-work metrics, keeps the category tree from repainting during repeated real wheel scrolling, preserves one visible/rendered DX page host, and stays free of DX resize churn in `Debug|x64`.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_preferences_dialog_viewers_long_run_list_scrolling_stays_bounded`, `cmd_preferences_dialog_themes_long_run_list_scrolling_stays_bounded`, and `cmd_preferences_dialog_plugins_custom_paths_long_run_list_scrolling_stays_bounded` also pass in `ASan Debug|x64`, extending the authoritative app-window bounded-scroll matrix for the newly added Preferences list surfaces beyond `Debug|x64`.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_preferences_dialog_plugins_page_exposes_live_uia_grid_selection --selftest-case=cmd_preferences_dialog_plugins_custom_paths_page_exposes_live_uia_grid_selection --selftest-case=cmd_preferences_dialog_viewers_page_exposes_live_uia_grid_selection --selftest-case=cmd_preferences_dialog_keyboard_page_exposes_live_uia_grid_selection --selftest-case=cmd_preferences_dialog_themes_page_exposes_live_uia_grid_selection` passes in both `Debug|x64` and `ASan Debug|x64`, proving the re-landed Preferences DX grids expose live `SelectionPattern`, selected `DataItem` descendants, and updated selected-row accessible names across real selection churn.
- `RedSalamander/RedSalamander.vcxproj` builds successfully in `Debug|x64` with `ConnectionCredentialPromptDialog` migrated to a DX-hosted modal shell.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_connection_credential_prompt_dxui_validation_and_accept` passes, proving the migrated credential prompt uses `DxUi`, exposes no visible child controls, validates empty user/secret fields, toggles secret visibility, and returns accepted values.
- `RedSalamander.exe --commands-selftest --selftest-case=cmd_connection_credential_prompt_escape_cancels_secret_only` passes, proving the secret-only prompt uses the DX host path, focuses the secret field first, and preserves cancel semantics on `Esc`.
- `PoC/DxUiTests/DxUiTests.vcxproj` now includes attached-window bridge coverage proving hidden helper-edit creation, `WM_SETTEXT` sync, host default-button routing through the helper, `Tab` focus traversal out of the helper, select-all sync back into the helper selection, and editable-combo exact-match sync.
- `.build/x64/ASan Debug/DxUiTests.exe` passes after the bridge viewport sizing change, so the hidden helper resize path is now covered in both normal and ASan validation.
- `RedSalamander.exe --selftest-case=sqlite_index_store_load_and_apply_journal_delta` now passes in `Debug|x64` and `ASan Debug|x64` after separating missing-volume seed state from normal delta state so missing-volume seeding cannot downgrade an ordinary journal-delta import path.

## Deferred Work

- [x] Expanded text-input bridge parity to multiline plain-text `TextField` entry with newline normalization plus attached-window selection/paste/undo/redo coverage
- [x] Expanded multiline DX text-field polish to include pointer caret placement, logical and wrapped drag-selection round-trips, and double-click word selection synchronized back into the hidden bridge state.
- [x] Expanded multiline DX text-field polish to include viewport-based `PageUp` / `PageDown` navigation plus `Shift+PageDown` selection extension.
- [x] Expanded multiline DX text-field polish to include attached hidden-bridge sync coverage for `Shift+PageUp` / `Shift+PageDown` viewport-based selection extension, including wrapped visual-line page selection inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include attached hidden-bridge caret sync coverage for wrapped visual-line `PageUp` / `PageDown` navigation inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include attached hidden-bridge caret sync coverage for wrapped visual-line `Up` / `Down` and `Home` / `End` navigation inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include attached hidden-bridge viewport sync coverage for wrapped visual-line mouse-wheel scrolling inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include wrapped double-click word-selection coverage plus hidden-bridge selection sync inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include wrapped `Shift+Click` selection extension plus hidden-bridge selection sync inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include wrapped pointer drag-selection coverage plus hidden-bridge selection sync inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include wrapped `Shift+Left` / `Shift+Right` selection extension plus hidden-bridge selection sync inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include attached hidden-bridge collapsed-caret sync coverage for `Ctrl+Home` / `Ctrl+End` document-boundary navigation.
- [x] Expanded multiline DX text-field polish to include wrapped `Ctrl+Backspace` / `Ctrl+Delete` word-delete parity plus attached hidden-bridge sync inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include explicit attached hidden-bridge caret sync for wrapped later-line pointer caret placement inside long unbroken paragraphs.
- [x] Expanded multiline DX text-field polish to include explicit attached hidden-bridge caret sync for logical multiline pointer caret placement.
- [x] Expanded multiline DX text-field polish to include explicit attached hidden-bridge selection sync with replace-through-bridge round-trips for logical multiline pointer drag selection.
- [x] Expanded multiline DX text-field polish to include explicit attached hidden-bridge selection sync for logical multiline `Shift+Click` extension.
- [x] Expanded multiline DX text-field polish to include explicit attached hidden-bridge selection sync for logical multiline `Shift+Left` / `Shift+Right` extension.
- [x] Expanded multiline DX text-field polish to include logical and wrapped `Shift+Up` / `Shift+Down` selection round-trips through the hidden bridge.
- [x] Expanded multiline DX text-field polish to include wrapped `Shift+Up` / `Shift+Down` selection round-trips through the hidden bridge.
- [x] Expanded multiline DX text-field polish to include logical and wrapped `Shift+PageUp` / `Shift+PageDown` selection round-trips through the hidden bridge.
- [x] Expanded multiline DX text-field polish to include logical and wrapped `Shift+Left` / `Shift+Right` selection round-trips through the hidden bridge.
- [x] Expanded multiline DX text-field polish to include logical and wrapped `Shift+Click` selection round-trips through the hidden bridge.
- [x] Tightened wrapped multiline double-click word-selection coverage so bridge replacement is asserted as an exact selected-range splice instead of a substring-only heuristic.
- [x] Tightened logical multiline double-click word-selection coverage so bridge replacement is asserted as an exact selected-range splice instead of a substring-only heuristic.
- [x] Tightened logical multiline `Shift+Home` / `Shift+End` and `Ctrl+Shift+Home` / `Ctrl+Shift+End` coverage so bridge replacement is asserted as an exact selected-range splice instead of hardcoded result strings.
- [x] Tightened logical multiline `Home` / `End`, `Shift+Home` / `Shift+End`, `Ctrl+Home` / `Ctrl+End`, `Ctrl+Left` / `Ctrl+Right`, and `Ctrl+Shift+Left` / `Ctrl+Shift+Right` semantic-anchor assertions so the original caret is derived from exported visible state instead of setup literals.
- [x] Tightened logical multiline `Ctrl+Shift+Left` / `Ctrl+Shift+Right` coverage so bridge replacement is asserted as an exact selected-range splice instead of hardcoded or prefix/suffix-only heuristics.
- [x] Tightened wrapped multiline `Ctrl+Shift+Left` / `Ctrl+Shift+Right` coverage so bridge replacement is asserted as an exact selected-range splice instead of hardcoded or prefix/suffix-only heuristics.
- [x] Tightened wrapped multiline `Ctrl+Shift+Left` / `Ctrl+Shift+Right` semantic anchor assertions so the original caret is derived from the exported visible state instead of setup literals.
- [x] Tightened logical and wrapped multiline `Ctrl+Shift+Left` / `Ctrl+Shift+Right` attached-host round-trips so bridge replacement is asserted from the exported visible selection range instead of bridge indices, except for the logical newline-crossing `Ctrl+Shift+Right` splice which now uses RichEdit-aware mapping.
- [x] Tightened logical multiline `Shift+Left` / `Shift+Right` coverage so hidden-bridge range alignment and bridge replacement are both asserted from the exported visible selection range instead of hardcoded indices.
- [x] Tightened logical multiline `Shift+Left` / `Shift+Right` and attached logical multiline `Left` / `Right` semantic-start assertions so the starting caret is derived from exported visible state instead of setup literals.
- [x] Tightened logical multiline double-click word-selection coverage so hidden-bridge range alignment and non-empty single-word selection checks are both asserted from the exported visible selection range instead of fixed word-boundary indices.
- [x] Tightened logical multiline `Ctrl+Left` / `Ctrl+Right` caret-sync coverage so hidden-bridge caret alignment is asserted from the exported visible caret instead of fixed word-boundary indices.
- [x] Tightened wrapped multiline `Ctrl+Left` / `Ctrl+Right` caret-sync coverage so hidden-bridge caret alignment is asserted from the exported visible caret instead of fixed wrapped word-boundary indices.
- [x] Tightened logical multiline `Home` / `End` caret-sync coverage so hidden-bridge caret alignment is asserted from the exported visible caret instead of fixed line-boundary indices.
- [x] Tightened logical multiline `Ctrl+Home` / `Ctrl+End` caret-sync coverage so hidden-bridge caret alignment is asserted from the exported visible caret instead of fixed document-edge indices.
- [x] Tightened wrapped multiline `Ctrl+Home` / `Ctrl+End` caret-sync coverage so hidden-bridge caret alignment is asserted from the exported visible caret instead of fixed document-edge indices.
- [x] Tightened logical multiline `Up` / `Down` caret-sync coverage so hidden-bridge caret alignment is asserted from the exported visible caret instead of fixed line-step indices.
- [x] Tightened logical multiline `PageUp` / `PageDown` caret-sync coverage so hidden-bridge caret alignment is asserted from the exported visible caret instead of fixed viewport-step indices.
- [x] Tightened wrapped multiline `PageUp` / `PageDown` caret-sync coverage so hidden-bridge caret alignment is asserted from the exported visible caret instead of fixed wrapped-viewport-step indices.
- [x] Tightened logical and wrapped multiline `PageUp` / `PageDown` semantic return-caret assertions so the starting caret is derived from exported visible state instead of setup literals.
- [x] Tightened wrapped multiline `Shift+PageUp` / `Shift+PageDown` coverage so wrapped page-selection anchor and return-caret assertions are derived from the exported visible caret instead of fixed starting indices.
- [x] Tightened logical multiline `Shift+PageUp` / `Shift+PageDown` coverage so page-selection anchor and return-caret assertions are derived from the exported visible caret instead of fixed starting indices.
- [x] Tightened logical and attached logical multiline `Shift+Up` / `Shift+Down` semantic-anchor assertions so the starting caret is derived from exported visible state instead of setup literals.
- [x] Tightened logical and attached logical multiline `Up` / `Down` preferred-column assertions so the intermediate-line caret checkpoint is derived from exported visible state instead of fixed code-unit indices.
- [x] Tightened logical multiline `Left` / `Right` code-unit movement assertions so the starting caret is derived from exported visible state instead of fixed setup literals.
- [x] Tightened attached logical multiline `Home` / `End` and `Ctrl+Left` / `Ctrl+Right` caret assertions so the visible line-start, line-end, and word-boundary checkpoints are derived from exported visible state instead of fixed code-unit indices.
- [x] Tightened wrapped multiline `Ctrl+Left` / `Ctrl+Right` and wrapped `Ctrl+Shift+Left` / `Ctrl+Shift+Right` caret assertions so the wrapped word-boundary checkpoints are derived from exported visible state instead of fixed code-unit indices.
- [x] Tightened attached wrapped multiline `Ctrl+Shift+Right` restart and selection-anchor assertions so the wrapped restart boundary is derived from exported visible state instead of a fixed setup literal.
- [x] Tightened logical multiline `Shift+Up` / `Shift+Down` coverage so hidden-bridge range alignment and bridge replacement are both asserted from the exported visible selection range instead of relying only on post-replacement text.
- [x] Tightened logical multiline `Shift+Home` / `Shift+End` and `Ctrl+Shift+Home` / `Ctrl+Shift+End` coverage so hidden-bridge range alignment and bridge replacement are both asserted from the exported visible selection range with RichEdit-aware newline mapping instead of line/document edge assumptions.
- [x] Tightened logical and wrapped multiline `Shift+PageUp` / `Shift+PageDown` coverage so bridge replacement is asserted from the exported visible selection range instead of hardcoded page-boundary indices.
- [x] Tightened wrapped multiline `Shift+Home` / `Shift+End` and `Shift+Up` / `Shift+Down` coverage so bridge replacement is asserted from the exported visible selection range instead of remembered caret-edge indices.
- [x] Tightened wrapped multiline `Ctrl+Shift+Home` / `Ctrl+Shift+End` coverage so bridge replacement is asserted from the exported visible selection range instead of remembered document-edge indices.
- [x] Expanded multiline DX text-field polish to include explicit attached hidden-bridge caret sync for logical multiline `Up` / `Down`, `Home` / `End`, and `PageUp` / `PageDown`.
- [x] Expanded multiline DX text-field polish to include explicit attached hidden-bridge caret sync for logical multiline `Left` / `Right`.
- [x] Expanded multiline DX text-field polish to include wrapped `Ctrl+Home` / `Ctrl+End` collapsed-caret sync plus wrapped `Ctrl+Shift+Home` / `Ctrl+Shift+End` document-boundary selection round-trips through the hidden bridge.
- [x] Expanded multiline DX text-field polish to include wrapped `Shift+Home` / `Shift+End` selection round-trips through the hidden bridge.
- [x] Expanded multiline DX text-field polish to include `Shift+Home` / `Shift+End` selection extension plus RichEdit-aware multiline bridge selection mapping.
- [x] Expanded multiline DX text-field polish to include `Ctrl+Shift+Left` / `Ctrl+Shift+Right` word-boundary selection extension plus attached bridge-selection sync coverage, following the shared next-word-start semantics already used by single-line text fields and editable combos, including long wrapped paragraphs with no explicit newlines.
- [x] Expanded multiline DX text-field polish to include `Ctrl+Left` / `Ctrl+Right` word-boundary navigation plus attached hidden-bridge collapsed-caret sync coverage, following the shared next-word-start semantics already used by single-line text fields and editable combos, including long wrapped paragraphs with no explicit newlines.
- [x] Expanded multiline DX text-field polish to include `Ctrl+Shift+Home` / `Ctrl+Shift+End` document-boundary selection extension plus attached bridge-selection sync coverage.
- [x] Expanded multiline DX text-field polish to include `Ctrl+Backspace` / `Ctrl+Delete` word-delete parity plus attached hidden-bridge sync coverage.
- [x] Expanded multiline DX text-field polish to include attached hidden-bridge logical and wrapped select-all full-range alignment plus exact replace-through-bridge round-trip coverage, with both direct select-all and keyboard `Ctrl+A` routes covered and both attached keyboard `Ctrl+A` routes explicitly asserting hidden-bridge full-range alignment.
- [x] Expanded multiline DX text-field polish to include multiline classic edit clipboard shortcut parity for `Ctrl+Insert` copy and `Shift+Insert` paste on both logical and wrapped plain-text fields.
- [x] Expanded multiline DX text-field polish to include logical and wrapped `Ctrl+Insert` no-selection copy no-op coverage.
- [x] Expanded multiline DX text-field polish to include attached logical and wrapped `Ctrl+Insert` copy coverage with hidden-bridge selection alignment.
- [x] Expanded multiline DX text-field polish to include attached logical and wrapped `Ctrl+Insert` no-selection collapsed-caret no-op coverage.
- [x] Expanded multiline DX text-field polish to include attached logical and wrapped `Shift+Insert` paste coverage with post-paste host bridge synchronization.
- [x] Expanded multiline DX text-field polish to include attached logical and wrapped `Ctrl+V` paste coverage with post-paste host bridge synchronization.
- [x] Expanded multiline DX text-field polish to make direct and attached logical multiline keyboard paste (`Shift+Insert` and `Ctrl+V`) normalize CRLF clipboard text into LF text state before the attached bridge sync path.
- [x] Expanded multiline DX text-field polish to include direct logical and wrapped `Ctrl+V` paste coverage.
- [x] Expanded multiline DX text-field polish to include direct logical and wrapped `Ctrl+C` copy coverage plus attached logical and wrapped `Ctrl+C` copy coverage with hidden-bridge selection alignment.
- [x] Expanded multiline DX text-field polish to include direct logical and wrapped `Ctrl+C` no-selection copy no-op coverage plus attached logical and wrapped `Ctrl+C` no-selection collapsed-caret no-op coverage.
- [x] Expanded multiline DX text-field polish to include direct logical and wrapped `Ctrl+X` cut coverage plus attached logical and wrapped `Ctrl+X` cut coverage with post-cut host bridge synchronization.
- [x] Expanded multiline DX text-field polish to include direct logical and wrapped `Ctrl+X` no-selection cut no-op coverage plus attached logical and wrapped `Ctrl+X` no-selection collapsed-caret no-op coverage.
- [x] Expanded multiline DX text-field polish to include direct logical and wrapped `Shift+Delete` cut coverage plus attached logical and wrapped `Shift+Delete` cut coverage with post-cut host bridge synchronization.
- [x] Expanded multiline DX text-field polish to include direct logical and wrapped `Shift+Delete` no-selection cut no-op coverage plus attached logical and wrapped `Shift+Delete` no-selection collapsed-caret no-op coverage.
- [x] Expanded multiline DX text-field polish to include direct logical and wrapped selected-range deletion through plain `Backspace` / `Delete` plus attached logical and wrapped hidden-bridge selection alignment and post-delete host bridge synchronization.
- [x] Expanded multiline DX text-field polish to include direct logical and wrapped `Backspace`-at-start and `Delete`-at-end no-op coverage plus attached logical and wrapped hidden-bridge collapsed-caret no-op coverage for those boundary paths.
- [x] Expanded multiline DX text-field polish to include native hidden-bridge `WM_COPY` / `WM_CUT` partial-selection coverage for logical newline-spanning and wrapped multiline paths, including native CRLF clipboard text on the logical newline-spanning route.
- [x] Expanded multiline DX text-field polish to include native hidden-bridge `WM_COPY` / `WM_CUT` no-selection collapsed-caret no-op coverage for logical and wrapped multiline attached hosts.
- [x] Expanded multiline DX text-field polish to include native hidden-bridge `WM_CLEAR` partial-selection deletion plus no-selection collapsed-caret no-op coverage for logical newline-spanning and wrapped multiline attached hosts.
- [x] Expanded multiline DX text-field polish to include native hidden-bridge `WM_PASTE` collapsed-caret coverage on wrapped multiline attached hosts, complementing the already landed logical and partial-selection bridge-paste paths.
- [x] Expanded multiline DX text-field polish to include native hidden-bridge `WM_SETTEXT` normalization/full-text sync coverage on logical and wrapped multiline attached hosts.
- [x] Expanded multiline DX text-field polish to include wrapped attached-window hidden-bridge undo/redo coverage through the native RichEdit bridge.
- [x] Finish multiline native hidden-bridge RichEdit message parity for `WM_SETTEXT`, `WM_COPY`, `WM_CUT`, `WM_CLEAR`, `WM_PASTE`, `WM_UNDO`, and `EM_REDO` on logical and wrapped plain-text attached hosts.
- [x] Finish multiline hidden-bridge blur/focus-loss parity so logical and wrapped attached hosts clear focus on external bridge blur and ignore descendant bridge blur within the same host.
- [x] Finish multiline hidden-bridge non-IME special-key host routing parity so logical and wrapped attached hosts route `Tab`, `Escape`, and keyboard menu-key invocation through the host while preserving the expected bridge focus/activation state.
- [x] Finish multiline hidden-bridge keyboard context-menu and wrapped-return parity so logical and wrapped attached hosts route `Shift+F10` through the host context-menu path and wrapped multiline `Enter` stays bridge-owned without invoking the host default button.
- [x] Finish wrapped multiline IME bridge parity so attached wrapped multiline hosts keep `Enter` / `Esc` / `Tab` bridge-owned during composition and resume the non-`Enter` host routing immediately on IME result commit before `WM_IME_ENDCOMPOSITION`.
- [x] Finish multiline IME mixed result/composition ownership parity so logical and wrapped attached multiline hosts keep `Enter` / `Esc` / `Tab` bridge-owned while `GCS_RESULTSTR | GCS_COMPSTR` indicates that composition data is still active.
- [x] Finish logical multiline IME composition/candidate-window re-anchoring parity so attached logical multiline hosts keep later-line IME caret anchoring correct after focused-control moves during composition.
- [x] Finish wrapped multiline IME composition/candidate-window anchoring parity so attached wrapped multiline hosts track later wrapped-line caret positions and re-anchor correctly after focused-control moves during composition.
- [x] Finish wrapped multiline hidden-helper viewport parity so attached wrapped multiline hosts resize the hidden helper to the visible wrapped field bounds and re-apply exported wrapped `firstVisibleLine` state back into the helper viewport on activation.
- [x] Finish multiline newline-entry bridge parity so logical and wrapped multiline fields accept `Enter` as newline input on direct and attached paths, keep host default-button routing suppressed, and keep hidden-bridge text/caret synchronized with RichEdit-aware newline-boundary caret mapping after insertion.
- [x] Finish multiline newline-replacement bridge parity so logical and wrapped multiline fields replace active selections with `Enter` on direct and attached paths, keep host default-button routing suppressed, and keep hidden-bridge text/caret synchronized with RichEdit-aware newline-boundary caret mapping after replacement.
- [x] Finish multiline reverse-`Shift+Tab` bridge parity so attached logical and wrapped multiline hosts route reverse focus traversal through the hidden-bridge special-key path, move focus to the previous DX control, and deactivate the bridge when focus leaves the field.
- [x] Finish multiline native `EM_REPLACESEL` bridge parity so attached logical and wrapped multiline hosts synchronize collapsed-caret insertion plus selected-range replacement through the hidden helper, and route that native helper message through the same immediate bridge-sync path as the other mutating RichEdit messages.
- [x] Finish multiline native `WM_CHAR` selected-range replacement parity so attached logical newline-spanning and wrapped partial multiline selections replace through the hidden helper, keep hidden-bridge selection alignment before replacement, and keep hidden-bridge text/caret synchronized after replacement.
- [x] Finish logical multiline hidden-helper viewport parity so attached logical multiline hosts re-apply exported `firstVisibleLine` state into the hidden helper viewport on activation, including the exact no-wrap first-visible-line path where the helper must preserve the precise imported viewport line.
- [x] Finish multiline mouse-wheel viewport parity so logical multiline fields honor accumulated high-resolution wheel deltas, wrapped multiline fields use DWrite line metrics for viewport movement, and attached logical and wrapped hosts keep the hidden helper viewport synchronized during wheel scrolling.
- [x] Finish attached multiline pointer-selection bridge parity so logical and wrapped attached multiline hosts support pointer caret placement, `Shift+Click` extension, pointer drag selection, and double-click word selection with hidden-bridge caret or selection alignment plus exact replace-through-bridge round-trips.
- [x] Finish direct multiline pointer-selection parity so logical and wrapped multiline fields support direct pointer caret placement, `Shift+Click` extension, pointer drag selection, and double-click word selection with exact replace-on-type selected-range splices on the visible DX path.
- [x] Finish multiline collapsed-caret keyboard navigation parity so logical multiline fields keep direct code-unit, line-boundary, document-boundary, word-boundary, and viewport-line movement correct, wrapped multiline fields use wrapped visual-line and DWrite-backed word or viewport movement, and attached logical and wrapped hosts keep the hidden-bridge caret aligned for `Left` / `Right`, `Up` / `Down`, `Home` / `End`, `Ctrl+Home` / `Ctrl+End`, `Ctrl+Left` / `Ctrl+Right`, and `PageUp` / `PageDown`.
- [x] Finish attached multiline selection-extension parity so logical and wrapped attached multiline hosts keep the hidden-bridge selection aligned for `Shift+Left` / `Shift+Right`, `Shift+Up` / `Shift+Down`, `Shift+Home` / `Shift+End`, `Ctrl+Shift+Left` / `Ctrl+Shift+Right`, `Ctrl+Shift+Home` / `Ctrl+Shift+End`, and `Shift+PageUp` / `Shift+PageDown`, with exact replace-through-bridge round-trips across those selection paths.
- [x] Finish direct wrapped multiline selection-extension parity so wrapped multiline fields keep direct visible-DX `Shift+Left` / `Shift+Right`, `Shift+Up` / `Shift+Down`, `Shift+Home` / `Shift+End`, `Ctrl+Shift+Left` / `Ctrl+Shift+Right`, `Ctrl+Shift+Home` / `Ctrl+Shift+End`, and `Shift+PageUp` / `Shift+PageDown` selection extension correct, with exact replace-on-type selected-range splices across those wrapped visual selections.
- [x] Finish direct logical multiline selection-extension parity so logical multiline fields keep direct visible-DX `Shift+Left` / `Shift+Right`, `Shift+Up` / `Shift+Down`, `Shift+Home` / `Shift+End`, `Ctrl+Shift+Left` / `Ctrl+Shift+Right`, `Ctrl+Shift+Home` / `Ctrl+Shift+End`, and `Shift+PageUp` / `Shift+PageDown` selection extension correct, with exact replace-on-type selected-range splices across those logical selections.
- [x] Finish direct multiline full-range select-all parity so logical and wrapped multiline fields keep visible-DX `OnSelectAll` and keyboard `Ctrl+A` routes selecting the entire text range with exact replace-on-type full-range splices.
- [x] Finish multiline clipboard and plain deletion parity so logical and wrapped multiline fields keep direct and attached copy, cut, paste, native helper `WM_COPY` / `WM_CUT` / `WM_CLEAR` / `WM_PASTE`, selected-range deletion, collapsed-caret interior deletion, and boundary no-op behavior synchronized across the visible DX state and the hidden RichEdit bridge.
- [x] Finish multiline word-navigation and word-delete parity so logical and wrapped multiline fields keep direct visible-DX `Ctrl+Left` / `Ctrl+Right`, `Ctrl+Backspace`, and `Ctrl+Delete` semantics correct, while attached logical and wrapped hosts keep the hidden bridge caret and text synchronized for those word-boundary paths.
- [x] Finish multiline undo/redo bridge parity so attached logical and wrapped multiline hosts keep hidden RichEdit undo/redo synchronized for collapsed-caret insertion, partial-selection replacement, and full-range replacement through the hidden helper.
- [ ] Finish the remaining richer multiline editing polish beyond the now-closed native hidden-bridge message parity, blur/focus-loss, non-IME special-key host-routing, multiline IME ownership, multiline IME anchor, logical and wrapped hidden-helper viewport, mouse-wheel viewport, direct and attached pointer-selection parity, collapsed-caret keyboard navigation parity, attached selection-extension parity, direct wrapped selection-extension parity, direct logical selection-extension parity, direct full-range select-all parity, multiline clipboard and plain deletion parity, multiline word-navigation and word-delete parity, newline-entry, newline-replacement, reverse-`Shift+Tab`, native `EM_REPLACESEL`, native `WM_CHAR` selected-range replacement, and undo/redo sweeps.
- [x] Record the multiline bridge validation discovery that hidden RichEdit helpers normalize CRLF in text buffers but expose logical selection positions that treat each newline as one unit; multiline regressions should prefer observable replace-on-type / paste behavior plus explicit mapping checks over raw buffer-offset assumptions.
- [x] Preferences-style `DxUi::Toggle` switch parity, including compact switch-only geometry and explicit checked/unchecked state-label support for stateful switch text
- [x] Finish reduced-motion shared-animation parity so shared-grid sort-glyph transitions plus spinner, header-busy, and indeterminate marquee animation degrade to static presentation while focused text-field and editable-combo caret animation remains active.
- [x] Finish shared grid/tree scrollbar hot-state feedback parity so hover and drag tinting stays aligned across both surfaces and stale hot state clears on leave or drag release.
- [x] Finish shared animation tick-request parity so retained panels propagate descendant animation ticks correctly and shared-grid tick requests stay limited to actual animated states, including sort-glyph transitions, drawable header-busy indicators, and indeterminate marquee cells while text-only and determinate-progress cells remain static.
- [ ] Finish the remaining broader shared-control animation polish beyond the now-closed reduced-motion gating parity, scrollbar hot-state parity, animation tick-request parity, lightweight hover/focus fades, and shared grid/tree scrollbar fade refinements; any remaining work is now limited to later window-specific motion polish where it materially improves legibility without violating reduced-motion expectations.
- [x] Finish shared tree interaction and visual foundation parity so the shared navigation control covers pointer selection, expansion/collapse, keyboard parent/child traversal, typeahead, page-scroll, wheel-scroll, pointer and keyboard context menus, icon/badge layout, focus fallback, rainbow-mode row tinting, and scrollbar hot-state feedback with deterministic regression coverage.
- [x] Finish shared tree high-resolution wheel-delta parity so the shared navigation control accumulates partial mouse-wheel deltas instead of treating every non-zero wheel event as a full three-row scroll.
- [x] Finish Preferences category-tree high-resolution wheel-routing parity so the shell-level wrapper path proves routed half-wheel deltas accumulate on the live DX host without changing the active category or page title.
- [x] Finish Preferences category-tree reverse keyboard-navigation parity so the live wrapper path proves reverse one-step `VK_UP` navigation and `VK_HOME` remain on the shared DX tree path without leaking to dialog navigation or losing page-title/category tracking.
- [x] Finish Preferences category-tree page-navigation parity so the live wrapper path proves `VK_NEXT` / `VK_PRIOR` stay on the shared DX tree path, move the visible tree selection, and leave the page-scroll host position unchanged while the shell title stays synchronized.
- [x] Finish Preferences plugin-root left/right navigation parity so the live wrapper path proves `VK_RIGHT` enters the first plugin child, `VK_LEFT` returns to the expanded root, `VK_LEFT` collapses the subtree there, and `VK_RIGHT` re-expands it while the shell title/description and visible DX page surface stay synchronized.
- [ ] Finish the remaining tree/navigation parity such as navigation-shell-specific polish beyond the now-closed shared tree foundation, shared tree high-resolution wheel-delta sweep, Preferences category-tree high-resolution wheel-routing parity, Preferences category-tree reverse keyboard-navigation parity, Preferences category-tree page-navigation parity, and Preferences plugin-root left/right navigation parity.
- [ ] Advanced grouped/checkbox/icon parity for broader app replacement
- [ ] Post-pilot window migration waves tracked in `Specs/Plans/WIP/UI_DxUiWindowMigrationPlan.md`
