# Shared Helper Reuse Contract

## Status and scope

This is the authoritative repository contract for reusable implementation primitives. It applies to production
code, plugins, RedConfigure, first-party test executables, and in-product selftests. Domain specs remain
authoritative for the behavior of a particular feature; this document identifies the canonical implementation
owner and prevents a consumer from silently recreating an already shared policy.

## Mandatory reuse rule

Before adding a file-local or component-local helper:

1. Search this catalog, `Common/`, and, for test infrastructure, `Tests/TestSupport/`.
2. Reuse the canonical API when its semantics match. A rename, forwarding wrapper, or minor syntactic rewrite
   is still a duplicate and must not be added.
3. If a generally reusable capability is missing at the same dependency layer, extend the canonical helper and
   its focused tests instead of copying it into a consumer.
4. Keep a local variant only when it deliberately differs in policy, dependency layer, ABI, ownership, error
   mapping, or measured hot-path constraints. Its name and definition comment must state the difference, and
   the variant must appear in the relevant source-contract allowlist or focused test.
5. When a new canonical helper is introduced, update this catalog and the authoritative domain spec that owns
   the behavior. Add or extend a source-contract test when textual or structural regression could recreate the
   removed copies.

Similar names do not by themselves establish semantic equivalence. In particular, strict and replacement UTF
conversion, fully absolute and merely drive-qualified paths, JSON coercion policies, stable hash algorithms,
and product-specific color contrast thresholds remain distinct until their contracts are proven identical.

## Canonical implementation catalog

| Concern | Canonical owner | Required reuse boundary |
|---|---|---|
| ARGB to Direct2D color | `DxUi::ColorFromArgb` in the public `<DxUi/DxUi.h>` | All code already depending on DxUi; do not add another `ColorF`/ARGB unpacker there. |
| `ARGB`/`COLORREF`, compositing, and luminance math | `ColorRefFromArgb` and `Common::Colors` in `Common/Helpers.h` | Reuse the numeric conversion, `CompositeArgbOverOpaqueBackground`, and luminance math; keep product contrast thresholds and translucent-backdrop policy explicit at the caller. |
| Application and folder DxUi palettes | `MakeAppThemeDxPalette` / `MakeFolderContentDxPalette` in `RedSalamander/DxUiThemePalette.h` | RedSalamander application surfaces; viewer-only themes use their viewer contract instead. |
| Throughput stream hue colors | `DxUi::ThroughputGraphColorFromHue` in the public `<DxUi/DxUi.h>` | Throughput graphs and related UI that represents the same identified stream must reuse the canonical normalized hue conversion so saturation and brightness remain identical. Stream identity and hue assignment remain consumer-owned. |
| Primary plus verification throughput history | `DxUi::ThroughputGraph::SetSamples` and `SetSecondarySamples` in the public `<DxUi/DxUi.h>` | File Operations and any future two-stage transfer UI use the primary history for aggregate/pump throughput and the secondary history for the visually distinct verification series. Both inputs are bounded to `ThroughputGraphSample::kMaxSamples`; the control owns scaling, reduced-motion behavior, High Contrast rendering, and invalidation. |
| Directional and disclosure chevrons | `DxUi::DrawChevronGlyph` and `DrawDisclosureChevron` in the public `<DxUi/DxUi.h>` | DxUi ComboBox, Grid sort/group headers, trees, and app-owned DxUi cards/groups must use the Segoe Fluent/Unicode glyph path instead of recreating chevrons from line or triangle geometry. `DrawDisclosureChevron` owns the animated collapsed-to-expanded rotation; fixed directions use `DrawChevronGlyph`. |
| DirectWrite glyphs in HDC-backed Direct2D paint | `D2DHdcPaint::Session::DrawCenteredGlyph` in `RedSalamander/D2DHdcPaint.h` | App-owned HDC interop seams that need a symbol glyph must use the cached DPI-sized Segoe Fluent/Unicode DirectWrite path rather than GDI text or private line geometry. Pure shape painting remains on the existing fill/line primitives. |
| Passive DxUi transient-surface material | `DxUi::CaptureTransientSurfaceBackdrop`, `DxUi::TransientSurfaceBackdrop`, and `DxUi::PaintTransientSurface` in the public `<DxUi/DxUi.h>` | Small owned passive status popups depending on DxUi must keep DWM system backdrop disabled and reuse the shared bounded app-backdrop capture/cache plus clipped blur, fill, contour, inner-rim, shadow, pressed, material-opacity, and High Contrast policy. Capture timing, placement, animation/transforms, content, hit testing, and lifetime remain consumer-owned. Modal alert/scrim surfaces with distinct behavior are not forced through this helper. |
| DPI, popup geometry, and owner centering | `Common/WindowSizing.h` | Reuse conversions, bounds, popup placement, minimum-track sizing, and centering at Win32 consumers. |
| UTF conversion and validation | `Common/StringConversion.h` | Choose the named strict or replacement API; never hide the malformed-input policy in a local converter. Use `Common::Strings::IsValidUtf8Strict` when a bounded hot path needs validation without allocating a UTF-16 result, and defer conversion to the owning consumer thread. `TryUtf16FromUtf8Strict(text, output)` reuses caller-owned string capacity, clears output on failure, and returns true for valid empty input; input must not alias output storage. The optional-return overload delegates to the same strict implementation. |
| yyjson reads, ownership, and object rewrites | `Common/YyjsonHelpers.h` | Reuse document owners and policy-parameterized accessors. Use `ParseObjectDocument` for transactional object-root parsing and `WriteObjectWithoutMembers` for migration/scrubbing rewrites; schema-specific missing/null/coercion policy stays explicit. Numeric members are read through `GetDoubleMember` / `GetInt64Member` / `GetUInt64Member`, never a bare `yyjson_get_real` or `yyjson_get_int`: yyjson models integer-versus-real as a storage subtype, so `yyjson_get_real` returns `0.0` for a JSON integer even though `14` and `14.0` denote the same number. `GetDoubleMember` accepts sint/uint/real uniformly and reports magnitudes that cannot round-trip the 53-bit mantissa as `OutOfRange`. A consumer needing a different boolean encoding (for example the `"0"`/`"1"` strings a schema `option` field stores, which `GetBoolMember` rejects) documents that difference at its local variant; `Plugins/Terminal`'s `ReadBoolean` is the reviewed example. |
| Plugin configuration schema parsing, defaults, and serialization | `Common/PluginConfiguration.h` (`ParseSchema`, `ParseConfiguration`, `MakeDefaultValue`, `SerializeConfiguration`) | Editors, plugins, and conformance tests share one definition of a schema field's declared default. Use `MakeDefaultValue` rather than reading `Field::default*` directly, so "the schema's own defaults" means the same thing in the Preferences page and in `Tests/PluginContractTests`. `SerializeConfiguration` writes current values over the original object, preserving unknown members in their original order. |
| Windows path classification, Local mutation admission, normalized local containment, and unique files/siblings | `Common/PathUtils.h` (`IsSupportedLocalFileOperationPath`, `BuildUniqueSiblingPathCandidate`, `CreateUniqueFileInDirectory`, `CreateUniqueSiblingFile`) | Select the exact drive-qualified/absolute/UNC/device predicate, normalized local Windows path comparison/containment helper, and shared unique primitive. Local mutation entry points use `IsSupportedLocalFileOperationPath` to admit ordinary absolute drive/UNC and volume-GUID paths while rejecting relative, incomplete, Win32 device, NT object-manager, and `GLOBALROOT` namespaces; this lexical envelope never replaces no-follow object binding. `BuildUniqueSiblingPathCandidate` constructs provider-neutral `name (N).ext` / `folder (N)` candidates without probing; the caller owns provider existence checks and the ordinal bound. `CreateUniqueFileInDirectory` and `CreateUniqueSiblingFile` own bounded local `CREATE_NEW` collision retry and publish the path only with the owned handle, so an existing object is never reopened or truncated. The directory form normally generates GUID tokens; its candidate-factory overload is the deterministic collision/exhaustion test seam and rejects empty tokens or separators. Callers still own directory trust/no-follow validation and must choose lexical versus physical normalization explicitly. |
| Provider path identity, parent/leaf/relative derivation, joining, and structural containment | `Common/FileSystemPathIdentity.h` | File Operations, Batch Rename, Compare, and provider-path consumers must use `EquivalentPath`, `TryGetFileSystemParentPath`, `TryGetFileSystemLeafName`, `TryGetFileSystemRelativePath`, `JoinFileSystemPath`, and `IsStrictDescendantPath` with the admitted path profile. The relative helper returns an empty path for the root itself and rejects paths outside that root. Never use host `std::filesystem` separator/case rules as mutation, focus, refresh, cache, or sparse-mapping authority for a provider path. Exact object identity still requires provider binding. |
| Typed filesystem route, namespace, name, peer, and failed-mutation classification | `FileSystemRouteContract` in `Common/FileSystemRouteContract.h` | All production route/path-identity consumers query `IFileSystemRouteCapabilities` through this helper. It validates record prefixes, enums, strict booleans, route/watchdog consistency, full provider/profile/root identity, bounded arenas and returned pointers, copies all provider strings immediately, maps typed name identity, and returns only `Available`, `Unsupported`, or `ContractViolation`. Its peer/name/join/collision helpers use the same fail-closed rules, and its failed-mutation classifier returns only `Unsupported`, `RetryableNoCommit`, `FailedKnown`, `Indeterminate`, or `ContractViolation`. Capability JSON remains diagnostic and must not be parsed as executable authority. Provider-side ABI copying and name mechanics reuse `FileSystemRouteCapabilitiesBase` in `Common/FileSystemRouteProviderBase.h`; providers retain ownership of descriptor facts. |
| Provider discovery scope accounting and closure | `Common::FileOperations::DiscoveryScope` in `Common/FileSystemDiscoveryScope.h` | Every provider that owns a governed transfer or removal reports through this scope instead of assembling `FileSystemDiscoveryProgress` by hand. It owns cumulative-per-call counters, the single `traversalClosed` report, closure on every exit including error and cancellation, serialized emission so concurrent workers cannot deliver a stream that goes backwards, and the rule that an unknown size still counts the item with no bytes. The caller keeps what a file, a directory and queued work mean in its namespace, plus cancellation, progress and bandwidth. The host bridge keeps its own reporter because it must also suppress nested scopes and stage a provisional closure, which are host policies rather than provider accounting. |
| File Operations traversal and discovery reservation policy | `Common/FileOperationTraversalPolicy.h` | The host bridge and Local recursive Copy must share the 128-depth, 4,096-entry, 16-MiB retained-path, 8-MiB metadata, and 32/128/256 discovery ceilings plus `DiscoveryQueueTarget` and `DiscoveryWorkerLimit`. Queue ownership, cancellation, provider I/O, and worker lifecycle remain layer-specific. |
| Exact owned-stage cleanup options | `MakeOwnedStageCleanupOptions` in `Common/PlugInterfaces/FileSystem.h` | File Operations host/provider compensation must reuse the canonical snapshot before `AbortOwnedObject`: it preserves compatible primary options, removes the primary operation-control pointer and cookie, and installs a fresh saturated two-second deadline. Use it only to remove an exact object already owned by the operation; it never authorizes resumed primary work or pathname-based cleanup. |
| Streaming BLAKE3 digest state | `Common::Crypto::Blake3Hasher` in `Common/Blake3Digest.h` | File Operations host readback and provider exact-object proof must reuse the canonical byte-order-preserving BLAKE3 state. The caller owns exact-object binding, reader I/O, cancellation, progress, bandwidth, and result classification; hashes never authorize overwrite, skip, cleanup, or deletion. |
| URI percent encoding | `Common/UriEncoding.h` | Select an explicit slash policy; no local byte encoder. |
| Bounded handle I/O | `Common/HandleIo.h` | Reuse exact read, total write, rewind, and bounded-size helpers, including zero-progress failure behavior. |
| Bounded registry strings | `Common::Registry::ReadBoundedStringValue` in `Common/RegistryUtils.h` | This is a bounded policy adapter, not a replacement for `wil::reg`: use WIL for key ownership, open/create, iteration, and ordinary typed reads. Use this adapter only when a caller must cap allocation before a registry string read and enforce exact query/read type and byte stability, embedded-null, expansion, trimming, empty-value, and bounded-retry policy. WIL's allocating string getter cannot enforce the pre-allocation cap. Do not construct a string from an unbounded or assumed-null-terminated registry buffer. |
| WSL distribution discovery | `Common::Wsl::EnumerateDistributions` in `Common/WslDistributionCatalog.h` | App and plugin consumers must use the canonical bounded catalog for distribution name, GUID, base path, WSL version, utility-distribution exclusions, and deterministic case-insensitive ordering. The `HKEY`/subkey overload is the volatile-registry test seam; product callers use the current-user overload. |
| Cloud continuation paging | `Common::Paging::ContinuationGuard` in `Common/PaginationGuard.h` | Cloud/provider pagination must reuse the page/item/byte/deadline/cancellation and repeated/empty-continuation guard. Provider request, token parsing, item semantics, and diagnostics remain local. |
| Local regular-file output transactions | `Common::Files::LocalFileTransaction` in `Common/LocalFileTransaction.h` | Local writers that need unique sibling creation, complete/zero-progress-safe writes, flush, optional size verification, same-volume fail-if-exists or replace promotion, and optional exact finalized-file identity returned from `Commit`. Replace promotion first uses handle-based `FileRenameInfoEx` with replace-if-exists and POSIX semantics so delete-sharing readers may retain the immutable old generation while the path atomically names the new generation; unsupported filesystems fall back to the established `MoveFileExW(..., MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH)` path. Provider-backed writers retain their provider transaction contract. |
| Delete-on-close temporary files | `Common::Files::CreateDeleteOnCloseTemporaryFile` in `Common/DeleteOnCloseTemporaryFile.h` | Curl, S3, Microsoft Drive, and future compatible handle-backed consumers must reuse the RAII reservation helper. The caller supplies its prefix, access/share mode, and file flags; the helper owns the `GetTempFileNameW` reservation until a valid WIL handle with `FILE_FLAG_DELETE_ON_CLOSE` takes over, so reopen failure cannot strand the pathname. Process-visible selected-path manifests intentionally use a separate launch-lifetime lease because a delete-on-close handle cannot be transferred to an arbitrary external process; they use `CreateUniqueFileInDirectory` under their private validated app-data root and stream through `Common::HandleIo::WriteAll`, never the shared `%TEMP%` reservation pattern. |
| Connection profile identity | `Common::Settings::CreateConnectionProfileId`, `NormalizeConnectionProfileId`, and `ValidateConnectionProfileIds` in `Common/SettingsStore.h` | Generate canonical lowercase profile GUIDs and enforce reserved/duplicate identity rules before Settings Store load, UI commit, save, WinCred target construction, or authorization-cache use. Do not add another local GUID formatter for connection profiles. |
| Throughput parsing/formatting | `Common/ThroughputParsing.h` | Reuse the shared binary-throughput grammar; broader UI option parsing remains local. |
| Windows argv argument quoting | `Common::Process::QuoteWindowsCommandLineArgument` in `Common/ProcessCommandLine.h` | Windows `CreateProcessW` callers that need the `CommandLineToArgvW`/MSVC inverse grammar must reuse the always-quoted canonical argument primitive. Executable selection, token validation, total command-line bounds, shell syntax, and process policy remain caller-owned. |
| File metadata display | `Common/FileMetadataFormatting.h` | Reuse normalized fields and an explicit display profile. |
| Unicode clipboard writes | `Common::Clipboard::TrySetUnicodeText` in `Common/UnicodeClipboard.h` | Reuse the direct WIL-owned Unicode allocation/publication path, bounded non-reentrant `OpenClipboard(...)` retry, and explicit empty-text policy. The payload is prepared before `EmptyClipboard` and released only after successful `SetClipboardData`; do not add leaf forwarding wrappers for individual Win32 calls. |
| Commands clipboard fixture quiescence | `WaitForClipboardQuietForSelfTest` in `RedSalamander/SelfTest/Commands/Commands.SelfTest.Settings.cpp` | Test setup waits for an unchanged clipboard sequence and no open owner for 250 ms, then probes availability; the total wait is bounded to three scaled seconds. Clear/seed operations use it before the single product action. It never retries product commands or changes DxUi clipboard policy. |
| Viewer file-combo hosting | `Common/ViewerFileComboHost.h` | Reuse the proven popup-opening, keyboard, subclass, and height behavior in compatible viewers. |
| Viewer title-bar theming | `Common/ViewerTitleBarTheme.h` | Reuse viewer DWM attribute and accent resolution; app-level tool windows follow their app theme contract. |
| App modal DxUi shell | `Common/ModalWindowShell.h` | Reuse top-level modal lifetime, nested-loop, owner restoration, and teardown behavior; content remains local. |
| Focused/inactive pane opacity policy | `Common::PaneVisualState` in `Common/PaneVisualState.h` | FolderView and other pane content use the canonical normal-text, icon, focus-border, and full-content background-overlay opacity rules. High Contrast consumers must suppress the full-content inactive overlay. Selection colors and control-specific paint order remain consumer-owned. |
| FolderView context-menu resource IDs | `Common/FolderViewMenuIds.h` | The host resource compiler and ViewerSpace clone share this numeric-only contract for `IDR_FOLDERVIEW_ITEM_CONTEXT` and `IDM_FOLDERVIEW_CONTEXT_*`. Consumers include the header instead of copying numeric IDs; labels, menu structure, command state, and dispatch remain owned by the host/viewer layers. |
| HWND Direct2D target lifecycle | `Common/HwndRenderTargetResources.h` | Reuse the factory/target/brush lifecycle where the exact FunctionBar/StatusBar contract applies. |
| Callback registration and module pins | `RegistrationCallbackState<T>` and `TransferModulePinToCallbackReturn` in `Common/Helpers.h` | Reuse callback generation/drain and callback-return module-pin transfer; cancellation/session policy remains explicit. |
| Packed plugin `FileInfo` buffers | `Common::Plugins::PackedFileInfoBuffer`, `PackedFileInfoCursor`, and `LocatePackedFileInfoRecord` in `Common/PackedFileInfoBuffer.h` / `Common/PlugInterfaces/FileSystem.h` | Buffered plugin facades use the checked/aligned owner. Every consumer of an ABI-provided record chain uses the canonical bounded cursor/locator before reading record fields; streaming and purpose-built fixture models remain explicit producer exceptions, not validation exceptions. |
| Plugin configuration schema/codec | `Common/PluginConfiguration.h` and `Common/Common/PluginConfiguration.cpp` | Reuse the common schema model, validation, parse, and write behavior; provider policy is expressed through the model. |
| Process-wide libcurl lifetime | `Common::CurlRuntime::ProcessLease` in `Common/CurlProcessRuntime.h` | Every independently unloadable DLL that uses libcurl owns one lease. Initialize through the lease, reach the plugin quiet point before releasing it, and let only the final process participant call `curl_global_cleanup`; never add a DLL-local global cleanup. |
| Posted payload ownership/coalescing | `PostMessagePayload`, `TakeMessagePayload`, `InitPostedPayloadWindow`, `DrainPostedPayloadsForWindow`, and `TakeAndCoalesceContiguousPostedPayloads` in `Common/Helpers.h` | A nonzero `lParam` is a process-unique opaque registry token, never a payload pointer. Receivers must use `TakeMessagePayload<T>` and ignore a null result from a nonzero stale/drained/type-mismatched token before message-specific work. Zero is reserved for an explicitly defined payload-less fallback. Registered windows initialize on create and drain on `WM_NCDESTROY`; the drain fences new posts, invalidates tokens, and deletes each registered payload exactly once. Stale queued tokens may survive numeric HWND reuse but cannot reacquire storage. Reuse queue-head-safe keyed contiguous draining; message-specific merge semantics stay in the caller. |
| RedConfigure binary reads | `RedConfigure/RedConfigureBinaryFile.h` | All RedConfigure bounded binary-file consumers; it is intentionally RedConfigure-local because of the dependency boundary. |
| Test sandbox authorization and environment scopes | `Common/TestSandboxPath.h`, `Tests/TestSupport/TestSupport.h`, native `AcquireTestSandbox` / `SelfTest::RemoveAll`, and `Tools/Modules/Testing/TestSandbox.psm1` (through `TestRunPlan.psm1`) | Reuse the canonical normalization, repository containment, fixed-local dedicated-root, filesystem-capability, ownership-marker, and existing-component reparse checks before any test-path mutation. First-party tests use the shared run/scratch/artifact layout; environment variables request paths but never authorize arbitrary locations. `Get-RSToolingPesterSandboxRoot` retains the repository boundary when native suites use an external short root; marker-validated cleanup removes only the exact run. Native no-follow cleanup unlinks junction fixtures rather than traversing them. |
| Documentation window screenshots | `RedSalamander::TestSupport::SaveWindowScreenshot` in `Tests/TestSupport/WindowScreenshot.h` | Capture only the test process's visible, non-minimized HWND into its marker-authorized sandbox. Preserve the complete D2D/DirectComposition surface and chrome; use named scenarios under the existing activation guard, record provenance, and fail rather than taking a desktop or partial-layer fallback. |
| Test message/snapshot polling | `PumpMessagesUntil` and `WaitForSnapshot` in `Tests/TestSupport/TestSupport.h` | Reuse bounded polling and diagnostics; fixed sleeps are not a replacement for observable readiness. |
| Noninteractive GUI-test activation blocking | `RedSalamander::TestSupport::ScopedWindowActivationBlocker` in `Common/TestWindowActivationGuard.h` | GUI test executables and in-product selftests whose contract excludes real focus must hold this UI-thread CBT guard across window creation and execution. It adds `WS_EX_NOACTIVATE` to top-level test windows and rejects attempted activation/focus transfers. Tests that genuinely validate foreground, caret, menu, pointer, or keyboard routing must run without the guard and retain their interactive-desktop plan marker. |
| Contained child processes | `RunChildProcess` in `Tests/TestSupport/ChildProcess.h` | Reuse structured arguments, concurrent bounded output drains, timeout handling, and kill-on-close Job containment. |
| Guarded tooling output paths and file publication | `Resolve-RSGuardedRepositoryPath` and `Publish-RSGuardedRepositoryFile` in `Tools/Modules/Build/ArtifactOperationLock.psm1` | Normalize caller-selected tooling destinations beneath one exact repository-relative root, reject every existing reparse component, create missing directories segment-by-segment when requested, and revalidate after creation. Final file publication uses a validated same-directory staged file and atomic move/replace so a failed publication preserves the prior complete destination. Packaging callers must use these helpers instead of lexical prefix checks or force-overwrite commands. |
| Directed desktop-input warning | `Tests/TestSupport/DirectedSelfTestInputWarning.h` | Reuses the original Commands warning across Commands, DxUi Menu/NativeTextInput and interactive ViewerPE/ViewerSqlite cases. A suite guard owns one large, DPI-scaled, centered, non-activating, click-through surface with a three-second lead; nested input guards borrow it. No-activation lanes and case listing remain silent. Every guarded path also restores cursor/focus state on all exits. `TryActivateOwnedWindow` applies the established Commands foreground-queue activation technique only with a visible shared warning and a process/UI-thread-owned target; the opt-in FileOps interaction gallery uses it. |
| Terminal evidence directory identities and frozen host headers | `Get-RSDirectoryIdentity` and `Resolve-RSTerminalGate0HostHeaderRoot` in `Tools/TerminalEngine/TerminalEngineLifecycle.psm1` | Terminal evidence tooling must reuse the ordinal, case-fold collision-safe complete-tree identity. Harness/probe callers must use the frozen-host-header resolver so every existing component from the filesystem root through the explicit include root and exact-case `wil/resource.h` is non-reparse and the complete tree matches its expected SHA-256; candidate or provider code may not substitute an ambient vcpkg tree, a prior expansion, or a file-only check. |

The pure `FileSystemRouteContract::IsValidItemMutationResultPrefix`,
`SnapshotItemMutationResult`, and `ClassifyFailedMutation` helpers are inline in the
canonical header so host, provider-internal decisions, and test callback captures
share validation and bounded ABI copying without linking provider DLLs to host
route-query machinery. Never copy an advertised old prefix with full-struct assignment.

## Reviewed domain-local variants

These entries document deliberate local ownership under the mandatory-reuse rule;
they are not Common APIs. A second compatible consumer requires a fresh review
and, when semantics truly match, extraction into the canonical catalog above.

| Concern | Deliberate local owner | Required boundary and reason |
|---|---|---|
| IMAP UID/FETCH response framing | `TryConsumeImapUntaggedFetchResponse` and the borrowed quoted/literal/list helpers in `Plugins/FileSystemCurl/FileSystemCurl.Imap.cpp` | IMAP byte-counted literals and nested wire values differ from FTP rows, shell strings and argv parsing. UID enumeration skips unsolicited data responses with the same borrowed quoted/literal/list framing and validates complete nonzero unsigned-32-bit tokens. The summary parser shares these iterative, constant-stack helpers with envelope decoding, reuses the provider's `EqualsAsciiIgnoreCase`, and accepts metadata only from complete top-level attributes. Do not restore substring extraction or copy opaque payloads for framing. Literal offsets use remaining-byte bounds; malformed framing stops that response without resynchronizing into message data. The listing fixture covers malformed, reordered, nested, quoted/literal and partial-summary cases; the IMAP spec owns the grammar and fallback contract. |
| FTP/SFTP namespace command operands | `FileSystemCurlInternal::RemotePathForCommand` in `Plugins/FileSystemCurl/FileSystemCurl.Shared.cpp` | All native namespace commands share literal-path joining and canonical strict UTF conversion. FTP control-line operands and libcurl SFTP quoted operands differ from URLs, shell strings, and Windows argv; reject CR/LF/NUL and invalid Unicode, preserve percent sequences, and never reuse URL escaping or a generic process-argument helper. |
| Native FTP/SSH incremental listing cursor | `FileSystemCurlInternal::CurlDirectoryCursor` in `Plugins/FileSystemCurl/FileSystemCurl.Internal.h` / `.Shared.cpp` | Native Copy/merge, Delete, Move source-commitment preflight, and directory-size traversal (including its root lookup) reuse this worker-owned, bounded libcurl multi/pause cursor instead of the buffered FolderView listing facade. It shares the provider's Unix/DOS parsers, pool, operation control, and watchdog policy, but deliberately rejects ambiguous/malformed rows and never replays emitted entries. FTP/SSH transport framing and borrowed-handle lifetime differ from cloud continuation paging and Local directory handles; do not move this transport owner into `Common` or duplicate it in another Curl walker. |
| Curl size progress/transport cancellation | `CurlDirectorySizeControl` in `Plugins/FileSystemCurl/FileSystemCurl.DirectoryOps.cpp` | This synchronous raw-vtable adapter borrows the size result/callback/cookie and feeds existing Curl operation control. It owns 100-entry/200-ms coalescing, progress-before-cancel order, and sticky callback failure without concurrent or post-return callbacks. Size counters and final-total reporting differ from mutation progress; do not duplicate it in a Curl protocol-specific size walker. |
| Native Curl connection-failure diagnostics | `RecordCurlConnectionFailure` in `Plugins/FileSystemCurl/FileSystemCurl.Shared.cpp` | Cursor completion, targeted size probing and namespace quote dispatch share this numeric-only, per-failed-attempt capture before borrowed-handle reset. It depends on live libcurl easy-handle state, unlike Common HRESULT/HTTP helpers. Keep Curl/OS codes, availability and numeric ports/reply code; never retain URLs, profiles, credentials, reply text or error buffers. It does not change mapping, cancellation, retries or mutation authority. The bound-only loopback control and archived metrics cover all three call sites. |
| Test-only Curl allocation observations | `ProbeCurlEphemeralBindForSelfTest` and `ProbeCurlLoopbackConnectForSelfTest` in `Plugins/FileSystemCurl/FileSystemCurl.SelfTest.Ftp.cpp` | Unlike the Common HTTP fixture, these synchronous probes have no accept worker or protocol traffic. They observe a fresh wildcard bind or one bounded nonblocking connect to a fresh owned loopback listener, close all sockets and preserve the caller's Winsock error. Reuse the existing local listener factory; never connect to the failed peer, change machine/reuse policy or grant retry authority. The three exact-lookup controls and failure-only archived observations cover this deliberate test-local boundary; see `Testing_PerformanceValidation.md`. |
| Curl descendant size error policy | `IsRecoverableCurlSizeChildFailure` in `Plugins/FileSystemCurl/FileSystemCurl.DirectoryOps.cpp` | Curl-specific HRESULT transport mapping differs from Local Win32 enumeration errors. Only listed recoverable descendant errors become partial totals; root, structural, authentication, overflow, cancellation, and callback failures remain global. The caller must exclude sticky callback failures before applying this policy. |
| File Operations durable state-file mechanics | `FileOperationDurableStore` in `RedSalamander/FileOperationDurableStore.h` | Retained app-local File Operations state such as the interrupted-Move breadcrumb reuses configured byte/direct-entry limits, final-root no-follow validation and identity stability checks, handle-based direct-child enumeration, bounded exact reads opened with replace-compatible read/write/delete sharing, `LocalFileTransaction` exclusive/replace publication, and exact-handle removal constrained to a direct child of the configured root. Every direct child consumes the enumeration cap; regular non-reparse files and rejected directory/reparse entries are returned separately so each schema owner can count canonical rejected siblings independently. Read and acknowledgement reject a final reparse entry. Replace publication swaps the final directory entry without following it, so a planted junction/symlink cannot redirect the write and its target remains untouched. Schema JSON/versioning, writer serialization, and Move count/pane notice projection remain in their separate owners. Batch Rename does not use this helper and its retired journal root is not enumerated. The helper contains no parser, journal, claim, recovery, or UI action policy. Compatible non-File-Operations consumers require a fresh review before this app-local surface moves to `Common`. |
| Terminal bounded asynchronous input queue | `Terminal::writeInput`, `Terminal::InputDrainCallback`, and `Terminal::drainInput` in `Plugins/Terminal/Terminal.cpp` | Keep the plugin-local mutex/queue, 3,840-request and 8 MiB admission bounds, secure clearing, one scheduled threadpool drain, COM-object lifetime, and callback-return module pin together. Ghostty input ownership and plugin-unload semantics differ from a generic work queue. The EXE, `Common`, and other plugins must not depend on it or recreate a weaker leaf helper. |
| App command/shortcut catalogue | `ShortcutCommandCatalog` in `RedSalamander/ShortcutCommandCatalog.{h,cpp}` | The binding-centric Helper and command-centric global palette MUST share this aggregation, localized metadata, enabled-state, keyword, visual-ID, and alias projection. It deliberately remains EXE-local because it depends on `CommandRegistry`, app resources, and live host context; plugins and `Common` must not depend on it. A new app command surface must reuse this catalogue instead of building another command list. |
| App window placement persistence | `WindowPlacementPersistence` in `RedSalamander/WindowPlacementPersistence.h` | App-owned top-level windows, including Main, Preferences, Find, Batch Rename, Compare Directories, Connection Manager, Shortcuts, Item Properties, and the singleton floating Terminal, MUST reuse the monitor-identity capture/restore and full-visibility clamping policy. A consumer-specific minimum size is applied before the final visibility clamp so restoring a too-small or off-screen record cannot move required chrome outside the monitor work area. It remains EXE-local because it operates on the app settings model and app-owned top-level HWND lifecycle. Viewer/plugin-owned placement remains under its own ABI/settings boundary. |
| Selected-path manifest serialization | `FileActionLauncher` in `RedSalamander/FileActionLauncher.{h,cpp}` | This remains EXE-local because it combines app-data ownership, file-action macro context, process-launch lifetime, localized launch failure, and test observations. It accepts a span over the existing selection, validates the complete record set before creating anything, then streams the exact UTF-16LE BOM/path/CRLF grammar through `Common::HandleIo::WriteAll` with checked accounting and a bounded 64 KiB buffer. Compatible generic file creation still reuses `Common::Paths::CreateUniqueFileInDirectory`; another app-visible manifest consumer requires a fresh ownership/lifetime review rather than copying this policy. |
| Selected-path manifest identity cleanup and recovery | `FileActionLauncher` in `RedSalamander/FileActionLauncher.{h,cpp}` | This remains EXE-local because the exact-object lease is coupled to external-process completion and the crash-recovery age policy is coupled to the private FileAction namespace. The live lease records manifest/root `FILE_ID_INFO`, closes creation handles before launch, and reopens the root and candidate no-follow without delete sharing for one identity-check/disposition interval. Recovery validates and holds the private root, uses age/entry/delete/time/pass bounds plus a continuation cursor, and leaves uncertain objects untouched. `Common::Files::CreateDeleteOnCloseTemporaryFile` is incompatible because it retains a handle and delete-on-close share contract; `Common::Files::LocalFileTransaction` is incompatible because this is neither promotion nor output replacement. A second consumer with the same exact-object and recovery semantics requires an extraction review; do not copy the local primitives. |
| Template-owned quoted argument content | `AppendWindowsQuotedArgumentContent` in `RedSalamander/FileActionLauncher.cpp` | This local operation escapes only the content of a macro already surrounded by quotes in the configured template. Its trailing-backslash behavior is deliberately different from quoting one complete argv item. All ordinary unwrapped arguments MUST use `Common::Process::QuoteWindowsCommandLineArgument`; do not generalize or duplicate the local content-only operation without a second compatible consumer and conformance review. |

Native Curl single/bulk admission and directory-size root lookup share
`FileSystemCurlInternal::GetEntryInfo` in `FileSystemCurl.Imap.cpp`. For FTP/SSH it
drains `CurlDirectoryCursor`, retains only a matching entry plus the current row,
rejects duplicate matches, and publishes output only after complete validation.
The optional checkpoint preserves size progress/cancellation; optional metrics
report owned lookup path/metadata reservations, not opaque transport memory.
The IMAP branch retains its mailbox/UID semantics and is not claimed bounded.
Do not restore a separate whole-parent `DirectoryEntryCache` for bulk mutations.
`CurlEasyPool` also owns the lazily allocated cursor multi connection cache in the
same exclusive borrow as its easy handle. Cursors must detach before return; pool
return clears callbacks and retains both owners under the existing idle/expiry rules.
Each entry keeps distinct blocking and cursor easy handles. Alternating the APIs on
one easy handle destroys libcurl's private blocking-transfer cache; both easy handles
must be reset before idle publication and destroyed before the retained multi cache.

## Enforcement

The domain specs cited by each consumer define behavior. `Tools/Tests/*SourceContracts.Tests.ps1` guards the
high-risk migrations, including render-target ownership, modal windows, plugin lifetime/configuration, packed
buffers, posted payload coalescing, and test-support adoption. A source guard complements behavioral tests; it
must not replace them or force semantically different policies into one helper.

## Viewer DxUi adapter

`Common/ViewerDxUiTheme.h` maps validated `ViewerTheme` values field by field into
`DxUi::ThemeColors` and calls the public palette factory. The library record owns its
size; no layout cast or DxUi C++ ownership crosses the plugin ABI. `ProductUiTests`
covers the complete mapping and base-theme accent polarity.
